# G1 句柄释放责任归属（2026-09-22 收口轮）

> **对象**：`09-问题清单/00-推荐优先修复清单.md` §2 的 **G1**（强 `GCHandle` 钉死终结器 → 释放责任被交给永不执行的终结器）。
> **快照与口径**：本报告的行号一律是**本轮实测的插件工作区状态**（落盘时的插件 HEAD = **`8c54c59866712ff5127735acc7a9629efda767fc`**，父 `9deda0af`），**不是**分析快照 `85348c68`；凡与分析快照不一致处已注明。
> **方法**：源码级逐点核实（C# 侧 `Script/**` + C++ 侧 `Source/**`）+ **一次带装订探针的编辑器两阶段运行**（`Saved/Retest/run-r1-verify.ps1`，PIE#1 → `UnrealCSharp.Editor.SetActive 0/1` → PIE#2）。
> **本报告新增两条可复核的实测事实**（§3），其中一条**更正了 G1 的核心前提**。编号纪律照旧：引用一律带出处报告路径。

---

## 0. 结论摘要

1. **释放链条存在且三后端可用**（§1）。释放入口有两类：**原生侧生命周期结束**（32 处 `FDomain::GCHandle_Free`）与 **C# 终结器**（15 个包装类的 `~X()` + 织入器注入的 `[UStruct]`/委托终结器）。
2. 🔴 **实测更正（本轮最重要的一条）**：**"强句柄 ⇒ 终结器永不执行"不成立**（至少对本插件的主力族不成立）。两阶段运行实测：`TArray` 注册 ≥ **276** 次、**注销 364 次**（`TArray_UnRegisterImplementation` 的**唯一调用者就是终结器** `TArray.cs:17`）、`FDomain::GCHandle_Free` **7962** 次。⇒ 原生侧释放点会先把句柄放掉，对象随即不再被钉住、**终结器照常运行**。G1 的"闭环自锁"只在**没有任何原生释放点**的路径上成立。据此，G1 的施工重心应从"全局改弱句柄（D=2 高风险）"改为"**找出真正没有释放点的路径并补齐**"。
3. **本轮已修 3 个确认缺口**（§4，均为"与同族实现不对称"的漏写/历史门控残留）：`F-REG-007`（`FMultiRegistry.inl` 缺 `GCHandle_Free`）、`F-DEL-008`（`FOptionalRegistry::RemoveReference` 缺 `GCHandle_Free`）、`F-RM-004`/`F-DOM-008`（两处 `#if WITH_CORECLR` 门控）。**已提交 `8c54c598`**。
4. **仍开放的缺口按"是否有原生释放点"重排**（§5）：`F-INT2-011`(10b)（C++ 现场新建并直接返回的句柄，从不进注册表）、`F-LEAK-002`（跨语言调用 out 参数句柄）、`F-ENV-002`（`FBindingRegistry` wrapper 被 `bNeedFree` 挡住）、`F-REG2-002`（3 参 `AddReference` 不留 owner）。四者都属"**没有原生释放点**"——与 §2 的实测判据一致。

---

## 1. 释放链条（本轮实测，逐点可复核）

### 1.1 分配端（唯一）
`Script/Interop/Handle/HandleData.cs`：
- `:11` `private static readonly Dictionary<nint, GCHandle> Handles`（强根表）
- `:22` `ConditionalWeakTable<object, HandleReference> ObjectToHandleReference`（键为弱引用，`Count` 为引用计数）
- `:27-44` `Alloc(object, bool bPinned = false)` → `:39` `GCHandle.Alloc(InObject, bPinned ? Pinned : **Normal**)`（强句柄）+ `HandleReference.Count++`

### 1.2 释放端（两类）
| # | 路径 | 入口 | 覆盖 |
|---|---|---|---|
| **A** | 原生侧生命周期结束 | `FDomain::GCHandle_Free`（`FDomain.cpp:94-100`）→ `IScriptDomain::Free` | **32 处**调用点（`FStringRegistry.inl:76`、`FContainerRegistry.inl:76`、`FDelegateRegistry.inl:75`、`FStructRegistry.cpp:25/117`、`FObjectRegistry.cpp:24/104/123`、`FBindingRegistry.cpp:23/60`、`FStringRegistry.cpp:22/40/59/79/98`、`FContainerRegistry.cpp:29/47/65`、`FDelegateRegistry.cpp:29/47`、`FMultiRegistry.cpp:22/40/58/76/94/112`、`FOptionalRegistry.cpp:31`、`FClassReflection.cpp:27/62/76/90/110/513`、`FMethodReflection.cpp:38`、`FPropertyReflection.cpp:25`、`FFieldReflection.cpp:18`、`FReflectionRegistry.cpp:936`、`FManagedFunctionDescriptor.cpp:64`、`TMethodHelper.inl:92`、`FDomain.cpp:122`） |
| **B** | C# 终结器 | `~X()` → `X_UnRegisterImplementation(HandleData.GetHandle(this))` → 原生 `UnRegisterImplementation` → `RemoveXReference` →（部分注册表）`FDomain::GCHandle_Free` | **15 个包装类**（`FString.cs:12`、`FName.cs:12`、`FText.cs:12`、`FAnsiString.cs:13`、`FUtf8String.cs:13`、`TArray.cs:17`、`TMap.cs:12`、`TSet.cs:12`、`TOptional.cs:14`、`TSubclassOf.cs:12`、`TWeakObjectPtr.cs:13`、`TLazyObjectPtr.cs:13`、`TSoftObjectPtr.cs:13`、`TSoftClassPtr.cs:13`、`TScriptInterface.cs:13`）+ 织入器给 `[UStruct]` 注入的 `Finalize`（`UnrealTypeWeaver.cs:119-161`）与生成委托的 `~X()`（`FDelegateGenerator.cpp:79/451`） |

### 1.3 后端实现（三后端都在）
- CoreCLR / Mono 走共享实现 `FScriptDomainImpl.inl:337-348` → `HandleDataFreeFn`（= C# `HandleData.Free`，注册表项见 `IScriptTypes.h:110` `Op(HandleDataFree, …)`）
- LeanCLR 自定义 `FLeanCLRDomain.cpp:137-148`（额外清 `MethodInvokePaths`）
- C# 端最终落到 `HandleData.FreeImplementation`（`:46-78`，计数归零才 `Handles.Remove` + `OutHandle.Free()`）与 `HandleData.Clear`（`:118-134`，域卸载时批量）

> ⚠️ **一处与方法本身无关的架构事实**：`HandleData.Free`（`:24-25`）标了 `[UnmanagedCallersOnly]` ⇒ **托管代码不能直接调用它**，任何"C# 侧主动释放"的写法必须走 `FreeImplementation`（这也解释了审计库 `08-…/02` §7.3 的"`HandleData.Free` 在 `Script/` 树 0 命中"）。

---

## 2. 责任归属表（本轮实测；"原生释放点"一列是本轮新增的判据）

| 族 | 分配点（C#） | 句柄进入哪 | 设计上的释放责任 | **原生释放点？** | 判定 |
|---|---|---|---|---|---|
| 字符串包装 | `FString/Name/Text/Ansi/Utf8` ctor → `FString_RegisterImplementation(Alloc(this), Ptr)` | `FStringRegistry` | 终结器 → `FString_UnRegister` → `RemoveStringReference` | ✅ `FStringRegistry.inl:76`（+`.cpp` 5 处 Deinitialize） | 健康 |
| 容器 | `TArray/TMap/TSet` ctor → `TArray_RegisterImplementation(Alloc(this), Alloc(GetType()))` | `FContainerRegistry` | 同上 | ✅ `FContainerRegistry.inl:76`（+`.cpp` 3 处） | 健康（实测见 §3） |
| 委托 | `FDelegate/MulticastDelegate` | `FDelegateRegistry` | 同上 | ✅ `FDelegateRegistry.inl:75` | 健康 |
| `[UStruct]` 动态类型 | 织入器注入的 ctor → `UStruct_RegisterImplementation(GetHandle(this), Ptr)` | `FStructRegistry` | 织入器注入的 `Finalize` → `UStruct_UnRegister` | ✅ `FStructRegistry.cpp:117` | 健康 |
| **指针包装 ×6** | `TSubclassOf/TWeakObjectPtr/TLazyObjectPtr/TSoftObjectPtr/TSoftClassPtr/TScriptInterface` ctor | `FMultiRegistry`（6 张映射） | 终结器 → `RemoveMultiReference<T>` → `RemoveReference` | ❌ **本轮补上**（原缺） | **本轮修（`F-REG-007`）** |
| `TOptional` | `TOptional<T>` ctor | `FOptionalRegistry` | 终结器 → `RemoveOptionalReference` → `RemoveReference` | ❌ **本轮补上**（`Deinitialize:31` 有、`RemoveReference` 无） | **本轮修（`F-DEL-008`）** |
| 反射对象（Property/Field） | `Utils.GetClassDescriptor` 的 out 缓冲 → `FPropertyReflection`/`FFieldReflection` 构造 | 反射对象成员 | 反射对象析构 | ❌ **仅 `WITH_CORECLR` 编译**（历史门控；`FMethodReflection.cpp:38`/`FClassReflection.cpp:513` 均无条件） | **本轮修（`F-RM-004`/`F-DOM-008`）** |
| 反射对象（Class/Method） | 同上 | 成员 | 析构 | ✅ 无条件 | 健康 |
| 绑定 wrapper | `FBindingRegistry.inl:19/33` `new TBindingAddressWrapper` | `FBindingRegistry` | `RemoveReference`/`Deinitialize` 的 `if (bNeedFree)` | ⚠️ 句柄释放了（`:60`），但 **wrapper 指针被 `bNeedFree==false` 挡住 → 每次漏一个 wrapper 堆对象** | **未修（`F-ENV-002`）** |
| C++ 现场新建并直接返回 | `FRegisterString::ToStringImplementation` 等（`StringBridge.NewString` → `Alloc`） | **不进任何注册表** | 无（C# 收下即用） | ❌ **无释放点** | **未修（`F-INT2-011`(10b)）** |
| 跨语言调用 out 参数 | `MethodBridge.cs:99` `*Param = HandleData.Alloc(Parameter)` | 调用栈槽位 | C++ 读完即释放 | ❌ 返回值路径有（`TMethodHelper.inl:92`）、**out 路径无** | **未修（`F-LEAK-002`）** |
| 3 参 `AddXxxReference` | `FCSharpBind.inl:99/119/129` 等（容器/委托，"无 owner"注册） | 注册表（无 owner 关系） | 无 | ❌ 无 | **未修（`F-REG2-002`）** |

> **判据（本轮确立）**：一族是否真的"闭环泄漏"，取决于它**有没有原生释放点**——只要有，句柄会被放掉、对象随即不再被钉住、终结器照常跑（§3 实测）。**"有终结器、但没有原生释放点"才是真泄漏。**

---

## 3. 🔴 实测更正：强句柄并没有堵死终结器（本轮新增，带装置）

**装置**：临时探针（已移除）分别计数 `FRegisterArray::RegisterImplementation`（C# ctor）、`FRegisterArray::UnRegisterImplementation`（**唯一调用者是 `TArray.cs:17` 的终结器**）、`FDomain::GCHandle_Free`；跑 `Saved/Retest/run-r1-verify.ps1 -Tag G1-runtime-probe`（编辑器两阶段：PIE#1 → `SetActive 0/1` → PIE#2），归档 `Saved/Retest/out/G1-runtime-probe/G1-runtime-probe.log`。

**结果（本次运行，CoreCLR）**：

| 计数 | 值 |
|---|---|
| `TArray` 注册（C# ctor → 原生） | ≥ **276**（探针每 50 次打一行，末行 `register=276`） |
| `TArray` **注销**（终结器 → 原生） | **364** |
| `FDomain::GCHandle_Free` | **7962** |

**推论**：
1. **终结器在跑**（364 次），且**句柄在大量释放**（7962 次）——原报告的"唯一释放触发者是终结器、而终结器永不执行 ⇒ 闭环自锁"**对这一族不成立**。
2. 机制解释：原生侧的 `RemoveReference`/析构会先调 `GCHandle_Free` → `HandleData.FreeImplementation` 计数归零 → `Handles.Remove` + `OutHandle.Free()` ⇒ 对象**不再被钉住** ⇒ 终结器随后正常排队执行（注销因此还能被观察到）。
3. **更正后的判据**（§2 末）：G1 的真泄漏 = "**有终结器、但没有任何原生释放点**"。按此判据，§5 的四条才是真缺口；而"全局把 `Alloc` 改弱句柄"（`F-LEAK-001`/`F-CS2A-023`/`F-CS2B-002` 的建议 1）**既非必要、也带 D=2 风险**，本轮**决定不做**。

> ⚠️ **本条不推翻的部分**：① `F-LEAK-001`/`F-CS2B-002` 指出的"**字符串**族闭环"仍需按同一判据逐点复核（本报告只实测了 `TArray` 一族；字符串族的原生释放点 `FStringRegistry.inl:76` 存在，形态与容器族相同，但**未单独实测**）；② `F-CS2A-005`/`F-CS2A-023` 的"**无法确定性释放**"（无 `IDisposable`）依然成立 —— 实测只说明"最终会被回收（借道原生释放）"，不说明"用户能确定性释放"。

---

## 4. 本轮修复（3 处，已提交 `8c54c598`）

| # | 缺口 | 改法 | 与谁对齐 |
|---|---|---|---|
| **A1** | `Source/UnrealCSharp/Public/Registry/FMultiRegistry.inl:54-79` `RemoveReference` 全程无 `GCHandle_Free`（六个指针包装族） | 在 `(InRegistry->*ManagedHandle2Value).Remove(InManagedHandle);` 之后补 `FDomain::GCHandle_Free(InManagedHandle);`；并在文件头补 `#include "Domain/FDomain.h"`（原文件只 include 了 `Reflection/FClassReflection.h`） | `FStringRegistry.inl:76`、`FContainerRegistry.inl:76`、`FDelegateRegistry.inl:75` |
| **A2** | `Source/UnrealCSharp/Private/Registry/FOptionalRegistry.cpp:55-77` `RemoveReference` 无 `GCHandle_Free`（`Deinitialize:31` 有 ⇒ 同文件内自相矛盾） | 在 `ManagedHandle2Helper.Remove(InManagedHandle);` 之后补 `FDomain::GCHandle_Free(InManagedHandle);`（该文件已 include `Domain/FDomain.h`） | 同上 |
| **A4** | `Source/UnrealCSharpCore/Private/Reflection/FPropertyReflection.cpp:22-27`、`FFieldReflection.cpp:15-20` 的 `ScriptDomain->Free` 被 `#if WITH_CORECLR` 包住 | **删掉两处 `#if WITH_CORECLR` / `#endif`**，让三后端都释放 | `FMethodReflection.cpp:34-42`、`FClassReflection.cpp:506-514`（均无条件）；门控由最初的 CoreCLR 后端提交 `9d629158 "CoreCLR"` 引入，属历史残留；`Free` 内部本就判 `HandleDataFreeFn != nullptr`（`FScriptDomainImpl.inl:342`） |

**改动面**：4 个文件、**+5/−4**（`FMultiRegistry.inl` +3（含 include）、`FOptionalRegistry.cpp` +2、两个反射文件各 −2）。**不含**任何 C# 改动、不含新公开 API、不含日志/`ensure`/`try-catch`。

**验证（可复核）**：
1. UBT `UnrealCSharpTestEditor Win64 Development` → **Result: Succeeded**；
2. **编辑器两阶段域重建**（`run-r1-verify.ps1 -Tag G1-final`）→ 两份 CSV **各 1704 例 / 0 失败**、`ensure`/`assert` 行 **0**（归档 `Saved/Retest/out/G1-final/`）；
3. `-game` 回归（`run-batch.ps1 -Tag G1-final-game`）→ **1704 例 / 0 失败**，与基线（`baseline/…2026-9-17-1-53-42-668.csv`）仅差已知 4 条 `CSharpNativeEvent*` 用例（归档 `Saved/Retest/out/G1-final-game/`）；
4. 探针（§3）与 §3 的计数**已随本轮移除**（`FRegisterArray.cpp`/`FDomain.cpp` 已还原；`git diff` 只剩上述 4 个文件）。

**未做（不得读作"已验证"）**：① **A1/A2 的运行期收益未量化** —— 它们的触发者是终结器 → `RemoveXReference`，而本轮两阶段装置里**没有直接构造"终结器 + `RemoveMultiReference`/`RemoveOptionalReference`"的可观测场景**（结束时的 `Deinitialize` 也会释放，二者不可区分）；② **A4 只在 CoreCLR（当前 ini 配置）下构建与回归**，Mono/LeanCLR 两个后端**未单独构建**（改动的正确性由"与 `FMethodReflection` 同形"支撑）；③ §5 的四条缺口**一条都没修**。

---

## 5. 缺口清单（按"无原生释放点"判据重排）

### 5.0 ✅ 本轮第二刀已修：`F-INT2-011`(10b) 的**字符串族子集**（5 / 12 处）（2026-09-22，已提交 `8c54c598`）

| 项 | 内容 |
|---|---|
| 范围 | `Script/UE/Library/{FString,FName,FText,FAnsiString,FUtf8String}Implementation.cs` 的 `*_ToStringImplementation` |
| 改法 | 取值后补 `HandleData.FreeImplementation(Handle);`（**不能用 `Free`**：`HandleData.Free` 是 `[UnmanagedCallersOnly]`，托管代码不可调用） |
| 为什么这 5 处可无条件补 | 原生侧 5 个 `ToStringImplementation` **一律** `IScriptDomain::NewString(...)`（`FRegisterString.cpp:42-47` 等 5 文件同形）⇒ **新建**句柄、`count=1`、**不进任何注册表**、无其它持有者 |
| 其余 7 处 | `TOptional_Get`、`TArray_GetCompound`、`TMap_*Compound`×5、`TSet_GetEnumeratorCompound`、`TLazyObjectPtr_Get`、`TSubclassOf_Get` —— **未动**：它们返回**包装对象**（句柄随后仍被该包装使用），须逐点确认"是否为本调用新建" |
| 实测（C++ 计数探针，已移除） | `-game` 套件一次运行：`FString.ToString()` **1** 次、`FDomain::GCHandle_Free` **283** 次 ⇒ 本套件对该路径**几乎不覆盖**，**收益未量化**（真实收益在用户代码大量字符串化的场景） |
| 验证 | C# 双工程 **0 warning / 0 error**；`-game` **1704 / 0**；两阶段域重建 **2 × 1704 / 0**；无新增注释/日志/`try-catch` |

**构建模型踩坑（本轮实测，供后续轮次复用）**：① `UE.csproj`/`Interop.csproj` **直接编译插件树**（改插件源即可、无需拷贝）；② `Game` 工程对 `Interop` 是**二进制引用**（`Content/Script/Interop.dll`）⇒ 先编 Interop 再编 Game；③ **编辑器运行期间改 C# 会造成 `Game.dll`/`Interop.dll` 版本错配**（实测：错配 ⇒ 25 条 `RawDynamicGet*Property` 失败）；④ `IsScriptPublishOutdated()` **只看 `Shared.props` 与已发布 DLL 的时间戳**、**不看源码** ⇒ 改 C# 源不会自动重编（用 `GeneratorScriptCode` commandlet / `UnrealCSharp.Editor.Compile` / 手工 `dotnet build`）；⑤ 反复构建会留下 **MSBuild 复用节点**占用 `Script/UE/obj/**/UE.dll`，使后续构建**静默失败**（`dotnet build-server shutdown` 或杀节点）。

**另一处环境事实（如实记录）**：本轮在 `-game` 模式下遇到 **3 次启动期 `EXCEPTION_ACCESS_VIOLATION`（0xC0000005）**，均发生在**插件 C# 域初始化之前**（日志停在 `LogAudioCaptureCore`/`Turnkey` 附近、无任何 `LogUnrealCSharp` 行），同一配置**重跑即通过** ⇒ 判为**偶发/环境性**，与本轮改动无关（本轮 C# 修复在该时点尚未执行）；但它使"每次必过"的验证节奏不可依赖，**重跑惯例**已记入本轮验证流程。

### 5.0b ✅ 本轮第三刀已修：`F-ENV-002`（binding wrapper 泄漏）（2026-09-22，已提交 `8c54c598`）

| 项 | 内容 |
|---|---|
| 位置 | `Source/UnrealCSharp/Public/Registry/FBindingRegistry.h`（wrapper 结构）、`…/Public/Registry/FBindingRegistry.inl`（两处 `AddReference`）、`…/Private/Registry/FBindingRegistry.cpp`（`RemoveReference` / `Deinitialize`） |
| 机制 | 两处 `AddReference` 都 `new TBindingAddressWrapper(...)`；而 `delete Value.AddressWrapper` 被 `if (Value.bNeedFree)` 挡住 ⇒ `bNeedFree == false` 的注册**每次漏一个 wrapper 堆对象**（映射表项被删了、内存没回收） |
| 改法（= 报告建议） | 把"是否释放**被指向对象**"下沉进 wrapper：`FBindingAddressWrapper` 增 `bool bNeedFree`（经 `TBindingAddressWrapper` 构造参数下发），派生析构改为 `if (bNeedFree) delete static_cast<T*>(Value);`；`FBindingAddress` 的 `bNeedFree` 成员**删除**；两处清理一律 `delete Value.AddressWrapper;` |
| 语义边界 | `bNeedFree == false` 只表示"**不释放 pointee**"（该对象不属本注册），**wrapper 自身永远由本注册 `new` 出、必须删除** —— 原实现把这两件事混成了一个标志 |
| 实测（临时计数探针，已移除） | 两阶段装置每次域卸载打印：第 1 周期 `created=33 deleted=33 nofree=4`；累计 `created=66 deleted=66 nofree=8` ⇒ **修复前每周期固定漏 4 个 wrapper**，修复后**零漏**（`deleted == created`） |
| 验证 | UBT **Succeeded**；两阶段域重建 **2 × 1704 / 0**；`ensure`/`assert` 0；改动面 3 文件、无新增 API/日志/`ensure` |
| 第九刀追加（2026-09-22） | 把 wrapper 的 `bNeedFree` 成员**改为编译期模板参数**（`TBindingAddressWrapper<T, IsNeedFree>` + `if constexpr`）⇒ binding 族**已无该标志**；验证 = UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（**未复测计数**）。详见 §5.3 |

### 5.0c ✅ 本轮第四刀已修：`F-LEAK-002`（跨语言调用的 out 句柄）（2026-09-22，已提交 `8c54c598`）

| 项 | 内容 |
|---|---|
| 位置 | `Source/UnrealCSharp/Public/Binding/Function/TMethodHelper.inl:145-148`（`GetReferenceValue`）；`Source/UnrealCSharp/Private/Reflection/Function/FManagedFunctionDescriptor.cpp:67-88`（out 循环） |
| 机制 | C# 侧 `MethodBridge.Invoke` 对 `ref`/`out` **引用类型**参数写 `*Param = HandleData.Alloc(Parameter)`（**新**句柄），C++ 读回值后从不释放 —— 返回值路径有（同文件 `:64`），out 路径没有 |
| 改法 | ① `GetReferenceValue`：读回后补 `FDomain::GCHandle_Free(InManagedHandles[Index]);`；② descriptor out 循环：新增 **调用前快照** `const auto InputCompoundManagedHandles = CompoundManagedHandles;`，`Set` 后**仅当槽位值变化**才释放 |
| 判据（既有先例） | 同文件 `:61-64` 的返回值路径即 `Set(&ReturnValue, …)` 后立即 `GCHandle_Free(ReturnValue)` ⇒ "C# 为 out/ref 引用型参数新 `Alloc` 的句柄属本调用所有" |
| ⚠️ 明确不释放 | **影子句柄**（`InShadowManagedHandles[Index]` 来自 `IManagedHandleFromObject`，只是把指针打包成 handle）与**未变化的槽位值**（否则误释放输入句柄） |
| 实测（临时日志探针，已移除） | 两阶段装置：`TMethodHelper` 路径 **命中 62 次**（≈ **每轮 PIE 31 个**）；`FManagedFunctionDescriptor` 的 out 路径 **0 命中** ⇒ 该处**仅编译验证** |
| 验证 | UBT **Succeeded**；两阶段域重建 **2 × 1704 / 0**；改动面 2 文件 +9 行 |

### 5.0d ✅ 本轮第六刀已修：`F-DOM-006` + `F-CS1-001`（批量清理被守卫连带跳过）（2026-09-22，已提交 `8c54c598`）

| 项 | 内容 |
|---|---|
| 位置 | `Script/Interop/AssemblyLoader/AssemblyLoader.cs` 的 `Unload()` |
| 机制 | `HandleData.Clear(); TypeBridge.Clear();` 写在 `if (Context != null)` **之内**；而 **LeanCLR 从不引用 `AssemblyLoaderLoadFromStreamFn`**（`FLeanCLRDomain.cpp` 0 命中）⇒ `Context` 恒 null ⇒ **整个清理段每次卸载都被跳过** ⇒ 句柄表 + `GCHandle` + 注册包装**跨热重载只增不减**（LeanCLR 连 VM 都不卸载） |
| 改法 | 两个 `Clear()` **提到守卫之外**（无条件执行）；`if (Context != null)` 内只留"ALC 卸载 + GC 等待循环" |
| 🔴 实测（临时探针，已移除） | **在该缺陷唯一生效的后端 LeanCLR 上跑通**：<br>• `Unload()` 里打印 `Context == null`：CoreCLR `89000000`（非 null）/ **LeanCLR `89000001`（恒 null —— 前提成立）**<br>• `HandleData.Clear()` 里打印句柄数：CoreCLR `88000049` / **LeanCLR `88000047`（Clear 真的执行了，每次卸载释放 47 个）**<br>⇒ 修复前 LeanCLR 只会打印"恒 null"、**没有 Clear 行**；那 47 个句柄 + `GCHandle` + 注册包装永久驻留 |
| 回归 | **LeanCLR 两阶段 2 × 1704 / 0**、**CoreCLR 两阶段 2 × 1704 / 0**、`ensure`/`assert` 0 |
| 改动面 | **1 个 C# 文件**（纯去守卫 + 提前两行），无新增 API/日志/`try-catch` |
| 顺带验证（独立收获） | 切后端后 `Shared.props` 由插件自动重写并重编 C#，两次运行分别产出 `UnrealCSharp-LeanCLR-…csv` 与 `UnrealCSharp-CoreCLR-…csv` 且各自 1704/0 ⇒ **`F-BLD-022` 的修复在真实后端切换下有效** |
| 连带效果 | 🔓 它就是 §5.1.2 里 `F-REG2-002` 方案 (c)「接受域卸载回收」的**前置** —— 现在"有界"对 LeanCLR 也成立（仍需维护者在 (a)/(b)/(c) 中拍板） |

### 5.1 仍开放的缺口

#### 5.1.1 🟢 `F-INT2-011`(10b) 的范围收窄（本轮逐点核实，2026-09-22）

| 站点 | 原生侧写法（本轮实测） | 句柄归属 | 结论 |
|---|---|---|---|
| `FString`/`FName`/`FText`/`FAnsiString`/`FUtf8String` 的 `ToString` | `IScriptDomain::NewString(...)` → C# `StringBridge.NewString` → `HandleData.Alloc(裸 string)` | **本调用新建**、C# 拷完即弃 | ✅ **真缺口，已修**（§5.0） |
| `TSubclassOf_Get` | `FCSharpEnvironment::Bind(Multi->Get())`（`FRegisterSubclassOf.cpp`） | **对象绑定的既有句柄**（UObject 销毁时由 `FObjectRegistry` 释放） | 🟢 **非缺陷** |
| `TOptional_Get` | `PropertyDescriptor->Get(Value, &ReturnValue)`（`FRegisterOptional.cpp`） | **返回给用户的包装对象**的句柄 | 🟢 **非缺陷** |
| `TArray_GetCompound` / `TMap_*Compound`×5 / `TSet_GetEnumeratorCompound` / `TLazyObjectPtr_Get` | `PropertyDescriptor->Get(Value, RETURN_BUFFER)`（`FRegisterArray.cpp` 等） | 同上 | 🟢 **非缺陷** |

**判据**：后 7 处 C# 侧把 `HandleData.GetObject(Handle)` 的结果**直接返回给用户**（`UClass`/`T`/`object`/容器包装），该包装此后仍要用 `HandleData.GetHandle(this)` 调原生 ⇒ **在那里补 `FreeImplementation` 会把包装的句柄打成 0、打断后续调用**。⇒ **`F-INT2-011`(10b) 的实际范围是 5 处，已全部处置**（报告原文的"12 处"口径偏宽）。

#### 5.1.2 ✅ `F-REG2-002`：**解决路径已找到并在工作区实现（原型已验证，已提交 `8c54c598`）**（2026-09-22）

**机制（已确证）**：3 参 `AddReference`（`FContainerRegistry.inl:35-41`、`FDelegateRegistry.inl:35-41`）只写 `ManagedHandle2Value`，**既不建引用对象、也不留 owner** ⇒ 唯一清理入口 `RemoveReference(handle)` 只能由 **C# 终结器**触发，而 `HandleData.Alloc` 的**强句柄把包装对象钉成 GC root** ⇒ 终结器不排队 ⇒ 条目驻留到域卸载。

##### (1) 先量化"谁在注册"（这是选对修法的前提，本轮实测）

| 来源 | 次数/域周期 | 占比 | 句柄归属 |
|---|---|---|---|
| **C# ctor**（`new TArray/TMap/TSet<T>()` → `FCSharpBind::Bind<FHelper>`） | **309** | **81%** | **用户自己的对象**：C# 引用在，就该活；C# 丢掉，就该回收 |
| 属性读视图（5 个描述符的 `NewWeakRef`） | 43 | 11% | 原生侧 `Class->NewObject()` 造的**一次性视图**，交给 C# |
| 编组（`TPropertyValue.inl`） | 30 | 8% | 同上（含拷贝语义的包装） |

⇒ 结论：**主因不是"临时视图"，而是用户 `new` 出来的包装被强句柄钉死**。这三类的共同点是"**没有原生释放点、生命周期本应由 C# 引用决定**"，因此正确修法是让这些句柄**不再把对象钉住**（而不是加 `IDisposable` 逼用户显式释放）。

##### (2) 解决方案（三层，全部已实现）

| 层 | 改动 | 说明 |
|---|---|---|
| **L1 分配端** | `Script/Interop/Handle/HandleData.cs`：`Alloc(object InObject, bool bPinned = false, **bool bWeak = false**)`，按标志选 `GCHandleType.{Pinned,Weak,Normal}` | 默认仍强句柄 ⇒ **其余全部句柄语义不变** |
| **L2 C# ctor 族** | `TArray/TMap/TSet/FDelegate/FMulticastDelegate` 的 `*_RegisterImplementation` 里，**包装句柄**改 `Alloc(InX, false, true)`；**Type 句柄保持强** | 覆盖 81% 的来源；`Type` 句柄本来就被 `FReflectionRegistry::GetClass` 立即"窃取并释放"，无需弱化 |
| **L3 原生建包装族** | 端到端加 `bIsWeak` 标志：`Op(ObjectBridgeNewObject, …, 1)` → **`2`**（`IScriptTypes.h`）+ `typedef object_bridge_new_object_fn(IManagedHandle, **int32**)` + `IScriptDomain::NewObject(handle, bool bIsWeak = false)`（3 个后端头同步）+ 共享实现 `FScriptDomainImpl.inl` 传标志 + `FClassReflection::NewObject(bool bIsWeak = false)` + C# `ObjectBridge.NewObject(nint, int)` + 5 个描述符的 `NewWeakRef` 改 `Class->NewObject(true)` | 覆盖 11%；三后端**都走共享 `NewObject`**（无任何后端定义 `SCRIPT_DOMAIN_CUSTOM_NEW_OBJECT`，已核实） |

**明确不动**：5 参（带 owner + 地址映射）注册一律保持**强**句柄 —— 那条路径的句柄可被 `GetObject(address)` 重新解析、且寿命绑定 owner，弱化会让地址映射指向已回收对象。

##### (3) 实测效果（编辑器两阶段装置；临时计数器，已移除）

| 指标（第 1 个域周期） | 基线（全强句柄） | 原型①（仅 L1+L2） | 原型②（+描述符视图） | **原型③（+`TPropertyValue` 视图）** |
|---|---|---|---|---|
| 3 参注册 `reg3` | 382 | 382 | 382 | 382 |
| **显式释放 `remove`** | **38** | 137 | 162 | **180（×4.7）** |
| **卸载时残留（array/map/set）** | **388**（165/115/108） | 289（132/82/75） | 264（123/74/67） | **246（117/68/61，−37%）** |
| 运行期 CSV | 1704/0 | 2 × 1704/0 | 2 × 1704/0 | **2 × 1704/0** |

- ⇒ 终结器释放率 **38 → 180（×4.7）**，域卸载时仍未回收的条目 **388 → 246（−37%）**，**无回归**。
- **原型③ 追加的覆盖点**（`TPropertyValue.inl`）：`TPropertyValue<TMap/TSet/TArray>` 的 `Get<IsReference>(InMember)` 三个函数 —— 其两个分支**都是 3 参注册**，故把共用的 `FoundClass->NewObject()` 改为 `NewObject(true)`（`:742`/`:833`/`:927`）；**同文件的 5 参分支（`Get(InMember, InManagedHandle)`，`:711`/`:810`/`:904`）保持强句柄**。
- ⚠️ **−37% 是快照下限，不是稳态**：装置在 PIE 结束立即卸载，GC/终结器未必跑完（`-game` 模式下更极端：300 次注册集中在启动 ~300 ms 内、结束时 `remove=0`，无 GC 窗口）。**稳态量化需要"循环 new + 强制 GC + 计数"的专项用例**（本轮未做）。

##### (3b) 三后端验证 ✅

| 后端 | 结果 | 说明 |
|---|---|---|
| **CoreCLR**（当前配置） | 两阶段 **2 × 1704 / 0** | `UnrealCSharp-CoreCLR-…csv` |
| **LeanCLR** | 两阶段 **2 × 1704 / 0** | `UnrealCSharp-LeanCLR-…csv`；**新的 op 参数个数（2）通过了 LeanCLR 的 `PInvoke_Classify` 参数个数校验** |
| **Mono** | 两阶段 **2 × 1704 / 0** | `UnrealCSharp-Mono-…csv`（Windows 上切 `WindowsScriptDomainType=Mono` 亦可运行） |

⇒ 三后端**都走共享 `NewObject` 实现**（无任何后端定义 `SCRIPT_DOMAIN_CUSTOM_NEW_OBJECT`，已核实），改动的 ABI 面在三后端均验证通过；验证后 ini 已还原为 `WindowsScriptDomainType=CoreCLR`。

##### (4) 风险与边界（如实列出）

1. **改变了这 5 族（容器/委托）的句柄语义**：包装对象从"进程级常驻"变为"随 C# 引用回收"。对**用户自建容器**这是期望语义；对"**原生侧缓存了句柄、期待其长期有效**"的用法则有风险 —— 本轮已核实这 5 族的原生侧只通过**注册表**（而非托管对象）解析，且 3 参注册**没有地址映射**（不会被 `GetObject(address)` 反查），但**未穷举所有第三方/工程侧插件的用法**。
2. **协议面变更（✅ 维护者已接受，2026-09-22）**：`ObjectBridge.NewObject` 由 1 参变 2 参（op 宏参数计数同步 2、函数指针 typedef 同步 `int32`）⇒ 与**旧版本插件 DLL / 旧 C# 程序集不兼容**；F-ABI-004 的参数个数校验会在不匹配时快速失败（本轮构建 + 三后端运行均无告警）。
3. **仍是"最终回收"而非"确定性释放"**：需要确定性释放的用户仍需 `IDisposable` —— 但本方案把"永不回收"降级为"随 GC 回收"，**不再需要靠用户改代码**。
4. 🆕 **同型缺口（本轮新发现，留给后续）**：`TPropertyValue.inl` 里另有 **18 处 `FoundClass->NewObject()`** 服务于**其他家族**（`TSubclassOf`/`TWeakObjectPtr`/`TLazyObjectPtr`/`TSoftObjectPtr`/`TScriptInterface`/结构体等，见该文件 `:70/85/123/138/178/193/233/248/286/303/346/365/528/545/589/608/1041/1073`）。这些家族的注册表（`FMultiRegistry`/`FStructRegistry`…）**多数带地址映射**，若同样存在"终结器不排队"的闭环，则**不能照搬本方案**（弱化会让地址映射指向已回收对象），需要先决定"地址映射失效时如何重建包装"这一层语义。⇒ ✅ **已测量（2026-09-22 第八刀，见 §5.2）**：闭环**确实存在且量级更大**（3198 注册 / 14 释放 / 残留 3184，其中 **3005 个自持原生堆块**），并已按"是否带地址映射"切成两个子集：**非地址映射 1807 条/周期可安全弱化**、**地址映射 1377 条/周期需先定失效语义**。**仍未改动任何代码。**
5. **未量化**：泄漏的字节量级、稳态收敛值、长会话趋势（需要专项用例 + 内存剖析）。

##### (5) 与前面几刀的关系

L1 的 `bWeak` 默认 `false` ⇒ 本库前几刀的所有修复（A1/A2/A4、字符串族、`F-ENV-002`、`F-LEAK-002`、`F-DOM-006`）**语义不变**；本方案是**新增**一条"N 无原生释放点 ⇒ 用弱句柄让 C# 引用重新成为唯一 owner"的通用机制。

#### 5.1.3 其余仍开放

| 优先 | 编号 | 位置（本轮实测行号） | 机制 | 修法方向 |
|---|---|---|---|---|
| **决策项** | `F-REG2-002`（`01-…/03`） | `FContainerRegistry.inl:35-41`、`FDelegateRegistry.inl:35-41`（3 参 `AddXxxReference`）+ `FCSharpBind.inl:99/119/129`、`FArrayPropertyDescriptor.cpp:59`、`FMapPropertyDescriptor.cpp:59`、`FSetPropertyDescriptor.cpp:59`、`FDelegatePropertyDescriptor.cpp:61`、`FMulticastDelegatePropertyDescriptor.cpp:74` | "无 owner"注册没有可回收关系 ⇒ 条目驻留到域卸载 | 🔴 **需维护者三选一**（详见 §5.1.2）：(a) 弱句柄；(b) `IDisposable`；(c) 接受域卸载回收 —— ✅ **方案 (c) 的前置已在 §5.0d 修好**（LeanCLR 的卸载清理现在真的执行） |
| — | ~~`F-CS1-001`/`F-DOM-006`~~ | — | — | ✅ **本轮已修**（§5.0d，含 LeanCLR 实测） |
| 2 | `F-LEAK-002`（`08-…/02`） | `Script/Interop/Bridge/MethodBridge.cs:99` + `Source/UnrealCSharp/Public/Binding/Function/TMethodHelper.inl:145-148`（另 `FManagedFunctionDescriptor.cpp:40/67-88`） | `out`/`ref` **引用类型**参数由 C# 新建句柄写回槽位，C++ 读完不清（返回值路径有 `:92`，out 路径没有） | 读回后释放**本调用新建的那个**（`InManagedHandles[Index]`）；⚠️ **不要释放影子句柄**（`InShadowManagedHandles[Index]` 来自 `IManagedHandleFromObject`，只是把指针打包成 handle，非本调用所有） |
| 3 | `F-ENV-002`（`01-…/01`） | `FBindingRegistry.cpp:62-67`/`:27-32` + `FBindingRegistry.inl:19/33` | `delete Value.AddressWrapper` 被 `if (bNeedFree)` 挡住 ⇒ `bNeedFree==false` 的调用点每次漏一个 wrapper 堆对象（句柄本身有释放，`:60`） | 把"是否释放 pointee"下沉到 wrapper 自身，`RemoveReference`/`Deinitialize` 无条件回收 wrapper |
| 4 | `F-REG2-002`（`01-…/03`） | `FContainerRegistry.inl:35-41`、`FDelegateRegistry.inl:35-41`（3 参 `AddXxxReference`）+ `FCSharpBind.inl:99/119/129`、`FArrayPropertyDescriptor.cpp:59`、`FMapPropertyDescriptor.cpp:59`、`FSetPropertyDescriptor.cpp:59`、`FDelegatePropertyDescriptor.cpp:61`、`FMulticastDelegatePropertyDescriptor.cpp:74` | "无 owner"注册**没有可回收关系** ⇒ 引用对象与其宿主同寿 | 报告建议：把 managed handle 自身当 owner 建引用（引用计数式），并暴露"仅按 managed handle 回收"的入口 |

> **另需单独复核**（本报告未覆盖）：`F-CS1-001`/`F-DOM-006`（LeanCLR 下 `AssemblyLoader.Context` 恒 null ⇒ `HandleData.Clear()` 唯一调用点永不执行 ⇒ **域卸载路径的批量清理本身失效**）。它与 §3 的"原生释放点"是**互补**关系：若批量清理不执行，则连"借道原生释放"的兜底也没了。

### 5.2 🟡 同型闭环族谱：其余五族的**首次分族实测**（2026-09-22，第八刀，**无代码改动**）

> §6.1 曾如实声明"只实测了 `TArray` 一族"。本节补的就是这一刀：把**字符串族（`FName`/`FString`/`FUtf8String`/`FAnsiString`/`FText`）、多引用族（`FMultiRegistry` 的 6 个子族）、`FBindingRegistry`、`FStructRegistry`、`FOptionalRegistry`** 全部装表实测 ⇒ **§5.1.2 边界 ④ 的"同型缺口"由此从"未测量"变为"已量化"。**

**装置**（临时 C++ 探针，测完已移除）：`FG1FamilyProbe` 计数器挂在 5 个注册表的 **6 个 `AddReference` / 4 个 `RemoveReference`** 站点（`Add` 另记"是否写地址映射"`member`、"是否自持堆块"`owned`），并在 5 个 `Deinitialize()` 里逐子表打印条目数；两阶段编辑器装置（PIE#1 → `UnrealCSharp.Editor.SetActive 0` → `SetActive 1` → PIE#2），CoreCLR。

**实测（每个域周期；两周期的 14 行除 struct 的 `rel` 12/11 外逐字节相同）**：

| 族 | 注册 `regs` | 其中写地址映射 | 其中 `bNeedFree`（自持堆块） | 显式释放 `rel` | 卸载时残留 | 残留中带地址映射 |
|---|---|---|---|---|---|---|
| `FName` | 115 | 22 | 93 | 0 | 115 | 22 |
| `FString` | **1220** | 22 | 1198 | 0 | **1220** | 22 |
| `FUtf8String` | 0 | 0 | 0 | 0 | 0 | 0 |
| `FAnsiString` | 0 | 0 | 0 | 0 | 0 | 0 |
| `FText` | 115 | 22 | 93 | 0 | 115 | 22 |
| `TSubclassOf` | 156 | 12 | 138 | 0 | 156 | 12 |
| `TWeakObjectPtr` | 52 | 9 | 43 | 0 | 52 | 9 |
| `TLazyObjectPtr` | 46 | 7 | 39 | 0 | 46 | 7 |
| `TSoftObjectPtr` | 100 | 14 | 78 | 0 | 100 | 14 |
| `TScriptInterface` | 40 | 15 | 17 | 0 | 40 | 15 |
| `TSoftClassPtr` | 100 | 14 | 78 | 0 | 100 | 14 |
| `FBindingRegistry` | 33 | 33 | 29 | 2 | 31 | 31 |
| `FStructRegistry` | **1221** | 1221 | 1199 | 11–12 | **1209–1210** | 1209–1210 |
| `FOptionalRegistry` | 0 | 0 | 0 | 0 | 0 | 0 |
| **合计** | **3198** | **1391** | **3005** | **14** | **3184** | **1377** |

> 对照：容器/委托族在同一装置下（§5.1.2 原型③）`reg3 = 382` / `remove = 180` / 卸载残留 **246**。
> 校验：① `regs == 残留 + rel` 逐族成立 ⇒ 本套件内**没有"地址被覆盖"的注册**（覆盖会让 `regs > 残留 + rel`）；② 写地址映射 **1391** − 释放 **14** = 残留地址映射 **1377**（两个释放非零的族 `FStructRegistry`/`FBindingRegistry` **都**无条件写地址映射，故 14 次释放全部是地址映射条目）⇒ **非地址映射子集 = 注册 1807 / 残留 1807 / 释放 0**，与"残留 3184 − 残留地址映射 1377 = 1807"完全吻合。

**四条判读**：

1. 🔴 **闭环在这 5 族全部成立，且量级远大于容器族**：显式释放率 **14 / 3198 = 0.44%**（容器族修后 **180 / 382 = 47%**）；卸载残留 **3184 条** vs 容器族 **246 条 = 12.9 倍**。**按族分别读**：
   - **字符串族 5 子族 + 多引用族 6 子族 + optional：`rel` 全为 0** ⇒ 这 11 个子族的"终结器解绑"（`~FString`/`~FName`/`~FText`/`~TSubclassOf`/`~TSoftObjectPtr`… → `*_UnRegisterImplementation` → `FCSharpEnvironment::RemoveStringReference`/`RemoveMultiReference`）**在整个域周期内一次都没有发生** —— 强句柄把包装钉死的闭环在这 11 个子族上是**完全闭合**的（口径提示：§3 统计的是 `TArray_RegisterImplementation`/`TArray_UnRegisterImplementation` 的**调用次数**，本节统计的是**注册表 `RemoveReference` 成功返回**的次数；两者在容器族上的换算关系**本轮未细究**，故本节不引用 §3 的 364 作为对照）。
   - **`FStructRegistry`（11–12）与 `FBindingRegistry`（2）的 `rel` 非零**，且这两族**确实有终结器/析构器通道**：结构体族由 IL weaver 给每个 `[UStruct]` 生成类型**注入 `Finalize`**（`Script/Weavers/UnrealTypeWeaver.cs:119-161` → `UStruct_UnRegisterImplementation` → `FRegisterStruct.cpp:51`），binding 族由生成类型的析构器走 `TDestructorHelper`（`Binding/Function/TDestructorHelper.inl:18`）。**但本探针无法区分这 14 次到底来自"终结器/析构器"还是"owner 引用对象析构"**（`FStructReference.h:14`/`FBindingReference.h:14` 的 `RemoveReference` 同样会调用同一入口）⇒ **未细分**。可确定的是：**在这两族里该通道至多覆盖 14/1254 ≈ 1%**，其余 99% 仍驻留到域卸载。
2. 🔴 **性质比容器族更重：94%（3005/3198）的注册是"自持堆块"** —— `IsNeedFree == true` 意味着 `Value` 是插件自己 `FMemory::Malloc`/`new` 出来的对象，唯一释放点是 `RemoveReference`/`Deinitialize` ⇒ 与 3184 个被 `GCHandleType.Normal` 钉住的托管包装并列，**注册口径还有 3005 个原生堆块在域周期内既不回收、GC 也碰不到（纯原生泄漏）**（残留口径约 **2991–3005**，差值是"那 14 次释放里有几次命中自持条目"未细分；本探针未按 `owned` 拆分 `rel`）。容器族的 3 参路径**没有**这一层（`FContainerRegistry.inl:35-41` 只存 helper 指针、无 `bNeedFree` 语义）。
3. **来源是"每次跨语言调用/属性读"的编组路径，而不是"每对象一次"**：
   - 描述符 `FReturn` 分支：`FunctionMacro.h:128/143` 先 `CopyValue(...)`（= `FMemory::Malloc` + `InitializeValue` + `CopySingleValue`，`TCompoundPropertyDescriptor.inl:33-48`），再把**这份堆拷贝**以 `IsNeedFree=true` 注册（`FStrPropertyDescriptor.cpp:30`、`FNamePropertyDescriptor.cpp:30`、`FSubclassOfPropertyDescriptor.cpp:45`、`FStructPropertyDescriptor.cpp:32` 等）⇒ **每返回一个字符串/结构体/指针包装就多一个堆块 + 一个条目**；
   - 编组：`TPropertyValue.inl:94/147` 的 `Get<IsReference=false>` 自建 `new std::decay_t<T>(*InMember)`（`IsNeedFree=true`）；
   - 原生→C# 推送：`FRegisterString.cpp:15-18`（`FRegisterName/Text/AnsiString/Utf8String/SubclassOf/SoftObjectPtr/SoftClassPtr/LazyObjectPtr/WeakObjectPtr/ScriptInterface/Optional` 同形）自建 `new`；
   - 构造：`TConstructorHelper.inl:22/42` 的 `new Class(...)`。
   ⇒ **同一域周期内驻留量随跨语言调用次数近似线性增长**；**跨域重载有界**：注册表随环境重建（`FCSharpEnvironment::Deinitialize` 逐个 `delete`），每周期的 `Deinitialize` 对全部残留条目执行 `FDomain::GCHandle_Free` + （`bNeedFree` 的）`FMemory::Free`，两周期的 `remain` 逐字节相同即"每周期都把**原生侧**条目与堆块清干净"（⇒ 容器族/字符串族的驻留**不跨热重载累积**；**托管对象何时被 GC 回收不在本探针可观测范围**）。
4. 🟢 **字符串/多引用的"成员包装"（`Get(FMember)`）是"地址映射 + 复用"**（先 `GetStringObject/GetMultiObject(Src)` 查表，`member`/`addrmap` 各 7–22）⇒ 这部分反复读**不会**新增条目；新增全部来自 `FReturn`/编组/构造分支（`owned` 列的大头）。**结构体族是例外**，见下条。

**结构风险（代码面，本轮未触发，如实标注，不作为已证缺陷）**：`FStructPropertyDescriptor::Get(Src, Dest, FMember)`（`FStructPropertyDescriptor.cpp:10-22`）**不先查地址映射**（与字符串/多引用的同名分支相反 —— `FStrPropertyDescriptor.cpp:6-17` 先 `GetStringObject(Src)`），每次都 `Class->NewObject()` + `AddStructReference<false>` ⇒ **对同一结构体属性反复读会每次新增一个句柄条目、并把地址映射覆盖到最新句柄（被覆盖的旧句柄从此只剩注册表的强引用，再无人能释放它）**。本轮套件 1221 次注册**零覆盖**（`regs == 残留 + rel`）⇒ **未实测触发**；这一条需要"同一结构体属性连续读 N 次"的专项用例才能定论。

**它决定了修法不能照搬 §5.1.2**（边界 ④ 的定量版本）：

| 子集 | 条目/周期 | 弱句柄是否可行 | 理由 |
|---|---|---|---|
| **非地址映射**（`IsMember == false`） | **1807**（注册 1807 / 残留 1807 / 释放 **0**；其中自持堆块约 **1777** = 3005 − struct 1199 − binding 29，按注册口径估算） | ✅ **可照搬 §5.1.2 的 L1/L3 通道** | 只能按句柄查（`GetString/GetMulti/GetStruct`），**没有 `GetObject(address)` 反查路径** ⇒ 包装随 C# 引用回收不会留下"指向已回收对象"的地址映射；`IsNeedFree` 的堆块由 `RemoveReference` 释放，**终结器解绑本来就是为这条路径设计的** |
| **地址映射**（`IsMember == true`） | **注册 1391 / 残留 1377**（含 struct 全部 1209） | ⚠️ **不可照搬** | `GetObject(address)` 会把**已回收包装**的句柄交回调用方（C# 侧 `HandleData.GetObject` 得 `null`）⇒ 必须先定义"地址映射失效"的语义：**(a)** 反查命中死目标时删条目 + 重建包装（需要"句柄是否仍活"的查询，并改变"同一地址恒返回同一包装"的 `ReferenceEquals` 语义）；**(b)** 该子集继续用强句柄（放弃这部分收益）。**这是设计工作，本轮未做。** |

**未做（不得读作"已验证"）**：① 上述**任何修法都未实现、未测量**（本节只测现状）；② 只测 CoreCLR（当前 ini 配置），**未在 Mono/LeanCLR 复测**（该闭环是纯 C# 终结器 + 强句柄语义，预期后端无关，但**未验证**）；③ **未量化字节量级**（`bNeedFree` 堆块大小随类型而异：`FString` 小、`[UStruct]` 可很大）；④ **未做稳态/长会话曲线**（本装置在 PIE 结束立即卸载，与 §5.1.2 同一边界）；⑤ 未穷举工程侧/第三方对"地址映射长期有效"的依赖。
**探针残留自检**：探针删除后 `FG1FamilyProbe`/`G1FAM` 全库 0 命中；两个被探针改动的注册表 `.cpp`（`FStringRegistry.cpp`/`FStructRegistry.cpp`/`FMultiRegistry.cpp`）`git diff` **与 HEAD 完全一致**，其余文件只剩本轮已知修复（`F-ENV-002`/`F-DEL-008`/`F-REG-007`）。
**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Succeeded**；两阶段域重建 **2 × 1704 / 0**、`ensure`/`assert` **0**、`LogBlueprint: Error` **0**（装表运行与去表后的收尾运行各一次，结果一致）。

---

### 5.3 📋 第九刀：binding 所有权判据**编译期化** + `bNeedFree` 全量核对（2026-09-22，已提交 `8c54c598`）

> **起因**：第三刀把"是否释放 pointee"从**注册表侧的 `FBindingAddress::bNeedFree`** 下沉进 wrapper ⇒ 于是 `FBindingRegistry.cpp` 的两处清理**不再用这个标志判断**，而字符串/多引用/结构体三族的注册表**还在用**同一个名字的标志。这一刀回答："其余那些 `bNeedFree` 能不能也删掉？"

**全量核对（静态：逐条列声明/写入/读取点；`Script/` 全树 `NeedFree` **0** 命中）**

| 变量 | 声明处 | 写入 | 读取 | 判定 |
|---|---|---|---|---|
| `FBindingAddress::bNeedFree` | 第三刀前在 `FBindingRegistry.h` | — | 曾在 `FBindingRegistry.cpp` 的 `RemoveReference`/`Deinitialize` 当门槛 | ✅ **第三刀已删**（那两处现为无条件 `delete wrapper`） |
| `FBindingAddressWrapper::bNeedFree` | 第三刀新增 | — | 原为 `TBindingAddressWrapper<T>::~` 的唯一判据 | ✅ **本刀删除**（改为编译期模板参数，见下） |
| `FStringRegistry::TStringAddress<T>::bNeedFree` | `FStringRegistry.h:19` | ctor ← `FStringRegistry.inl:50` 的 `IsNeedFree` | `FStringRegistry.inl:67` + `FStringRegistry.cpp:26/44/63/83/102` | ❌ 删不掉（`false`/`true` 在成员路径、`FReturn`、`TPropertyValue` 拷贝、`FRegister*` 推送四条路径上都真实出现） |
| `FMultiRegistry::TMultiAddress<T,IsType>::bNeedFree` | `FMultiRegistry.h:29` | ctor ← `FMultiRegistry.inl:50` | `FMultiRegistry.inl:67` + `FMultiRegistry.cpp:26/44/62/80/98/116` | ❌ 同左 |
| `FStructRegistry::FStructAddress::bNeedFree` | `FStructRegistry.h:27` | ctor ← `.inl:25`(`IsNeedFree`) / `FStructRegistry.cpp:101`(`false`) | `FStructRegistry.cpp:29/126` | ❌ 同左 |
| `FArrayHelper`/`FMapHelper`/`FSetHelper::bNeedFreeData`/`bNeedFreeProperty`、`FOptionalHelper` 同名 | 各自 `.h` | ctor | 各自析构（`FArrayHelper.cpp:36/50`、`FMapHelper.cpp:47/61`、`FSetHelper.cpp:43/57`、`FOptionalHelper.cpp:38/45`） | ❌ 活。**这正是"判据放在被拥有对象体内"的既有范式** —— 与 binding 改完后的形态一致 |
| `DelegateHandler::bNeedFree` / `MulticastDelegateHandler::bNeedFree` | `.h:152` / `.h:113` | ctor 自算（`InScriptDelegate == nullptr`） | 析构 `:36` / `:44` | ❌ 活 |

⇒ **没有任何一个是死变量**，能删的只有 binding 那一处 —— 本刀已完成。

**本刀改法（行为等价，只是把判据从运行期搬到编译期）**
- `FBindingAddressWrapper`：删掉 `bNeedFree` 成员与 ctor 第二参数，只剩 `void* Value`（虚析构保留 ⇒ `delete` 仍可经基类指针）；
- `template <typename T, auto IsNeedFree> struct TBindingAddressWrapper`：ctor 收 `const T*`，析构 `if constexpr (IsNeedFree) { delete static_cast<const T*>(Value); }`；
- 两个注册点：`new TBindingAddressWrapper<T, IsNeedFree>(InObject)` / `new TBindingAddressWrapper<T, false>(InObject)`。
- ⚠️ **实测踩到并记下来的编译坑**：原代码靠 CTAD（`new TBindingAddressWrapper(InObject, …)`）让 `T` 对**常指针**实参推导成 `const X`；一旦所有权变成必须显式给出的非类型模板参数、写成 `TBindingAddressWrapper<T, IsNeedFree>`，ctor 若仍收 `T*` 就**接不住 `const T*` 实参**（实测 3 处 `C2665`：`<T,false>` / `<FRawTestStruct,false>` / `<FEnhancedInputActionEventBinding,false>`）⇒ ctor 改收 `const T*`、`delete` 也按 `const T*` 转换（与原先 CTAD 推出 `const X` 的行为一致）。
- **收益**：wrapper 少 1 字节、判据不再有运行期分支；`FBindingRegistry.h` 的改动面由 23 行降到 14 行。
- **验证**：UBT **Succeeded**；两阶段域重建 **2 × 1704 / 0**、`ensure`/`assert` 0、`LogBlueprint: Error` 0（`Saved/Retest/out/G1-env002-tidy`）。
- ⚠️ **未复测计数**：第三刀那组 wrapper 探针（`created/deleted 33/33`、`nofree=4→0`）**已移除**，本刀只有"编译通过 + 无回归"，**没有**重跑"每周期漏 4 个"的对照实验 —— 改动是编译期常量替换、语义等价，但**按本库口径记作未复测**。

**若还要继续降重复（未做，供取舍）**：重复不在变量而在**清理块** —— `if (bNeedFree) { FMemory::Free(Value); Value = nullptr; }` 这 6 行在 `FStringRegistry.cpp` 抄 **5** 遍、`FMultiRegistry.cpp` **6** 遍、`FStructRegistry.cpp` **2** 遍，另加 3 处 `RemoveReference`。

| 改法 | 做法 | 触及面 | 评价 |
|---|---|---|---|
| (i) 各族自带成员函数 | 3 个 wrapper 各加 `FreeValue()`（字符串/多引用两份同形；结构体那份含 `DestroyStruct`） | 3 个 `.h` + 3 个 `.cpp`/`.inl` | 最小风险；~16 块 → 3 份实现 |
| (ii) 共用 mixin | 新增 `TNeedFreeValueWrapper<T> : TValueWrapper<T>`（标志 + `FreeValue()`），`TStringAddress`/`TMultiAddress` 改继承它 | 新增 1 个 `.inl` + 2 个 `.h` + 3 个 `.cpp`/`.inl` | 字符串/多引用合成 **1 份**；结构体因自持 `Address` + 需 `DestroyStruct` 仍自带一份 ⇒ 共 2 份 |
| (iii) 三族全多态化 | 照 binding 那样把 wrapper 改成多态堆对象，判据随对象走 | 面大 + **每次注册多一次 `new`** | ❌ **不划算**：这两族每周期注册 2500+ 次（§5.2 实测 `FString` 1220 + `FStructRegistry` 1221） |

> **为什么字符串/多引用/结构体不能照搬 binding 的"判据随对象走"**：它们的 wrapper（`TStringAddress`/`TMultiAddress`/`FStructAddress`）是**按值存进 TMap 的普通结构**，pointee 又是裸 `FString*`/`TSubclassOf<UObject>*`/`UScriptStruct*+void*` —— **没有析构钩子可以挂判据**；binding 的 wrapper 本来就是多态堆对象（`FBindingAddressWrapper` 带虚析构），所以判据能跟对象走。顺带更正一个直觉：`TValueWrapper<T>` 其实是字符串/多引用/结构体**共同的基类**（`FStructAddressBase` 也是它的派生），差别只在结构体自持的成员叫 `Address`（`Value` 存的是 `TWeakObjectPtr<UScriptStruct>`）。

---

### 5.4 ✅ 第十刀：`TOwnedValue<Derived>` —— 把"拥有/释放"抽成 CRTP 基类（2026-09-22，已提交 `8c54c598`）

> **对象**：§5.3 里评估过、当时明确"未做"的**清理块去重**。选定形态 = **真 CRTP**：基类持有所有权标志并实现门禁，派生类只实现"怎么放"。

**新增 `Source/UnrealCSharp/Public/Registry/TOwnedValue.inl`（25 行）**

```cpp
template <typename Derived>
struct TOwnedValue
{
	explicit TOwnedValue(const bool InNeedFree) :
		bNeedFree(InNeedFree)
	{
	}

	void Free()
	{
		if (bNeedFree)
		{
			static_cast<Derived*>(this)->FreeImplementation();

			bNeedFree = false;
		}
	}

	bool bNeedFree;
};
```

**三个 wrapper 变成"值 + 拥有能力"两个基类**（多继承）：`TStringAddress<T> : TValueWrapper<T>, TOwnedValue<TStringAddress<T>>`、`TMultiAddress<T,IsType> : TValueWrapper<T>, TOwnedValue<TMultiAddress<T,IsType>>`、`FStructAddress : FStructAddressBase, TOwnedValue<FStructAddress>`；各自的 `bNeedFree` 字段**删除**，改为实现一个 3 行钩子 `FreeImplementation()`，且钩子做成 **`private` + `template <typename> friend struct TOwnedValue;`** ⇒ **唯一公开入口是带门禁的 `Free()`**，避免有人绕过标志直接释放"借来的属性内存"（`FMemory::Free` 到属性地址上）。

**15 个调用点从 6–13 行条件块变成一行**（字符串族 6：`Deinitialize`×5 + `RemoveReference`×1；多引用族 7：6+1；结构体族 2）：`Value.Free();` / `FoundValue->Free();`。卡口逻辑由 16 份降为 **1 份**，释放实现 3 个钩子；结构体的钩子保留 `DestroyStruct`（`STRUCT_IsPlainOldData | STRUCT_NoDestructor` 判定不变）。

**命名过程（如实记录，供后续同类命名复用）**：`TNeedFreeValue`（`NeedFree` 是动词短语做定语，语序别扭）→ `TOwnerValue`（❌ 英语读作"owner 的值"；且本库 `Owner` 已被固定为"**宿主句柄**"——`AddReference(InOwner, …)`/`OwnerManagedHandle`/审计库"3 参无 owner、5 参带 owner"口径，源码 **332 处**命中，再用会造成"属于哪个 owner"的误读）→ 定名 **`TOwnedValue`**（`true`＝这份值归我、`false`＝借用）。**方法名分工（维护者定名）**：对外入口用短名 **`Free()`**（调用点看到的就是它），派生钩子用 **`FreeImplementation()`** —— 与本库 `*Implementation` 惯例一致（`TStringRegistryImplementation`、`FRegisterString::RegisterImplementation`、C# 侧 `*_RegisterImplementation`）。⚠️ 第一版曾把名字反过来（对外 `FreeValue()`、钩子 `Free()`），维护者指出后已改名并**重新构建 + 复验**（见下）。

**形态先例**：自引用模板基类在本库已有 —— `FClassCollector : public TSharedFromThis<FClassCollector>`（`NewClass/ClassCollector.h:28`）、`FUnrealCSharpEditorCommands : public TCommands<FUnrealCSharpEditorCommands>`（`UnrealCSharpEditorCommands.h:9`）；引擎侧同类写法 `TLinkedList : public TLinkedListBase<TLinkedList<…>, …>`。**但多继承在插件自有类里此前 0 命中**（本刀是第一次），如实登记。⚠️ 更正 §5.3 里的一句话：那里说"CRTP + 多继承 2 个范式均无先例"——**CRTP/自引用模板参数其实有先例，只有多继承没有**。

**改动面**：8 文件 **+66/−116（净 −50 行）** + 新增 1 文件（`TOwnedValue.inl`，**已随提交 `8c54c598` 入库**；行尾已与全树对齐为 CRLF）。
**验证**：① **CoreCLR**（改名前的第一版方法名）UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（`Saved/Retest/out/G1-ownedvalue`，CSV `UnrealCSharp-CoreCLR-…`）；② **LeanCLR**（改名后的最终形态）UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（`Saved/Retest/out/G1-ownedvalue-rename`，CSV `UnrealCSharp-LeanCLR-…`）—— ⚠️ 两次回归**后端不同**：改名前那一轮在 CoreCLR，改名后这一轮跑在维护者本地已切换的 **LeanCLR** 配置上（`Config/DefaultUnrealCSharpSetting.ini` 现为 `WindowsScriptDomainType=LeanCLR`，**未改回**，因为那是维护者本地的选择）；两轮 `ensure`/`assert` **0**、`LogBlueprint: Error` **0**。⇒ "同一后端上跑改名前后两版"这一组合**未取得**（改名是纯标识符替换，风险极低，但如实记录）。
⚠️ **未复测计数**（计数探针已在第八刀后移除）：语义等价性由"门禁逻辑 1:1 搬迁 + 15 个调用点全部改走门禁入口"支撑，但**按本库口径记作未复测**。
**明确未做**：容器/委托/Optional 的 `bNeedFreeData`/`bNeedFreeProperty`/`Handler::bNeedFree` 未动（它们本来就在被拥有对象的析构里，见 §5.3 表）；绑定族的 `delete` 语义**未纳入**该基类（基类只做 `FMemory::Free`，与这三族一致；绑定族的所有权判据已在第九刀编译期化）。

---

## 6. 未覆盖 / 存疑（如实声明）

1. ~~**只实测了 `TArray` 一族**（§3）~~ ⇒ ✅ **已在 §5.2 补齐（2026-09-22 第八刀）**：字符串族 5 子族 / 多引用族 6 子族 / binding / struct / optional **全部单独计数**（`regs / member / owned / rel / 残留`），结论 = 闭环 5 族全中、残留 **3184 条/周期**（其中 **3005 个自持堆块**）。仍**未分别测**的是：`FObjectRegistry`（对象注册表，其生命周期由 `FUObjectListener` 驱动，见 `08-…/02b`）与 `FClassRegistry`/`FReferenceRegistry`/`FDynamicRegistry`；"364 次注销"多于"≥276 次注册"的**差**在 §5.2 的分族口径下仍未解释（`TArray` 族的 `remove` 里含 5 参/3 参两条路径，本节的 `rel` 只统计 `RemoveReference` 成功），**不作为结论使用**。
2. **量的部分全部未做**：`Handles.Count` 的净增长、原生内存/托管堆的量化、长会话趋势 —— 需要"临时加 `internal` 计数属性 + 循环构造"的专项用例（各报告已给出用例写法）。
3. **未跑 Mono / LeanCLR** 后端（A4 的收益正在这两个后端上）。
4. **未做 ASan/TSan**，也没有做"把 `Alloc` 临时改 `Weak` 再跑同一用例"的反向验证（报告原建议的反证法）——本轮已决定不做弱句柄改动，故该反证只在需要最终裁定"闭环是否还有残余"时才有价值。
5. **`09-…/01` 总表与本报告的编号**：本报告不新增编号；`F-REG-007`、`F-DEL-008`、`F-RM-004`、`F-DOM-008` 四行需按 §4 的处置结果回写（见 `09-…/00` §2.4）。
