# 推荐优先修复清单

> **本文件的唯一底表**：[`01-全局问题总表.md`](01-全局问题总表.md) —— 该表由 **逐条汇总 47 份报告正文**生成（生成时间 2026-09-11 01:04）。
> **本文件是派生视图**：编号、级别、复核结论、可达性、模块、一句话问题**全部取自 `01-全局问题总表.md`**（不重新判定）；分组的"根因归纳"与"修复点/影响面"取自各模块报告的 `复核证据` / `修复建议` 字段，并逐条标注出处报告。
> **厚版（评分公式、门控规则、逐条根因链、P0 处置表）**：[`../00-总览/03-修复优先级评估.md`](../00-总览/03-修复优先级评估.md)。两份文档**口径一致**，本文件只做"一行一条"的排期视图。
> **本文件不产生新发现**，也不回源码复验任何一条 Finding 的代码事实。

---

## 0. 级别分布（先读这一节）

数据来源：**逐条汇总 47 份报告正文**（汇总时间戳 **2026-09-11 01:05:23**，47 份报告）。**这是唯一的权威口径。**

| 级别 | 条数 |
|---|---|
| **P0** | **26** |
| P1 | **128** |
| P2 | **267** |
| P3 | **381** |
| **撤销（非缺陷）** | **32** |
| **合计** | **834** |

合计校验：26 + 128 + 267 + 381 + 32 = **834** ✅（与汇总输出的"发现总数 834"闭合）

| 复核结论 | 条数 |
|---|---|
| 确认 | **481** |
| 部分确认 | **319** |
| 证伪 | **26** |
| 无法验证 | **3** |
| 非标准书写（单列） | **5** |

| 可达性 | 条数 |
|---|---|
| 活跃 | **637** |
| 潜伏 | **140** |
| 不可达 | **52** |
| 无字段 | **5** |

> **口径纪律（已因此错了三次，务必遵守）**：
> ① **只用逐条汇总的输出**。任何"统计含 `严重度` 字样的行"之类自写正则都会虚高（实测会得到 1656）；用"整行含 `P0`"判级别曾把 P0 虚高到 **38**、用"整行含 `证伪`"判结论曾把证伪虚高到 **76**。
> ② **旧版数字全部作废**：本文件上一版写的 `P0 26 / P1 127 / P2 233 / P3 331 / 非缺陷 27`（合计指向 **749**）与 `P0 26 / P1 127 / P2 277 / P3 372 / 非缺陷 27`（合计指向 **829**）是**同一份文档内部的两套矛盾口径**，依据不可复现，**两组都已作废**。
> ③ `00-总览/03` 上一版写的"全部 **56** 条 P0"是 **V1 原始初判口径**，同样作废。
> ④ 早期的 `832 / P0 38 / P1 130 / P2 262 / P3 368 / 撤销 34` 是**修正汇总口径之前**的输出（时间戳 00:57:09），已被 01:05:23 的结果取代；**定性结论（O-1…O-20、M-1…M-12）仍然有效**，只是那几个计数需按本表阅读。
> ⑤ **级别/可达性的后续更正不回算本节（2026-09-17 新增）**：`01-…/07` 的重判轮把 `F-HLP-016`/`F-HLP-024` 由 **P1 降为 P2**，并把 `F-HLP-016`/`017`/`024` 由活跃改为**潜伏**。更正**只落在各报告正文与 `09-…/01` 的具体行**，本节的 `P1 128 / P2 267` 仍是 **2026-09-11 汇总口径** ⇒ 引用时须知其中 **2 条现为 P2**。回算需要一次重新汇总，见 [`09-…/01`](01-全局问题总表.md) 统计块下的同款提示。

**重号警告**：`09-…/01` 总表存在 **31 处跨报告重号** —— `F-INT2-*` **16** 处（`01-…/10b` ↔ `01-…/11`）、`F-PROP2-*` **15** 处（`01-…/04b` ↔ `01-…/05`）。**同一编号在两份报告里指向完全不同的发现**（例：`F-PROP2-004` 在 `04b` 是"6 个 `Identical` 不查空指针"= **P0**，在 `05` 是"`FFieldPathPropertyDescriptor` 无实现"= **P3/不可达**）。⇒ **本文件一律写成「编号 + 出处报告」**，裸引用编号一律视为错误。834 条对应 **803 个唯一编号**（= 834 − 31）。

**定级口径的三个必备前提**（读级别前请先接受）：

1. **本工程五平台全 LeanCLR**（构建产物 `Definitions.UnrealCSharpCore.h:19-21` = `WITH_CORECLR 0 / WITH_MONO 0 / WITH_LEANCLR 1`）⇒ Mono/CoreCLR 专属缺陷**当前不参与编译**，一律标 `潜伏`，其"自身后端内的级别"通常比表中级别高一到两档。
2. **分节标题不是级别**：为保住编号锚点**没有重排报告的物理分节**，于是"分节标题写初判级别、条内 `严重度` 字段写复核后级别"的错位普遍存在。**引用级别必须读 `严重度` 字段，不能读分节标题**。
3. **`严重度` 字段与"撤销"判定未完全同步**（M-11）：存在"标题/复核结论/级别变动三处都写撤销、而 `严重度` 字段仍是 P3"的条目（已实证 `08-…/04` 的 `F-ABI-022`）。⇒ 本表"撤销 32 条"**可能偏低**，"P3 381"**可能包含少量实际已撤销的条目**；该差集**未取得**（缺相应扫描器）。

---

## 1. 立刻修（P0，26 条 → 按根因/修复点合并为 11 组）

> **处置更新（修复提交链 `492ce5f7` → `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（**后六次分散提交已合并为这一次**）**：**P0-1、P0-2、P0-3、P0-4、P0-5、P0-6、P0-8、P0-9、P0-10、P0-11 十组（共 26 条编号）已修复**；**P0-7 为部分修复**（① `F-BR-001` 已修；② `F-DOM-003` 经核查**未修复**，且经确认**决定不做**：不符合项目规范）；**26 条 P0 编号至此全部有处置登记**（末条 `F-RM-001` 由 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` 登记；`F-DOM-003` 属"决定不做"，不是"已修"）。**逐提交映射**：`P0-1`/`P0-2`/`P0-3`/`P0-11` 与 `P0-10` ① → `492ce5f7046cfd07032adda275f94ae6b5be6505`（2026-09-15 16:53:12 +0800，54 个文件，父 `85348c68`）；**`P0-4`/`P0-5`/`P0-6`/`P0-8`/`P0-9` 与 `P0-10` ② 六组 → `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` "Fixed some bugs"（2026-09-16 12:54:34 +0800，**19 个文件 +222/-152**，父 `492ce5f7`）—— 六次分散提交已合并为这一次**；各组在本提交内的贡献：`P0-4` 8 文件 +82/-41（原生 3 + C# 5）、`P0-5` 2 文件 +3/-3、`P0-8` 3 文件 +37/-10、`P0-6` 1 文件 +2/-1、`P0-10`② 1 文件 +7/-2、`P0-9` 4 文件 +91/-95（纯原生）；各 Finding 的行号引用仍指**修复前快照**。**逐条登记（哪条 finding 由哪个提交修复）见 [§1.2 处置记录](#12-处置记录按修复提交登记)。**
>
> ⚠️ **四处"修复方向未照抄"必须并读**（详见各组"修复状态"行）：**P0-4** 的 C# 侧异常语义（`Script/UE/**` 全层无抛异常先例 ⇒ 越界 / 缺键 / 未注册句柄静默回 `default`/`null`）；**P0-8** 的"句柄存入成员容器"（改用 `RemoveAll(this)` 与 `CreateRaw` + `IsBoundToObject`，因为 `TArray<TDelegate>::Add` 没有句柄、且 lambda 型委托无法按对象匹配）；**P0-9** 的"去重键含参数类型 + 键冲突 `ensureAlwaysMsgf`"（查表端只有"名字 + 元数"，键里加类型会让全部调用点查不到；且"同键不同签名"现在是**合法状态**，`ensure` 不做）；**P0-10** ② 的"visited 集合 + `ensureAlwaysMsgf`"（visited 判据不成立、日志不做、`FNode` 成员换成局部 map 以避免跨轮累积）。
> ⚠️ **P0-6 的边界**：只覆盖**同线程重入**；跨线程 `Broadcast` ↔ `Add`/`Remove` 的竞争属 P1 族 `F-CONC-*`，不在本次修复内。
> 编号全部取自 `09-…/01` 总表的 P0 行（**26 行**，与逐条汇总的 `P0 = 26` 一致）。
> **分组口径**：一个补丁能一并覆盖的归为一组；**组的条数之和 = 26**。凡"同一处代码被两份报告各登记一次"的，在组内标 **⚠️ 同族需合并计数**（排期工作量按下限算）。

### P0-1 「句柄→对象」解析返回值未判空即解引用：字符串/结构体/Optional 描述符（2 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-PROP2-004`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)）、`F-PROP2-005`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)） |
| **级别 / 可达性** | 均 P0 / 均**活跃**（复核结论均"确认"） |
| **一句话根因** | 托管边界传入失效/已 `Dispose`/类型不符的句柄时，`GetString<>` / `GetStruct<>` / `GetOptional` **明确返回 `nullptr`**，描述符随即无条件解引用。 |
| **修复点（真实 `文件:行`）** | `FStrPropertyDescriptor.cpp:45-48`（`:48` 为解引用行）、`FNamePropertyDescriptor.cpp:45-48`、`FTextPropertyDescriptor.cpp:45-48`、`FAnsiStrPropertyDescriptor.cpp:46-49`、`FUtf8StrPropertyDescriptor.cpp:46-49`、`FStructPropertyDescriptor.cpp:49-52`（`:52` 把 `nullptr` 传进原生比较）；（`04b` 组）；`FOptionalPropertyDescriptor.cpp:38`（未判空）→ `:42`（解引用）（`04b` 组）。**同文件对照守卫**：`FStrPropertyDescriptor.cpp:33`、`FStructPropertyDescriptor.cpp:37`、`FOptionalPropertyDescriptor.cpp:9` |
| **影响面** | **11 个容器元素比较调用点**：`FArrayHelper.cpp:148,164,180,293`、`FSetHelper.cpp:87,124,154`、`FMapHelper.cpp:101,137,164,183`；`GetString` 侧 `FStringRegistry.inl:25-27` 显式 `: nullptr`，`GetOptional` 侧 `FOptionalRegistry.cpp:41-46` 未命中返回 `nullptr` |
| **修复方向** | 统一加守卫，且把失败**视为"不相等"/`InvalidManagedHandle` 而不是崩溃**（与 `FRegisterString::IdenticalImplementation:31` 的 `return 0` 口径一致）。可抽公共模板基类，检查只写一次 |
| **修复状态** | ✅ **已修复（`492ce5f7`）** —— 5 个字符串描述符与 `FStructPropertyDescriptor` 的 `Identical` 已改为先解析托管句柄、未命中即 `return false`；`FStructPropertyDescriptor` 同时不再经 `ContainerPtrToValuePtr(A)`；`FOptionalPropertyDescriptor::Set` 已对 `GetOptional` 判空。总表 `F-PROP2-004`/`F-PROP2-005` 两行已标 **已处置（`492ce5f7` 已修复）**。原文"可抽公共模板基类"**未采纳**（改为逐个描述符各自判空） （修复提交 `492ce5f7`） |

### P0-2 对象/委托描述符：`Class->NewObject()` 与 `GetMulti`/`GetDelegate` 返回值未判空（4 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-PROP2-001`（[01-…/05](../01-UnrealCSharp运行时/05-属性描述符-对象与委托类型.md)）、`F-PROP2-002`（[01-…/05](../01-UnrealCSharp运行时/05-属性描述符-对象与委托类型.md)）、`F-DEL-003`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)）、`F-LIFE-003`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)） |
| **级别 / 可达性** | 均 P0 / 均**活跃**（均"确认"） |
| **一句话根因** | ① `Class`（该属性类型在 C# 侧的反射类型）未命中时为 `nullptr`，代码仍无条件 `Class->NewObject()`；② `GetMulti<T>` 未命中（或按 T 分桶不匹配）返回 `nullptr`，4 个描述符的 `Set` 仍直接 `SrcMulti->Get()`；③ 委托属性 `Set` 对 `GetDelegate<>()` 的 `nullptr` 直接 `->GetUObject()`。**全部由 C# 侧输入完全控制，原生侧零校验**。 |
| **修复点（真实 `文件:行`）** | **① `Class->NewObject()` 共 18 处 / 6 类**：`FInterfacePropertyDescriptor.cpp:10`（+`:21`、`:31`）、`FWeakObjectPropertyDescriptor.cpp:10`、`FLazyObjectPropertyDescriptor.cpp:10`、`FSoftObjectPropertyDescriptor.cpp:10`、`FSoftClassPropertyDescriptor.cpp:10`、`FSubclassOfPropertyDescriptor.cpp:10`；`Class` 来源 `TPropertyDescriptor.inl:12`（构造函数**不校验**）；四处 `return nullptr`：`FReflectionRegistry.cpp:923`、`:974`、`FTypeBridge.cpp:178`、`:421`（`#if WITH_EDITOR` 占位接口类）。<br>**② 4 处解引用**：`FWeakObjectPropertyDescriptor.cpp:43`+`:47`、`FLazyObjectPropertyDescriptor.cpp:43`+`:47`、`FSoftClassPropertyDescriptor.cpp:43`+`:47`、`FInterfacePropertyDescriptor.cpp:43`+`:49`；**对照**：`FSoftObjectPropertyDescriptor.cpp:47` / `FSubclassOfPropertyDescriptor` **有**守卫。<br>**③ 委托**：`FDelegatePropertyDescriptor.cpp:24`（未判空）→ **`:30`**（`SrcDelegateHelper->GetUObject()`）；多播同构 `FMulticastDelegatePropertyDescriptor.cpp:22-34`（该条 `函数` 字段记 `:33`）；`FDelegateRegistry.inl:19-25` 明确可返回 `nullptr`（对照 `FRegisterDelegate.cpp:35-48` 有 4 层判空） |
| **影响面** | `FPropertyDescriptor` 全部对象/接口/委托族；`grep 'Class->NewObject\(\)'` 全插件共 **43 处 / 18 个描述符**（含字符串、结构体、容器、Optional）⇒ 该模式是**全插件系统性**的，汇总时按"族"处理；委托侧影响单播+多播两条属性读写路径 |
| **同族** | ⚠️ **`F-DEL-003` 与 `F-LIFE-003` 是同一处代码的两次登记 → 需合并计数**（2 条 = 1 个修复动作）。`F-PROP2-001` 的 `F-DYN-008` 后代（`PropertyClass` 永久 `nullptr`）在 P1 族跟踪 |
| **修复方向** | `Class == nullptr` 时写 `InvalidManagedHandle` 并 `return`（或 `ensureMsgf` 把问题前移到描述符创建时刻，此时能打印属性名）；`if (SrcMulti != nullptr)` 作为 `Set` 的统一前置条件；委托 `Set` 判空后 `return`。**根因是"同一组描述符里实现不一致"** —— 修法是抽公共模板（对应 P1 族的 `F-PROP2-010`） |
| **修复状态** | ✅ **已修复（`492ce5f7`）** —— 全插件 43 处 `Class->NewObject()`（18 个描述符类）已统一置于 `if (Class != nullptr)` 之内、未命中写 `InvalidManagedHandle`，且 `NewRef`/`NewWeakRef` 的守卫提到 helper 分配之前；`FWeakObject`/`FLazyObject`/`FSoftClass`/`FInterface` 的 `Set` 已补 `SrcMulti != nullptr`；单播与多播委托 `Set` 已补 `GetDelegate<>` 判空。总表 `F-PROP2-001`/`F-PROP2-002`/`F-DEL-003`/`F-LIFE-003` 四行已标 **已处置**。**未抽公共模板**（P1 族 `F-PROP2-010` 仍开放） （修复提交 `492ce5f7`） |

### P0-3 Interop 导出层：`GetMulti`/`GetString`/`TMap::Find` 返回值未判空（4 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-INT1-001`（[01-…/10](../01-UnrealCSharp运行时/10-Interop注册-对象与反射类.md)）、`F-INT1-002`（[01-…/10](../01-UnrealCSharp运行时/10-Interop注册-对象与反射类.md)）、`F-INT2-001`（[01-…/11](../01-UnrealCSharp运行时/11-Interop注册-数学值类型与引擎类.md)，**注意与 `10b` 的同名条目不是一条**）、`F-HLP-023`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)） |
| **级别 / 可达性** | 均 P0 / 均**活跃**（均"确认"） |
| **一句话根因** | 原生导出层的"未命中即返回 `nullptr`"约定，与 C# 侧的常态调用（`map[不存在的键]`、`TScriptInterface.GetObject(h)`、`UClass.RemoveFunction("X")`、行名不存在的 `GetDataTableRowFromName`）**直接相撞**。 |
| **修复点（真实 `文件:行`）** | `FRegisterScriptInterface.cpp:62-68`（`:67` 直接 `Multi->GetObject()`；`FMultiRegistry.inl:24-26`、`FCSharpEnvironment.inl:224-226` 明确 `nullptr`；**同文件 `IdenticalImplementation` `:36-40` 对两次 `GetMulti` 都判了空**）<br>`FRegisterClass.cpp:17-19`（`:19` 直接 `*Name`；`FStringRegistry.inl:25-27` 明确 `nullptr`；全组 6 文件仅此 1 处用 `GetString<`）<br>`FRegisterDataTableFunctionLibrary.cpp:31` = `*DataTable->GetRowMap().Find(*InRowName)`（**引擎对照** `DataTable.h:252-260`：显式判 `RowDataPtr == nullptr` 且对解引用值再 `check`；本实现只判了 `IsNone()`，**判错了条件**）<br>`FMapHelper.cpp:144`、`:171`、`:255`、`:262` + `FRegisterMap.cpp:91`、`:102`、`:124`、`:167`、`:179`（**五处取值实现全部无判空**；`07` 报告 §5 的"共同修复方案"把范围写成 `FRegisterMap.cpp:85-94`（`FindKey`）、`:96-105`（`Find`）、`:118-127`（`Get`）、`:161-171`（`GetEnumeratorKey`）、`:173-183`（`GetEnumeratorValue`），并明确**一次覆盖 `F-HLP-006`/`023`/`027`**，另含 `FRegisterArray.cpp:109-119`、`FRegisterSet.cpp:105-114`、`TPropertyValue.inl:872`）；C# 侧 `Script/UE/CoreUObject/TMap.cs:256-276` |
| **影响面** | `Source/UnrealCSharp/Private/Domain/Interop/**`（58 个 `FRegister*.cpp`）；用户可见 API：`TMap` 索引器/`Find`/`FindKey`、`TScriptInterface.GetObject`、`UClass.RemoveFunction`、`UDataTableFunctionLibrary.GetDataTableRowFromName<T>`。`GetDataTableRowFromName` 的触发路径**极常见**（行名过期/热重载后） |
| **修复方向** | 统一 `Checked<T>(p, What)` 辅助 + 机械替换；`FName::IsNone()` 必须换成"`Find` 结果判空"；或直接改调引擎的 `UDataTableFunctionLibrary::GetDataTableRowFromName`（`DataTableFunctionLibrary.cpp:68-89`）避免自己重写行查找 |
| **修复状态** | ✅ **已修复（`492ce5f7`）** —— `FRegisterScriptInterface`（`TScriptInterface.GetObject`）、`FRegisterClass`（`UClass.RemoveFunction`）、`FRegisterDataTableFunctionLibrary`（判据改为 `FindRowData != nullptr && *FindRowData != nullptr && Class != nullptr`）三处导出已补判空；`FRegisterMap`/`FRegisterArray`/`FRegisterSet` 共 7 处（`FRegisterMap` 5 处 + `FRegisterArray` 1 处 + `FRegisterSet` 1 处）改为先判 helper 命中、未命中回退新增的 `FPropertyDescriptor::GetDefaultValue`（按 `GetBufferSize()` 清零 C# 返回缓冲）。总表 `F-INT1-001`/`F-INT1-002`/`F-INT2-001`(11)/`F-HLP-023` 四行已标 **已处置**。原文建议的 `Checked<T>(p, What)` 统一辅助**未引入**；`FMapHelper` 返回 `nullptr` 的契约本身不变 （修复提交 `492ce5f7`） |

### P0-4 容器索引/句柄契约：C# 侧与导出层同时缺校验（5 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-INT2-001`（[01-…/10b](../01-UnrealCSharp运行时/10b-Interop注册-容器字符串与对象指针.md)，**与 `11` 的同名条目不是一条**）、`F-CS2A-001`、`F-CS2A-002`、`F-CS2A-017`、`F-CS2B-001`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md) ×3、[06-…/02b](../06-CSharp运行时与脚本/02b-对象字符串与工具类型包装.md)） |
| **级别 / 可达性** | 均 P0 / 均**活跃**（`F-CS2A-002` 为"部分确认"：原文并列的"setter 同样崩溃"**已证伪**，其余成立） |
| **一句话根因** | 容器索引型接口**不做下标校验**、缺键返回 `nullptr` 后上游不判空、未注册句柄（默认构造/`= null` 产生的 **0 号句柄**）被直接当有效句柄解引用。 |
| **修复点（真实 `文件:行`）** | **① index 零校验（Shipping 下堆破坏）**：`FRegisterArray.cpp:175-183 / 195-203 / 223-231 / 288-306`（五个导出）；下游 `FArrayHelper.cpp:208-228`（`:212` 未校验）、`:196-199`、`:251-261`、`:324-332`；C# 公开层 `TArray.cs:190-198`、`:206-207`、`:306-312` 全部直接透传（**同文件 `:63-64` 已有 `IsValidIndex` 却不用**）。<br>**引擎依据**：`FScriptArrayHelper::GetRawPtr` 唯一保护是 `checkSlow(IsValidIndex(Index))`（`UnrealType.h:3911-3920`，断言在 `:3918`）；`FScriptArray::Remove` 的 4 条校验全为 `checkSlow`（`ScriptArray.h:195-198`）；`InsertZeroed`→`Insert` 用 `check`（`:57-61`）；`SwapMemory` **连 `checkSlow` 都没有**（`:162-169`）；而 `AssertionMacros.h:237/245/325/327`（`DO_CHECK`）与 `:340-348`（`DO_GUARD_SLOW`）证明 **Test/Shipping 下 `check` 与 `checkSlow` 均展开为 `CA_ASSUME(expr)`** ⇒ `RemoveAt(0, 2)`（1 个元素）算出 `NumToMove = -1` → `(SIZE_T)(-1) * ElementSize` 的巨型 `Memmove` → 立即堆破坏。<br>**② 越界/缺键**：`FRegisterArray.cpp:115 之后`（`TArray.cs:82` → `:109-119` → `FArrayHelper.cpp:123-131`）；`FRegisterMap.cpp:85-127`（验证点 `:124`）+ `FMapHelper.cpp:171`。<br>**③ 未注册句柄**：`FRegisterSubclassOf.cpp:46-52`、`FRegisterLazyObjectPtr.cpp:47`、`FRegisterSoftObjectPtr.cpp:47`、`FRegisterSoftClassPtr.cpp:47`、`FRegisterScriptInterface.cpp`；`Script/UE/CoreUObject/FString.cs:8-12`（空构造）、`:43`、`Script/UE/Library/FStringImplementation.cs:39-41`、`FRegisterString.cpp:42-47` |
| **影响面** | 全部容器与字符串/指针包装 API（**用户写的每一行 C# 索引访问都可能撞上**）；`F-INT2-001`(10b) 在 **Shipping 下不是崩溃而是写坏堆**（后续随机损坏，最难排查） |
| **同族** | ⚠️ `F-CS2A-001` / `F-CS2A-002` / `F-CS2A-017` 在报告内被互为同族声明（`06-…/02a` `:1476`、`:1618`），与 `F-INT2-001`(10b) **同一病根的两端**（C# 入口 + 原生导出）→ 建议**同批改** |
| **修复方向** | C# 侧索引器与 `Find*` 改抛 `IndexOutOfRangeException` / `KeyNotFoundException`；原生索引型接口补 `IsValidIndex`；未注册句柄在 `HandleData.GetHandle` 处按需注册（或抛 `InvalidOperationException`）。**不要只靠 `checkSlow`** —— 它在 Shipping 下无行为 |
| **修复状态** | ✅ **已修复（`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）** —— **已完成**（父提交 `492ce5f7`）。**② `F-CS2A-001` / ③ `F-CS2A-002` / ④ `F-CS2A-017` / ⑤ `F-CS2B-001` 的失效机制由 `492ce5f7` 闭合**：`FRegisterArray` / `FRegisterMap` / `FRegisterSet` 的取值实现未命中一律回退 `FPropertyDescriptor::GetDefaultValue`（按 `GetBufferSize()` 清零 C# 返回缓冲），六个指针注册器的 `Multi->Get()` / `GetObject()` / `LoadSynchronous()` 与 4 个字符串注册器的 `ToStringImplementation` 全部判空回 `InvalidManagedHandle` ⇒ **空指针解引用不再成立**。**① `F-INT2-001`(10b) 的 Shipping 堆破坏由 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` 修复**：`FArrayHelper` 的 `RemoveAt`/`InsertZeroed`/`InsertDefaulted`/`SetNum`/`Reset`/`Empty`/`Swap`/`SwapMemory`/`AddUninitialized`/`AddZeroed` 与 `FMapHelper::Empty`/`FSetHelper::Empty` 改用**正向 `if (条件) { 函数体 }` 包裹**（与同文件 `Get`/`Set` 同形；`Swap`/`SwapMemory` 用 `if (auto ScriptArrayHelper = CreateHelperFormInnerProperty(); …IsValidIndex(a) && …IsValidIndex(b))`），越界 / 负值不再进 `Memmove`/`Memswap`/`DestructItems`，Shipping 下不再依赖被编译掉的 `checkSlow`。C# 侧另加**非抛出**兜底：5 个字符串包装的 `ToString()` 尾附 `?? string.Empty`（`ToString()` 不再返回 `null`）。<br>**提交内容核对**：本组改动恰好 8 个文件（原生 3 + C# 5，本组 +82/-41），`git show --name-only 2113e1e9 -- <路径>` 可复核；`FArrayHelper::RemoveAt` 中我原写的 `const auto Total = Num();` 局部量在提交时被内联为 `Num() - InCount`（语义等价，短路后 `Num()` 仍只调一次）。<br>⚠️ **契约面（修复方向的后半句）经确认决定不做**：C# 侧「改抛 `IndexOutOfRangeException` / `KeyNotFoundException`」、`HandleData.GetHandle` 处「按需注册」、六个包装取「抛 `InvalidOperationException`」、补 `IsValid()` **均不做**（`Script/UE/**` 与 `Script/Interop/**` 全层无抛异常先例）⇒ 越界 / 缺键 / 未注册句柄的最终语义是**静默回 `default` / `null`**，与「合法默认值」在 C# 侧不可区分；`TMap.Find` / `FindKey` 无任何表达「未命中」的途径。**零新增公开 API**（`TryGetValue` / `TryFindKey` / `ContainsValue` / `IsValid` / `RequireHandle` 均未引入）；不加注释 / 日志 / `ensure`。<br>**验证（在该提交上重跑）**：`dotnet build Script/Interop/Interop.csproj` 与 `Script/UE/UE.csproj` 均 **0 warning / 0 error**；UBT 增量构建 `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**（`UnrealEditor-UnrealCSharp.dll` 重新链接）；diff 机械核对：新增注释行 **0**、新增 `throw` **0**、`if (…) return;` 形式守卫 **0**。**仍未做**：§6 的运行期断言 —— ⚠️ 其中「各抛**具体 .NET 异常**」这一前提**按契约面不再成立**（应收敛为「进程存活 + 不写坏堆 + `HandleData` 字典条目回归基线」），需起编辑器实机跑 |

### P0-5 出缓冲步长错配：把「绝对前缀偏移」当成写出步长（1 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-BIND-001`（[01-…/08](../01-UnrealCSharp运行时/08-绑定层公开头文件（模板元编程）.md)） |
| **级别 / 可达性** | P0 / **活跃**（"确认"） |
| **一句话根因** | `TOut` 把"该参数在**全部参数**中的前缀偏移"当作写出步长，且**推进发生在写入之后** ⇒ 实际写入位置退化为"前缀和的累加"而非"前缀和本身"；C# 侧只按 **out 参数**紧凑打包读回 ⇒ 第 2 个及之后的 out 参数**逐级错位 + 至少一个槽位从未被写**。 |
| **修复点（真实 `文件:行`）** | `Source/UnrealCSharp/Public/Binding/Function/TOut.inl:36` = `Buffer += std::get<Index>(TBufferOffset<Args0...>()());`；偏移表在 `TBufferOffset.inl:41`（记录当前累计偏移）、`:43`（对**每个**参数都累加，**无 `IsRef` 门控**） |
| **影响面** | 全部多 out 参数的绑定 API。**已逐行推演精确复现**：三参数场景实际写 `0/0/4` 而 C# 读 `0/4/8`；`GetIdentity`（2×`FString&`）两参数同写 `+0` 同理。两条插件自带绑定（`FDateTime::GetDate`、`GetIdentity`）即可命中 |
| **修复方向** | 二者改其一：① `TBufferOffset` 额外提供增量尺寸（`Value[i]-Value[i-1]`），改为**先按增量推进再写**；或 ② 保留绝对偏移但改为**定位写**（记住 `Base`，写 `Base + Value[Index]`，不累加 `Buffer`）。建议同时加边界断言 |
| **⚠️ 非同族，必须分别修** | 同批的 `F-GEN2-001`（P1，[03-…/02](../03-代码生成器/02-委托结构体枚举与GameplayTag生成器.md)）字面上是"同一语义在 ABI 两端各有一次独立实现错误"，但**根因不同**：`F-GEN2-001` 是生成器**读端**字符串**缺括号**（`FGeneratorCore.cpp:625-633`，影响 **235 处**产物，含 `GameplayStatics`/`Actor` 等主路径）。**两者不是同一族** —— 只改一端会得到"两端不一致"的新错误，但**必须作为两个独立补丁**排期。 |
| **修复状态** | ✅ **已修复（`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）** —— **两端已作为同一个提交落地**（与 P0-4 同属 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` 这一次提交，本组恰好 2 文件 +3/-3）。**① 写端 `TOut.inl`**：`Buffer += std::get<Index>(TBufferOffset<Args0...>()());` → `Buffer += TTypeInfo<std::decay_t<T>>::Get()->GetBufferSize();`，即按**刚写入的那个参数自身的尺寸**紧凑推进（primitive 分支实际写 `sizeof(std::decay_t<T>)`、compound 分支写 `void*`，而 `TTypeInfo::GetBufferSize()` 对 primitive 返回 `sizeof(T)`、对 compound 返回 `sizeof(void*)` —— **与写入量逐一相等**，也与 C# 生成端 `FGeneratorCore::GetBufferSize` 同一单位）。**② 读端 `FGeneratorCore.cpp:626`**：`"…GetObject(\*(nint\*)%s%s);"` → `"…GetObject(\*(nint\*)(%s%s));"`。`#include "TBufferOffset.inl"` 相应换为 `"Binding/TypeInfo/TTypeInfo.inl"`。<br>⚠️ **报告给的两个修法选项均不可用（本组唯一的方法学更正）**：`TBufferOffset` 被**另外三处**当 **IN 缓冲**偏移表使用 —— `TFunctionHelper.inl:21,43`、`TConstructorHelper.inl:20`、`TSubscriptHelper.inl:20,32,34`，那里"全参数前缀偏移"语义**是正确的**；按选项 ①/② 改它会把正确的 IN 路径改坏。⇒ **真正的写端修法只有一行，且 `TBufferOffset` 一行未动**。<br>🔴 **新增量化（审计原文只有"已逐行推演"，本轮用产物反向精确分类）**：静态解析全部 16055 个生成文件里带 `OutBuffer` 的 1603 个方法（参数尺寸从产物自己的 `InBuffer` 偏移差自洽推出，不靠类型表）：**修复前 正确 1364 / 仅值错位 93 / 越界写 146**；带 ≥2 个 out 参数的公开 API 共 **288** 个 ⇒ **239 个实际出错**（另 49 个是 `k=2` 且前置参数尺寸恰好相等而侥幸正确）。越界样例（含算术）：`AnimationBlueprintLibrary.GetVectorKeys`（`ref={2,3}`，C# 缓冲 16B，原生写 0,16 → **越界 8B**）、`GetRawTrackData`（缓冲 24B，写 0,16,40 → **24B**）、`KismetAnimationLibrary.K2_TwoBoneIK`（缓冲 16B，写 0,40 → **32B**）。**修复后 1603/1603 满足 `Σ(ref 参数尺寸) == C# 缓冲长度`**（不等 0）⇒ 写出区间恰好贴合缓冲，既不错位也不越界。<br>📌 **对审计表述的两处更正**：**(a)** "至少一个槽位**从未被写**"⇒ 实为**读到 0** —— 全仓 `SkipLocalsInit` 命中 **0**，`stackalloc` 默认零初始化（若将来加 `SkipLocalsInit` 才变未初始化栈内容）；缺陷性质仍是"静默错误数据"，但不是未初始化读。**(b)** `F-GEN2-001` 的格式串调用点**有 3 处**，审计原文只列了 2 处 —— 除 `FClassGenerator.cpp:723`、`FBindingClassGenerator.cpp:597` 外，**`FDelegateGenerator.cpp:263` 同样传偏移**（同一格式串，一处修复即全覆盖）；另 `FGeneratorCore.cpp:597` 的 `GetReturn` **无 offset 参数**、恒为偏移 0，**本来就正确，未改**。<br>**产物**：235 处缺陷行（74 文件）已按**修复后的模板逐字重建**为 `*(nint*)(OutBuffer + N)`（非松散括号搬移），旧形态残留 **0**、无偏移形态 1073 处未动。**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**（重编 `Module.UnrealCSharp` + `Module.ScriptCodeGenerator`、重链两个 DLL）；`dotnet build Script/UE/UE.csproj` **0 warning / 0 error**（校验 235 处补丁可编译）；用户已确认验证无问题。<br>**仍未做**：经编辑器的**完整重新生成**（会顺带把 1073 处无偏移行规范化成 `*(nint*)(OutBuffer)`，语义等价、纯文本差异）与 `Content/Script/UE.dll` 运行期程序集更新；§6 的运行期断言条目文本仍写"各抛具体 .NET 异常"（该前提按 P0-4 契约面已不成立） |

### P0-6 多播委托广播期重入 → use-after-free（1 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-DEL-002`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)） |
| **级别 / 可达性** | P0 / **活跃**（"确认"） |
| **一句话根因** | range-for **遍历中同步回调进 C#**，C# 回调内调 `Remove`/`Clear` 修改正在被遍历的 `TArray`。 |
| **修复点（真实 `文件:行`）** | `MulticastDelegateHandler.cpp:11-13`（遍历中同步 `CallDelegate`）、`:111`（`Remove` 改数组）、`:126`（`RemoveAll`）、`:149`（`Clear` → `Empty()`）；UE 侧绑定点 `:83`/`:100` |
| **⚠️ 依据表述必须用更正后的版本** | 危害来自 **`Clear()` 路径**：`DelegateWrappers.Empty()` → `Array.h:2286-2312` → `ReallocTo(NewMax = 0)` → `FMemory::Realloc(Data, 0)` **真正释放并置空底层分配** → 迭代器 `Ptr` 悬垂 → 下一次 `*Ptr` 即 UAF（真崩溃路径）。`Remove`/`RemoveAll`（`Array.h:3118-3162`）只 `RelocateConstructItems + DestructItems` 并改小 `ArrayNum`，不释放底层分配 → 该回调被重复调用或被跳过。<br>🔴 **不得表述为"引擎有 ensure 检查"**：`Array.h:256-265` 的 `ensureMsgf("Array has changed during ranged-for iteration!")` 受 `TARRAY_RANGED_FOR_CHECKS` 门控（`Array.h:36-40`），**是诊断手段，不是防护** |
| **影响面** | 全部多播委托广播路径；与 P1 族的跨线程变体叠加（UE 多播委托可在工作线程 `Broadcast`，而 GameThread 同时 `Add`/`Remove` → 跨线程数组损坏） |
| **修复方向** | **广播前拷贝调用列表再遍历** —— 引擎自己的 `ProcessMulticastDelegate` 恰恰先复制调用列表，本实现缺这一层 |
| **修复状态** | ✅ **已修复（`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）** —— 同属 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`，本组 **1 个文件 +2/-1**（`MulticastDelegateHandler.cpp`）。<br>**改动**：`ProcessEvent` 的 `FUNCTION_CSHARP_CALLBACK` 分支里先取快照再遍历 —— `const TArray<FDelegateWrapper, TInlineAllocator<4>> DelegateWrappersCopy(DelegateWrappers);` 然后 `for (const auto& [Key, Value] : DelegateWrappersCopy)`。**逐字对照引擎的权威实现**（`Runtime/Core/Public/UObject/ScriptDelegates.h:917` `ProcessMulticastDelegate`：同样的 `TArray<…, TInlineAllocator<4>>` 拷贝、同样的理由注释 *"Create a copy of the invocation list, just in case the list is modified by one of the callbacks during the broadcast"*）；`TInlineAllocator<4>` ⇒ ≤4 个订阅者时不产生堆分配（$FDelegateWrapper = 16$ 字节，内联 64 字节）。**不加注释**（该模块 173 个 `.cpp/.inl` 仅 54 行注释且全为 `// @TODO` 类标签）。<br>**复核（按当前 HEAD，审计行号已漂移 `:11-13`→`:13`、`:111`→`:113`、`:126`→`:128`、`:149`→`:151`）**：① 容器是 `MulticastDelegateHandler.h:121` 的 `TArray<FDelegateWrapper> DelegateWrappers`（元素 `{TWeakObjectPtr<UObject>; FMethodReflection*}`）；② 遍历 `DelegateWrappers` 的**唯一**位置就是 `:13`（grep 全部 19 处引用后确认，其余为 `Empty`/`Add`/`AddUnique`/`Remove`/`RemoveAll`/`Contains`/`IsEmpty`）；③ 回调链成立：`MulticastScriptDelegate` 广播 → `ScriptDelegate`（`:83`/`:100` `BindUFunction(this, CSharpCallBack)`）→ `ProcessEvent` → `FCSharpDelegateDescriptor::CallDelegate`（`FCSharpDelegateDescriptor.cpp:10-26` → `Invoke` → 进托管）⇒ 回调期间确可回到同文件改该数组；④ 单播侧 `DelegateHandler` 用的是**单个** `FDelegateWrapper DelegateWrapper`（非容器）⇒ 无此问题（其缺口属 `F-DEL-004`）。<br>**语义变化（须知）**：拷贝后「广播开始时在场的条目」各被调用**恰好一次** —— 这正是引擎 `ProcessMulticastDelegate` 的既定语义（回调中被 `Remove` 掉的条目仍收到本次广播）；修复前是不确定三选一（跳过 / 重复 / 崩溃）。<br>**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**（重编 `Module.UnrealCSharp`、重链 `UnrealEditor-UnrealCSharp.dll`）；编译同时证明 `TArray<FDelegateWrapper>` → `TArray<FDelegateWrapper, TInlineAllocator<4>>` 的转换构造可用。<br>**⚠️ 明确边界（不算本次修复）**：**(a) 跨线程变体仍是 P1 族** —— 多播可在工作线程 `Broadcast` 而 GameThread 同时 `Add`/`Remove`，**快照本身也会竞争**；本条修的是**同线程重入**（P0-6 本义），跨线程要 `F-CONC-*`/G5 那一族。**(b) 元素载荷**：快照内 `FMethodReflection* Method` 仍是裸指针，回调期间若发生热重载/域重载使其失效，快照中同样悬垂（属 G4 域代次族）。**(c)** `Key.Get()` 可能为 `nullptr`（对象已 GC 而条目未清）时仍会调 `CallDelegate(nullptr, …)`，与本修复前行为一致（属判空族）。**(d) 运行期断言未做**：`C# 回调内调 Clear()` 后广播不崩、其余回调仍各被调用一次 |

### P0-7 工厂以 `nullptr` 表示"不支持"→ 调用方直接解引用（2 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-BR-001`（[02-…/06](../02-UnrealCSharpCore核心/06-类型桥接编码设置与引擎监听.md)）、`F-DOM-003`（[02-…/04](../02-UnrealCSharpCore核心/04-脚本域抽象与三大后端.md)） |
| **级别 / 可达性** | 均 P0；`F-BR-001` **活跃**、`F-DOM-003` **潜伏**（均"部分确认"） |
| **一句话根因** | `FTypeBridge::Factory` / `FScriptDomainFactory::Create()` 用 `nullptr` 表达"该类型/该后端不支持"，而调用点**直接解引用**。（`F-BR-001`：写地址 `0 + offsetof(FField, Next)` → 访问违例） |
| **修复点（真实 `文件:行`）** | `F-DynamicGeneratorCore.cpp:904` 与 `:957` **照抄 `:942` 的写法**（报告建议段逐字）；`FTypeBridge::Factory` 的 `default: return nullptr;` 与 **18 处调用点中 17 处**直接解引用（初判写 6/7 处，**漏掉 `TPropertyValue.inl` 的 10 处**）。<br>`FScriptDomainFactory.cpp:44`（`#if WITH_LEANCLR` 等全不匹配时 `return nullptr;`）+ 调用点 `FDomain.cpp:31`（`ScriptDomain->Initialize();` 直接解引用） |
| **⚠️ `F-DOM-003` 的理由必须换成新版本** | 原理由"LinuxArm64 回退 CoreCLR 而构建 LeanCLR"**已证伪**（该平台构建期 `Build.cs:263-267` 同样回退 `"CoreCLR"`，与运行期一致）。**新理由更硬**：**插件不含任何 `Config` 文件**（glob `**/Config/*` 命中 **0**）⇒ 无 ini 的工程运行期默认 `Mono`（枚举零值 + CDO 内存被引擎清零，`UObjectGlobals.cpp:3845`）、构建期默认 `CoreCLR` ⇒ **必然** `Create() == nullptr` → `FDomain.cpp:31` 空指针崩溃**且无日志**。**这是"结论对、理由错"的典型，汇总时必须用新理由** |
| **影响面** | `F-BR-001`：全部走类型桥接的属性读写（含 `TPropertyValue.inl` 的 10 处漏算调用点）；`F-DOM-003`：**启动即崩**，且用户改一次 ini 后端配置就会命中 |
| **同族需合并计数** | ⚠️ `F-DOM-003` 与 `02-…/07` 的 `F-FL-001`、`01-…/03` 的 `F-REG2-003`、`08-…/03` 的 `F-CONC-012`、`01-…/03` 的 `F-REG2-007`、`08-…/02` 的 `F-LEAK-010` 是**同一行（`FDomain.cpp:20/31`）被 6 个编号描述** → **合并计一次**；另 `F-DOM-015` 声明"与 `F-DOM-003` 同源" |
| **修复方向** | `FDomain::Initialize()` 判空并给出**明确错误提示**（"ini 配置的后端 X 未被编译进来"）；工厂改返回 `TOptional` 或加 `checkf`；所有调用点补判空。中期把三处后端判定（编译期宏 / `UnrealCSharpCore.build.cs:234-307` / 运行期 ini）**收敛为单一真相源** |
| **修复状态** | ⚠️ **部分修复（`492ce5f7`）** —— **① `F-BR-001` 已修**：`FTypeBridge::Factory` 的全部调用点已判空（`TPropertyValue.inl` 10 处、`FTypeBridge.inl`、`FCSharpBind.inl`、`TPropertyBuilder.inl`、`FDynamicGeneratorCore.cpp:904/957`）；**② `F-DOM-003` 未修** —— 核查发现修复提交 `492ce5f7` **未包含** `FDomain.cpp`/`FScriptDomainFactory.cpp`，检出中 `FDomain.cpp:31` 仍是无条件 `ScriptDomain->Initialize();`（无判空、无日志），经确认**决定不做**（不符合项目规范），该条**保持未修复**。总表 `F-BR-001` 已标 **已处置**、`F-DOM-003` 保持原状。"工厂改返回 `TOptional` 或加 `checkf`"与中期项"三处后端判定收敛为单一真相源"**未做** |

### P0-8 编辑器委托/扩展器：注册↔反注册不成对 → 悬空 `this`（3 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-ED2-001`、`F-ED2-017`（[04-…/02](../04-编辑器模块/02-工具栏监听器进度对话框与模块入口.md)）、`F-LIFE-001`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)） |
| **级别 / 可达性** | 均 P0 / 均**活跃**（`F-LIFE-001` 为"部分确认"） |
| **一句话根因** | `AddRaw(this, …)` / 蓝图编辑器扩展器委托的**句柄被丢弃**，析构/`ShutdownModule` 里没有对应的 `Remove` → 监听器/工具栏销毁后回调悬空 `this`。 |
| **修复点（真实 `文件:行`）** | `FEditorListener.cpp:79,344,346,348,350`（5 次注册）、`:101-179`（析构，**无任何 `Remove`**）；建议段 `UnrealCSharpEditor.h:55`（需同步改）。<br>`UnrealCSharpBlueprintToolBar.cpp:25-32`（挂载捕获 `this` 的扩展器委托）、`:42-48`（**唯一的反注册点，且不含该委托**） |
| **影响面** | 编辑器模块生命周期；触发条件是"关闭编辑器""重开蓝图编辑器"—— **日常操作**。`F-LIFE-001` 另指出**确定性永久委托泄漏**（不只是 UAF） |
| **同族需合并计数** | ⚠️ `F-ED2-001` 与 `F-LIFE-001` 是**同一处代码的两次登记**（`F-LIFE-001` 自己写"同名同源报告 `04-…/02` F-ED2-001 同为 P0"）→ **合并计一次**。故本组 3 条 = **2 个修复动作** |
| **触发前提（如实写明）** | `AssetRegistryModule` 无 `ShutdownModule`，**触发前提是"会话中发生过模块卸载"**（Live Coding 就地打补丁不销毁模块；传统 Hot Reload 会命中） |
| **修复方向** | 所有 `Add*` 句柄存入成员容器，`ShutdownModule` 统一 `RemoveAll`；可写一个 clang-tidy 式检查（"`AddRaw` 返回值被丢弃"）一次扫出全部实例 |
| **修复状态** | ✅ **已修复（`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）** —— 同属 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`，本组 **3 个文件 +37/-10**（`FEditorListener.cpp` +13/-0、`UnrealCSharpBlueprintToolBar.cpp` +22/-9、`UnrealCSharpBlueprintToolBar.h` +2/-1）。<br>**① `F-ED2-001` + `F-LIFE-001`（同一处代码，1 个修复动作）**：`FEditorListener` 析构里补 —— **先取模块、再分开解绑**：`if (const auto AssetRegistryModule = FModuleManager::GetModulePtr<FAssetRegistryModule>(TEXT("AssetRegistry")))` 之内对 `OnFilesLoaded` / `OnAssetAdded` / `OnAssetRemoved` / `OnAssetRenamed` / `OnAssetUpdatedOnDisk` 各做一次 **`RemoveAll(this)`**。<br>⚠️ **这里必须写明"为什么不照抄修复方向"**：审计写的"句柄存入成员容器"在本组**不适用** —— 检出的 5 处注册全是 `AddRaw(this, …)`（裸指针绑定），而 `TMulticastDelegateBase::RemoveAll(FDelegateUserObjectConst)`（`MulticastDelegateBase.h:133`）正是按 `DelegateInstance->HasSameObject(...)` 匹配，裸指针绑定的 `HasSameObject` 即 `UserObject == InUserObject`（`DelegateInstancesImpl.h:488-491`）⇒ **无需句柄即可精确解绑**。故**未新增任何 `FDelegateHandle` 成员、5 处注册点一行未改**（本文件原有 12 个句柄成员保持原样）。取模块用 `GetModulePtr`（非 `LoadModuleChecked`）—— 关停期取不到就跳过，且跳过是安全的（委托表随该模块一起消失）。副作用收益：`RemoveAll` **幂等**，若 `OnFilesLoaded` 在一次会话内重入，重复注册会被一并清掉（句柄方案反而会因句柄被覆盖而漏掉第一份）。<br>**② `F-ED2-017`（1 个修复动作）**：⚠️ **审计的修复方向在此同样不适用** —— `TArray<TDelegate>::Add` **没有句柄**（返回 `int32` 下标），而**lambda 型委托的 `HasSameObject` 不按对象匹配**（`DelegateInstancesImpl.h:755-759` 注释 *"Raw Delegates aren't bound to an object so they can never match"*）⇒ lambda 注册进去就永远摘不掉。故修法为：把 `Initialize()` 的 `FAssetEditorExtender::CreateLambda([&](…))` **换成 `CreateRaw(this, &FUnrealCSharpBlueprintToolBar::GenerateBlueprintExtender)`**（裸指针绑定可匹配），并在 `Deinitialize()` 补上缺失的反注册：`GetExtenderDelegates().RemoveAll([](const FAssetEditorExtender& InExtender) { return InExtender.IsBoundToObject(this); })`（用 `IsModuleLoaded("Kismet")` + `GetModuleChecked` 守卫）。`GenerateBlueprintExtender` 的签名随之由 `(UBlueprint*)` 改为委托要求的 `(const TSharedRef<FUICommandList>, const TArray<UObject*>)`（唯一调用点就是那个 lambda，无其它引用），函数体原样保留。<br>**③ 顺手修的真实 UB（同一函数内，1 token）**：`FOnGetContent::CreateLambda([&]()` → `[this, InBlueprint]`。原写法按**引用**捕获了 `GenerateBlueprintExtender` 的形参/局部 `InBlueprint`，而该 lambda 被存进返回的 `Extender`（存活更久）⇒ 菜单打开时读已销毁栈帧。<br>**④ 审计建议的"clang-tidy 式检查"已落成脚本并跑过**（含"按 `RemoveAll(this)` 配对"识别）：全仓 `Add*` 注册 **44 处已保存句柄**（改前 36）、**5 处经 `RemoveAll(this)` 配对**、**1 处未配对** = `UnrealCSharpEditor.cpp:167` —— 已核实**非缺陷**（lambda 捕获列表为空 `[]`，且只在 `IsRunningCookCommandlet()` 分支注册，模块活到进程退出）。⚠️ 脚本**盲区**：只覆盖返回句柄的 `Add*` API，**覆盖不到委托容器**（`TArray<TDelegate>::Add`）—— `F-ED2-017` 那处是靠通读发现的。<br>**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**（重编 `Module.UnrealCSharpEditor`、重链 `UnrealEditor-UnrealCSharpEditor.dll`）；`RemoveAll(this)` 对全部 5 种委托类型编译通过。**残留（未做）**：① 跨越模块卸载**仍开着**的蓝图编辑器，其 `Extender` 内存着捕获 `this` 的内层 lambda → 仍悬垂（彻底覆盖需弱引用守卫，要求类继承 `TSharedFromThis`，属设计变更）；② §6 第 10 条的运行期断言（反复卸载模块 20 次无访问违例 + 句柄计数 == 注册数）未跑 |

### P0-9 反射去重键只用 `(Name, ParamCount)` → 同名同参数个数的重载被静默覆盖（1 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-RM-001`（[02-…/02](../02-UnrealCSharpCore核心/02-反射模型（类字段方法参数属性）.md)） |
| **级别 / 可达性** | P0 / **活跃**（"确认"） |
| **一句话根因** | 去重键不含**参数类型**，第二条同键记录**静默覆盖**第一条 ⇒ C++→C# 反射调用会**调到错误的方法，并用错误类型读取实参**。无编译错误、无日志。 |
| **修复点（真实 `文件:行`）** | `Source/UnrealCSharpCore/Private/Reflection/FClassReflection.cpp:369`（方法）、`:248`（属性）、`:282`（字段）。⚠️ **该 Finding 的"建议"段内没有 `file:line`**（已核，标"未取得"）；上列行号取自该 Finding 的 **`文件` 字段**，可直接定位 |
| **影响面** | `FClassReflection` 的全部方法/属性/字段注册；**静默数据损坏**（比崩溃更坏），影响一切 C++→C# 反射调用 |
| **修复方向** | 去重键改为包含参数类型（或直接用 `UFunction` 的签名哈希）；对"键冲突但签名不同"至少 `ensureAlwaysMsgf` 报出来。⚠️ 注意 `02-…/02` 的 `F-RM-001` 在报告内曾出现"`严重度` 字段写 P1、而标题与 §0 写 P0"的自相矛盾，**已统一为 P0** |
| **修复状态** | ✅ **已修复（`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）** —— 同属 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`，本组 **4 个文件 +91/-95**（`FCSharpBind.cpp` +7/-34、`FDynamicGeneratorCore.cpp` +48/-42、`FClassReflection.cpp` +34/-17、`FClassReflection.h` +2/-2），**纯原生、0 个 C# 文件**。<br>**① 数据结构**：`FClassReflection` 的 `Methods` 由 `TMap<TTuple<FString, int32>, FMethodReflection*>` 改为 `…, TArray<FMethodReflection*>>`；注册端 `ParseMethods` 的 `Methods.Add({Name, ParamCount}, new FMethodReflection(…))`（同名同元数**后写覆盖**）改为 **`Methods.FindOrAdd({Name, ParamCount}).Add(new …)`**（全部保留），`Deinitialize` 相应改为嵌套 `delete`（每个 `new` 恰好析构一次）⇒ 覆盖即泄漏（本报告 `:1037` 记的"必有泄漏"）一并消除。<br>**② 取方法**：`GetMethod(InName, InParamCount)` 由"返回该键的唯一值"改为"**优先返回该键数组里第一个 `IsOverride()`**；若一个 override 都没有，回退数组首元素"。判据来源：`FMethodReflection.cpp:22` `bIsOverride = bIsUFunction \|\| HasAttribute(OverrideAttribute)` ⇒ `IsOverride()` ⊇ `IsUFunction()` = "有原生对应物"，故有原生对应物的那个必须优先。⚠️ **回退分支必须保留**：它保住"派生类同名普通方法遮蔽父类"的旧语义（去掉后 `GetParentMethod` 会穿到父类，语义变化超出本条范围）。`GetMethod(IManagedHandle)` 改为嵌套遍历。<br>**③ 绑定端** `FCSharpBind::BindImplementation`：原"名字 → 单个方法"的 `TMap<FString, FMethodReflection*>`（`Add` 覆盖 ⇒ 同名只留最后注册的那个 override）整段删除，改为逐原生函数 `Class->GetMethod(FunctionName, FunctionParamCount)` + `IsOverride()` 判定（**41 行 → 12 行**）。候选选择与原写法**逐字等价**：命中键必是 `(name, FunctionParamCount)`（注册时键的元数与 `GetParamCount()` 同值），且两侧都取"该数组里第一个 `IsOverride()`"；一个 override 都没有时同样"不绑"。收益：`O(\|Functions\| × \|Methods\|)` 双重扫描 → 每函数一次 `O(1)` 哈希查找（`EnsureMethods()` 有 `bMethodsLoaded` 短路）。<br>**④ 生成端** `FDynamicGeneratorCore.cpp:199`/`:931` 两处 `GetMethods()` 遍历改嵌套循环；`:931` 的 `FName(Pair.Get<0>())` → `FName(Method->GetName())`（`:303` 起同一个 `FString` 既做键又传 ctor ⇒ **同值改写，零行为差异**）。<br>⚠️ **修复方向未照抄**（原文：去重键改为包含参数类型 / 用 `UFunction` 签名哈希；键冲突至少 `ensureAlwaysMsgf`）：**查表端手上只有"名字 + 元数"** —— `TMethodHelper.inl:59`（`sizeof...(Args)`）、`FCSharpFunctionDescriptor.cpp:14-22`（`PropertyDescriptors.Num()`）、`InitObject`/`ConstructorObject(InParamCount)`、`FTypeBridge.cpp:95`（传 0）—— 键里加类型会让这些调用点**全部查不到**，故只能"同键存全部候选 + 按 `IsOverride()` 取第一个"；`ensureAlwaysMsgf` **不做**（不符合项目规范，且"同键不同签名"现在正是**合法状态**而非错误）。<br>**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**（7 actions：`Module.UnrealCSharpCore` + `Module.UnrealCSharp` 重编、两个 DLL 重链）；新增注释行 0 / 新增 `throw` 0 / `if (…) return;` 0；4 文件 `{}`、`()` 配平；三元续行缩进按仓内既有规则（前导空格数 = 语句前缀长度）逐字符核对。<br>⚠️ **连带效应（交 `F-DYN-016` 跟踪，本次未动代码）**：同名**同参数个数**的两个 `[UFunction]` 现在**都**进反射表 ⇒ `FDynamicGeneratorCore.cpp:931` 对同名两次 `NewObject<UFunction>` ⇒ 引擎走"原地销毁重建"（`UObjectGlobals.cpp:3622` 同名查找、`:3669-3808` 替换，地址不变）返回**同一指针** ⇒ `:984-986` 的 `Children` **自环**（即 `F-DYN-016` 已登记的机制，行号由 `:927-987`/`:931`/`:977-983` 漂移到 `:931`/`:984-986`/`:990`）。**本组的目标输入不受影响**（`[UFunction] void SetHealth(float)` + 普通 `void SetHealth(int)` 只有一个 `IsUFunction()`；只有"两个同名同元数都带 `[UFunction]`"才自环）。<br>**残留**：① 同元数、不同类型参数的重载仍无法按类型区分（`GetMethod` 只到"元数"这一层），但选择从**哈希序不确定**变为**声明序确定**；② `F-RM-014`（`GetMethod`/`GetField` 不走继承链，只有 `GetParentMethod` 走）未变；③ 运行期断言未做（需起编辑器验证 override 分派确实落到 `[UFunction]` 版本）。**行号引用仍指修复前快照（`85348c68`）** |

### P0-10 动态类型生成期：父类解析失败崩溃 + 依赖图无环检测（2 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-DYN-001`、`F-DYN-002`（[02-…/03](../02-UnrealCSharpCore核心/03-动态类型生成.md)） |
| **级别 / 可达性** | 均 P0 / 均**活跃**（均"确认"） |
| **一句话根因** | ① C# 中间基类漏写 `[UClass]` ⇒ `PathName == ""` ⇒ `LoadClass` 返回 `nullptr` ⇒ `BeginGenerator` 直接解引用；② 依赖图**第二阶段没有环检测**，硬边构成的环会**无条件重新入队** → 主线程死循环（编辑器挂死，无崩溃报告）。 |
| **修复点（真实 `文件:行`）** | `FDynamicClassGenerator.cpp:286`（同类问题 `FDynamicInterfaceGenerator.cpp:156`）；建议点在该文件 `Generator()` 的 `:160-165` 之后加校验。<br>`FDynamicDependencyGraph.cpp:155-212`；建议段只给"`FDynamicDependencyGraph.h` 的 `FNode` 增加 `int32 PendingPasses = 0;`"（**仅文件名、无行号**） |
| **影响面** | 动态类型生成（本插件核心功能）全线；`F-DYN-002` 的后果是**编辑器永久挂死**（进程级、无诊断信息）。⚠️ 旧判据"自引用/互引用对象属性构成环"**不成立**（对象引用是软依赖，旧版在该路径整段跳过）—— **真环只能由硬边构成**（动态 struct 放 `TArray<自身>`、或 `[DefaultSubObject]` 互指） |
| **修复方向** | `BeginGenerator` 判空并**报出父类名**；第二遍加 visited 集合，环上 `ensureAlwaysMsgf` 并降级为软依赖 |
| **修复状态** | ✅ **已修复（① `492ce5f7` + ② `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）** —— **① 已修**：`FDynamicClassGenerator::BeginGenerator` 与 `FDynamicInterfaceGenerator` 已对父类解析结果加判空守卫，`InClass->ClassFlags \|= CLASS_Native` 移到守卫之外（总表 `F-DYN-001` 已标 **已处置**）；⚠️ 原文"报出父类名"这半句经确认**决定不做**：不符合项目规范。<br>**② 已修（同属 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`，本组 1 个文件 +7/-2）**：`FDynamicDependencyGraph::Generator()` 第二遍的重新入队改为**受限于每个节点的重入队计数** —— 新增 `TMap<FString, int32> PendingCounts;`，把 `NodeQueue.Enqueue(OutNode); continue;` 包进 `if (PendingCounts.FindOrAdd(OutNode)++ < NodeArray.Num())`。超过上限即**不再入队**、落到原地的 `if (bIsCompleted) { Generator(); Completed(); }` 完成该节点 = **把回边当软依赖**（环上无法存在拓扑序，只能任选一个先生成）。<br>⚠️ **修复方向未照抄（两处）**：**(a)** 审计建议"第二遍加 visited 集合" —— **visited 不够**：一个节点可以**合法地**为多个硬依赖各等一次（每次等待对应某个依赖完成），"重入队两次"并不等于成环，只有**计数上限**才是正确判据；**(b)** 审计建议"环上 `ensureAlwaysMsgf`"**决定不做**（沿用项目规范：不加日志/`ensure`）。**(c)** 上限 `NodeArray.Num()` 是**可证的安全上界**：每次合法重入队都对应"某个别的节点完成了"，故合法次数 ≤ 节点总数。<br>🔑 **修法得以极小的关键**：`bIsPending == true` 时 `bIsCompleted` **仍是 true**（两者只在 `IsInitial()` 分支里被置 `false`）⇒ 破环**不需要任何额外赋值**，只需不再 `continue`。**(d)** 未采用审计建议的 `FNode::PendingPasses` 成员：`NodeArray` 挂在 `static` 单例上、`AddNode` 只追加不重置 ⇒ 成员计数会**跨 `Generator()` 调用累积**，导致后续某轮把"本可正常等待"的节点提前强生成；局部 map 天然按轮重置。**(e)** 变量名经评审定为 `PendingCounts`（不用审计的 `PendingPasses`：计的是"重入队次数"而非"pass 数"，且须体现"每节点一份"）。<br>**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**（重编 `Module.UnrealCSharpCore`、重链 `UnrealEditor-UnrealCSharpCore.dll`）；**算法仿真**（把 pass 1 的 DFS + pass 2 的 drain 逐行照抄成脚本，旧版 vs 新版对照）：修复前 `A⇄B` 硬环、`A→A` 自环、硬环+软边混合三种拓扑**全部 HANG**（>20000 轮），链 `A→B→C`（3 轮）与菱形 `A→{B,C}→D`（3 轮）正常；修复后三种成环拓扑**全部终止**（6/2/8 轮），而**链与菱形的轮数与生成顺序逐字不变**（`C→B→A` / `D→B→C→A`）⇒ 无环图零回归。<br>**残留（未做）**：`else { // @TODO }`（"硬依赖仍为 `Initial` ⇒ 既不生成也不重入队"）—— ⚠️ **我上轮说"一并收"是错的，此处更正**：按状态单调性（`Initial→Pending→Completed` 只前进，且 pass 1 会把每个可达的 `Initial` 硬依赖推栈处理）推导，该分支**不可达**，改不可达代码是投机，故**未动**（该推导为静态推演，非机器验证）。**运行期断言未做**：需起编辑器构造硬边环（动态 struct 放 `TArray<自身>`、或 `[DefaultSubobject]` 互指），断言生成**正常结束**且不挂死 |
| **同族** | `F-DYN-008` 声明"与 `F-DYN-001` 同根因"（在 P1 族跟踪） |

### P0-11 JSON 反序列化失败后解引用空 `TSharedPtr`（1 条）

| 项 | 内容 |
|---|---|
| **组内编号（带报告路径）** | `F-GENC-001`（[03-…/01](../03-代码生成器/01-代码生成框架与模块入口.md)） |
| **级别 / 可达性** | P0 / **活跃**（"确认"） |
| **一句话根因** | `Deserialize` 的返回值被丢弃，随后解引用空的 `TSharedPtr`。引擎 `SharedPointer.h:1082` 的 `operator->` 用 `check()` ⇒ **Development 下是确定性致命断言**。 |
| **修复点（真实 `文件:行`）** | `Source/UnrealCSharpCore/Private/Common/FUnrealCSharpFunctionLibrary.cpp:1181-1219`（**崩溃行 `:1195`**）；**同型 4 处**：`:1193/1195`、`:1233/1235`、`:1319/1321`、`:1364/1366`；调用点 `Source/ScriptCodeGenerator/Private/FGeneratorCore.cpp:1016`、`Source/UnrealCSharpCore/Private/Dynamic/FDynamicGeneratorCore.cpp:21` |
| **影响面** | **每次生成都崩，用户无法通过重开编辑器绕过**。触发面比初判宽：`FGeneratorCore.cpp:1016` 的读盘**无 `bIsFull` 守卫** ⇒ 损坏的 `OverrideFunction.json` 会在**第一次保存资产时**崩 |
| **同族需合并计数** | ⚠️ 与 `08-…/04` 的 `F-ABI-033`、`02-…/07` 的 `F-FL-002` **是同一条** → **合并计一次**（这两条 P1 行已在总表同步标 **已处置**）。⚠️ **计数口径更正（复核实测）**：`FJsonSerializer::Deserialize` **调用点 4 处**（`FUnrealCSharpFunctionLibrary.cpp:1192`/`:1232`/`:1319`/`:1367`，4 个 `if` 守卫**已全覆盖**），原文"6 处"指的是 **6 处 `->Values` 遍历点**（`:1194`/`:1234`/`:1324`/`:1337`/`:1372`/`:1385`）—— 两者不可混用，**不是漏修** |
| **修复方向** | 4 处补返回值检查；建议把读盘/反序列化收敛为一个"带回退与日志"的公共入口 |
| **修复状态** | ✅ **已修复（`492ce5f7`）** —— `FUnrealCSharpFunctionLibrary` 同型 4 处 `FJsonSerializer::Deserialize`（`:1191`/`:1231`/`:1319`/`:1367` 附近）均改为判返回值，`TryGetObjectField` 结果同样判空。总表 `F-GENC-001` 已标 **已处置**。原文建议的"读盘/反序列化收敛为带回退与日志的公共入口"**未做** （修复提交 `492ce5f7`） |

### 1.1 ⚠️ 与 P0 同批必读的「误修警戒」——活值 `Dest` 权威修复清单 = **4 个点，不是 7 个**

> 本节**不新增 P0 计数**（该族本身是 **P1**：`F-PROP2-002`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)）、`F-FN-001`、`F-ABI-032` 等）。
> 但它是**最重要的单项修正**：M5 裁决给出的"权威修复清单 = **7 个**「活值 `Dest`」调用点"被 `INDEX.md:66`、`00-总览/03:89`/`:392` **三处原样引用**，而**照它施工会引入比原缺陷更严重的 UB**。

**权威修复清单 = 4 个真泄漏点（只改这 4 处）**：

| # | 修复点 | `Dest` 状态 |
|---|---|---|
| 1 | `FRegisterProperty.cpp:35` | **活体 UObject 的属性内存**（主路径） |
| 2 | `FRegisterProperty.cpp:64` | **活体 UScriptStruct 的属性内存** |
| 3 | `FArrayHelper.cpp:137` | `Set(Index, InValue)` 覆盖**既有活元素**（`:135 IsValidIndex` 通过后） |
| 4 | `FOptionalHelper.cpp:91` | `:89 MarkSetAndGetInitializedValuePointerToReplace(Data)` 明示"待替换的已初始化值" |

**🔴 明确标注「不要改」的 4 个点（补 `DestroyValue` = UB）**：

| # | 不要改的点 | 为什么不改 |
|---|---|---|
| 1 | `FArrayHelper.cpp:269` | `:267 AddUninitializedValue()` → **新分配、未初始化** |
| 2 | `FMapHelper.cpp:200` | `:196 AddUninitialized` → **未初始化**（新键分支） |
| 3 | `FMapHelper.cpp:218` | 新键分支未初始化；复用分支在 **`:215` 已先 `DestroyValue`**（作者**正确**执行"复用前先析构"的对照实现） |
| 4 | `FSetHelper.cpp:102` | `:98 AddUninitialized(...)` → **新分配、未初始化** |

**引擎依据（决定性）**：`Runtime/CoreUObject/Public/UObject/UnrealType.h:4015-4021` 的 `AddUninitializedValues(int32 Count)` = `WithScriptArray([...]{ return Array->Add(Count, ElementSize, ElementAlignment); })` —— **`AddUninitialized*` 只裸分配、绝不构造**。⇒ 在它的产物上调用 `InitializeValue` 是**引擎契约要求的正确行为**；反之对其调用 `DestroyValue` 才是 **UB / 二次释放**。旁证：`UnrealType.h:1434` 的 `InitializeValue` 契约原文 *"this assumes over uninitialized memory"*。

**修法（三选一，按优先级）**：
1. **最小且安全**：不动描述符的 `Set`，让**活值调用点**自己先 `DestroyValue` —— 即只改上表 4 个点；
2. **语义最清晰**：给描述符拆一对 API —— `Set`（活值语义：内部 `DestroyValue` + `InitializeValue` + 赋值）与 `SetUninitialized`（仅 `InitializeValue` + 赋值），由调用点显式选择；
3. **不要**一刀切：全删 `InitializeValue` 会破坏 `Add` 路径；无差别加 `DestroyValue` 会对未初始化内存调析构（同样崩溃）。

> **这份「4 + 4」的措辞出自哪里（provenance，避免下游再被误导）**：`01-…/04b` 的 `F-PROP2-002` 正文 §的清单标题写的仍是"**7 个描述符**"，其 L617 的"权威修复清单"实际列了 **8 个路径**、且只把 **3 个**（`FArrayHelper.cpp:269`、`FMapHelper.cpp:200`、`FSetHelper.cpp:102`）标为"改则坏…必须排除在外"（**漏标了 `FMapHelper.cpp:218`**）。**"4 个真泄漏 + 4 个不要改"这一最终口径不在 04b 里** —— 本节上表即该口径，4 个真泄漏点（`7 个 → 4 个`）为 `FRegisterProperty.cpp:35`、`:64`、`FArrayHelper.cpp:137`、`FOptionalHelper.cpp:91`，4 个**不要改**的点为 `FArrayHelper.cpp:269`、`FMapHelper.cpp:200/218`、`FSetHelper.cpp:102`。⇒ **引用本清单时请引用本节的两张表，不要引用 04b 的"7 个描述符"标题。**

> **方法学教训**：**"已裁决"不是免检凭证。** 凡被多份文档反复引用的**聚合型结论**（修复清单、范围数字、计数口径），必须回到**原始代码**验证一次 —— 这类结论一旦错，会以"权威"的名义扩散到全套文档，且**照它施工会造成实际损害**。

### 1.2 处置记录（按修复提交登记）

> **本表是"哪条 finding 由哪个提交修复"的唯一登记处**：同一行内的所有编号属**同一次修复动作**。
> **修复提交**：插件（`UnrealCSharp`）仓库 **`492ce5f7` "Null Validation"**（2026-09-15，**54 个文件**，父提交 `85348c68`）。
> **分析快照**：`85348c68`（2026-09-11 —— **推断**，与总表"汇总时间戳 2026-09-11 01:04"一致，待确认）。报告里的 `文件:行号` 一律指该快照；修复后行号已漂移，核对照点请用 `git show 85348c68:<插件内路径>`。
> **本表现已覆盖全部 26 条 P0 编号**（最后补登的是 `F-RM-001`，见 `P0-9` 行）；**唯一没有修复提交可填的是 `F-DOM-003`** —— 它经确认**决定不做**（不符合项目规范），**不得视为已修**，详见 P0-7 组两行。⚠️ **P0-4 / P0-5 / P0-6 / P0-8 / P0-9 / P0-10② 六组统一记 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`**（**原六次分散提交已合并为这一次提交**，该合并提交的父提交正是 `492ce5f7`），故这六组不再各自对应一个提交。⚠️ **四处"修复方向未照抄"**：`P0-4` 的契约面、`P0-8` 的"句柄存入成员容器"、`P0-9` 的"键含参数类型 + 键冲突 `ensureAlwaysMsgf`"、`P0-10`② 的"visited + `ensureAlwaysMsgf`"（理由见各行）。

| 组 | 编号（带报告路径） | 级别 | 修复提交 | 修复要点（一句话） |
|---|---|---|---|---|
| **P0-1** | `F-PROP2-004`、`F-PROP2-005`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)） | P0 | `492ce5f7` | 6 个 `Identical` 覆写改为先解析托管句柄、未命中即 `return false`；`FStructPropertyDescriptor` 去掉重复计入的 `Offset_Internal`；`FOptionalPropertyDescriptor::Set` 对 `GetOptional` 判空 |
| **P0-2** | `F-PROP2-001`、`F-PROP2-002`（[01-…/05](../01-UnrealCSharp运行时/05-属性描述符-对象与委托类型.md)）、`F-DEL-003`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)）、`F-LIFE-003`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)） | P0 | `492ce5f7` | 全插件 43 处 `Class->NewObject()` 统一置于 `if (Class != nullptr)` 内；4 个描述符的 `Set` 补 `SrcMulti != nullptr`；单播与多播委托 `Set` 补 `GetDelegate<>` 判空 |
| **P0-3** | `F-INT1-001`、`F-INT1-002`（[01-…/10](../01-UnrealCSharp运行时/10-Interop注册-对象与反射类.md)）、`F-INT2-001`（[01-…/11](../01-UnrealCSharp运行时/11-Interop注册-数学值类型与引擎类.md)）、`F-HLP-023`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)） | P0 | `492ce5f7` | 三处导出层补判空（`TScriptInterface.GetObject`、`UClass.RemoveFunction`、`GetDataTableRowFromName` 改判 `Find` 结果）；`FRegisterMap`/`FRegisterArray`/`FRegisterSet` 共 7 处（`FRegisterMap` 5 处 + `FRegisterArray` 1 处 + `FRegisterSet` 1 处）未命中回退 `FPropertyDescriptor::GetDefaultValue` |
| **P0-4** | `F-INT2-001`（[01-…/10b](../01-UnrealCSharp运行时/10b-Interop注册-容器字符串与对象指针.md)，**与 `11` 的同名条目不是一条**）、`F-CS2A-001`、`F-CS2A-002`、`F-CS2A-017`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md)）、`F-CS2B-001`（[06-…/02b](../06-CSharp运行时与脚本/02b-对象字符串与工具类型包装.md)） | P0 | `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（父 `492ce5f7`，本次合并提交） | **已完成**。① `F-INT2-001`(10b) 由 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` 修复：`FArrayHelper` 变更类方法（`RemoveAt`/`InsertZeroed`/`InsertDefaulted`/`SetNum`/`Reset`/`Empty`/`Swap`/`SwapMemory`/`AddUninitialized`/`AddZeroed`）与 `FMapHelper::Empty`/`FSetHelper::Empty` 改用**正向 `if (条件) { 函数体 }` 包裹**（同文件 `Get`/`Set` 的形态；`Swap`/`SwapMemory` 用 if-init helper + 两次 `IsValidIndex`），越界/负值不再进 `Memmove`/`Memswap`/`DestructItems`。②③④⑤ 的失效机制由父提交 `492ce5f7` 闭合：`FRegisterArray`/`FRegisterMap`/`FRegisterSet` 取值未命中回退 `FPropertyDescriptor::GetDefaultValue`，六个指针注册器与 4 个字符串注册器的取值/`ToString` 判空回 `InvalidManagedHandle`；`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` 另在 5 个字符串包装的 `ToString()` 加非抛出兜底 `?? string.Empty`。**本组恰好 8 个文件**（原生 3 + C# 5，+82/-41）。<br>⚠️ **契约面经确认决定不做**：C# 侧一切异常语义（`IndexOutOfRangeException`/`KeyNotFoundException`/`InvalidOperationException`）、`HandleData.GetHandle` 处「按需注册」、`IsValid()`、`TMap.Find`/`FindKey` 的未命中信号、任何新增公开 API —— 故越界/缺键/未注册句柄静默回 `default`/`null`（与合法默认值不可区分）。不加注释/日志/`ensure`。<br>**验证（在该提交上重跑）**：两套 C# 工程 **0 warning / 0 error** + UBT 增量构建 **Result: Succeeded**；新增注释行 0、新增 `throw` 0、`if (…) return;` 0。**§6 运行期断言未做** |
| **P0-5** | `F-BIND-001`（[01-…/08](../01-UnrealCSharp运行时/08-绑定层公开头文件（模板元编程）.md)）＋同批的 `F-GEN2-001`（P1，[03-…/02](../03-代码生成器/02-委托结构体枚举与GameplayTag生成器.md)，**非同族但必须同批**） | P0 | `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（与 P0-4 同一次合并提交） | **两端同一提交**：**① 写端 `TOut.inl`** —— `Buffer += std::get<Index>(TBufferOffset<Args0...>()());` → `Buffer += TTypeInfo<std::decay_t<T>>::Get()->GetBufferSize();`，按「刚写入参数自身尺寸」紧凑推进（写入量与 `GetBufferSize()` 对 primitive/compound 两分支**逐一相等**，并与 C# 生成端同一单位）；**`TBufferOffset` 一行未动**（它被 `TFunctionHelper`/`TConstructorHelper`/`TSubscriptHelper` 共 5 处当 **IN 缓冲**偏移表用，语义正确）。**② 读端 `FGeneratorCore.cpp:626`** —— `GetObject(*(nint*)%s%s)` → `GetObject(*(nint*)(%s%s))`；该格式串有 **3 处**调用点（`FClassGenerator.cpp:723`、`FBindingClassGenerator.cpp:597`、**`FDelegateGenerator.cpp:263`**），一处修复即全覆盖；`GetReturn`（`:597`）无 offset、本来就正确。**产物**：235 处（74 文件）按修复后模板逐字重建，旧形态残留 0。**量化（本轮新增）**：1603 个带 out 缓冲的方法中修复前 正确 1364 / 值错位 93 / **越界写 146**；修复后 **1603/1603** 满足 `Σ(ref 尺寸) == C# 缓冲长度`。**验证**：UBT **Succeeded**（`Module.UnrealCSharp` + `Module.ScriptCodeGenerator`）+ `dotnet build UE.csproj` **0 warning / 0 error**；用户已确认验证无问题。**未做**：编辑器完整重新生成（1073 处无偏移行为纯文本规范化）与 `Content/Script/UE.dll` 更新 |
| **P0-6** | `F-DEL-002`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)） | P0 | `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（与 P0-4 同一次合并提交） | 1 文件 +2/-1：`ProcessEvent` 的 `FUNCTION_CSHARP_CALLBACK` 分支改为**先取快照再遍历** —— `const TArray<FDelegateWrapper, TInlineAllocator<4>> DelegateWrappersCopy(DelegateWrappers);`，逐字对照引擎 `ProcessMulticastDelegate`（`ScriptDelegates.h:917`）。修复前 `Clear()` → `Empty()` 真释放底层分配 ⇒ 迭代器悬垂（真 UAF），`Remove`/`RemoveAll` ⇒ 回调被跳过/重复。**语义变化**：广播开始时在场的条目各被调用恰好一次（= 引擎既定语义）。**边界**：跨线程变体仍属 P1 族（快照本身也竞争）；快照内 `FMethodReflection*` 裸指针寿命属 G4 族；运行期断言未做 |
| **P0-8** | `F-ED2-001`、`F-ED2-017`（[04-…/02](../04-编辑器模块/02-工具栏监听器进度对话框与模块入口.md)）、`F-LIFE-001`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)） | P0 | `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（与 P0-4 同一次合并提交） | **2 个修复动作**（`F-ED2-001` 与 `F-LIFE-001` 同处代码合并计），本组 3 文件 +37/-10。**①** `FEditorListener` 析构补反注册：`GetModulePtr<FAssetRegistryModule>` 取到模块后，对 5 个 AssetRegistry 委托各调一次 `RemoveAll(this)` —— **未加任何句柄成员**（这 5 处都是 `AddRaw(this,…)` 裸指针绑定，`RemoveAll` 按对象即可精确匹配；`GetModulePtr` 关停期取不到就跳过，且跳过是安全的）。**②** `FUnrealCSharpBlueprintToolBar`：`FAssetEditorExtender::CreateLambda` → **`CreateRaw`**（lambda 型委托 `HasSameObject` 恒 false，注册后无法摘除），`Deinitialize()` 补 `GetExtenderDelegates().RemoveAll(IsBoundToObject(this))`；签名随之改为委托要求的 `(const TSharedRef<FUICommandList>, const TArray<UObject*>)`。**③** 顺手修 `FOnGetContent::CreateLambda([&]` → `[this, InBlueprint]` 的悬垂引用捕获。**④** 审计建议的"丢弃返回值"扫描已落成脚本：44 处保存句柄 / 5 处 `RemoveAll(this)` 配对 / 1 处未配对（`UnrealCSharpEditor.cpp:167`，捕获为空 + cook-only ⇒ 非缺陷）。**残留**：跨模块卸载仍开着的蓝图编辑器内层 lambda 仍悬垂；运行期断言未做 |
| **P0-7** | ⚠️ 部分：`F-BR-001`（[02-…/06](../02-UnrealCSharpCore核心/06-类型桥接编码设置与引擎监听.md)）**已修复**；`F-DOM-003`（[02-…/04](../02-UnrealCSharpCore核心/04-脚本域抽象与三大后端.md)）**未修复** | P0 | `492ce5f7`（仅 `F-BR-001`） | `F-BR-001`：`FTypeBridge::Factory` 全部调用点判空（`TPropertyValue.inl` 10 处等）。**`F-DOM-003` 无修复提交** —— `FDomain.cpp`/`FScriptDomainFactory.cpp` 不在该提交内，`FDomain.cpp:31` 仍无条件解引用；经确认**决定不做**（不符合项目规范） |
| **P0-10** | `F-DYN-001`、`F-DYN-002`（[02-…/03](../02-UnrealCSharpCore核心/03-动态类型生成.md)） | P0 | `F-DYN-001` → `492ce5f7`；**`F-DYN-002` → `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`**（与 P0-4 同一次合并提交） | **①** 两个生成器加父类判空守卫（原文"报出父类名"经确认**决定不做**）。**②** `FDynamicDependencyGraph::Generator()` 第二遍的重入队加**每节点计数上限**：`TMap<FString, int32> PendingCounts;` + `if (PendingCounts.FindOrAdd(OutNode)++ < NodeArray.Num())` 包住原有 `Enqueue`+`continue`；超限即让原地的 `if (bIsCompleted)` 完成该节点 = **回边当软依赖**。⚠️ **未照抄**:"visited 集合"不成立（节点可合法地为多个依赖各等一次）、`ensureAlwaysMsgf` 不做、`FNode::PendingPasses` 成员换成局部 map（避免跨轮累积）。**验证**：构建 Succeeded + 旧/新算法仿真对照（三种成环拓扑 HANG→终止；链与菱形逐字不变） |
| **P0-11** | `F-GENC-001`（[03-…/01](../03-代码生成器/01-代码生成框架与模块入口.md)） | P0 | `492ce5f7` | `FUnrealCSharpFunctionLibrary` 同型 4 处 `FJsonSerializer::Deserialize` 均判返回值，`TryGetObjectField` 结果同样判空 |
| **P0-9** | `F-RM-001`（[02-…/02](../02-UnrealCSharpCore核心/02-反射模型（类字段方法参数属性）.md)） | P0 | `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（与 P0-4 同一次合并提交） | **4 个文件 +91/-95，纯原生**。`FClassReflection::Methods` 的 value 由 `FMethodReflection*` 改为 `TArray<FMethodReflection*>` —— 注册端 `Methods.FindOrAdd({Name, ParamCount}).Add(new …)`（不再覆盖 ⇒ **覆盖即泄漏一并消除**）、`Deinitialize` 嵌套 `delete`；`GetMethod(Name, ParamCount)` 改为**优先返回该键数组里第一个 `IsOverride()`**（`bIsOverride = bIsUFunction \|\| HasAttribute(Override)` ⇒ 有原生对应物者优先），无 override 时回退数组首元素（**保住"派生类遮蔽父类"的旧语义**，不可删）；`GetMethod(IManagedHandle)` 嵌套遍历。`FCSharpBind::BindImplementation` 的"名字 → 单个方法"map + 双重扫描（41 行）改为逐原生函数 `Class->GetMethod(FunctionName, FunctionParamCount)` + `IsOverride()` 判定（12 行；候选选择**逐字等价**，并顺带把 `O(\|Functions\| × \|Methods\|)` 降为每函数 `O(1)`）。`FDynamicGeneratorCore.cpp:199`/`:931` 两处改嵌套循环、`:931` 的 `FName(Pair.Get<0>())` → `FName(Method->GetName())`（同值改写）。⚠️ **未照抄修复方向**：审计的"去重键含参数类型 / `UFunction` 签名哈希"**不可行** —— 查表端只有"名字 + 元数"（`TMethodHelper.inl:59`、`FCSharpFunctionDescriptor.cpp:14-22`、`InitObject`/`ConstructorObject`、`FTypeBridge.cpp:95`），键里加类型会让这些点全部查不到；"键冲突 `ensureAlwaysMsgf`"**决定不做**（不符合项目规范，且同键不同签名现在是**合法状态**）。**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Succeeded**（7 actions，两个模块重编 + 双 DLL 重链）；注释 0 / `throw` 0 / `if (…) return;` 0。⚠️ **连带**：`F-DYN-016`（P1）触发面放大到"同名同参数个数的两个 `[UFunction]`"（`NewObject` 原地重建 ⇒ `Children` 自环），**本组目标输入不受影响**，代码未动。**残留**：同元数不同类型重载仍不能按类型区分（但选择由哈希序不确定 → 声明序确定）、`F-RM-014` 未变、运行期断言未做 |

**合计**：已修复 **10 组 / 26 条编号**（`P0-1`/`P0-2`/`P0-3`/`P0-11` 与 `P0-10`① 记 `492ce5f7`；**`P0-4`/`P0-5`（含同批 P1 条目 `F-GEN2-001`）/`P0-6`/`P0-8`/`P0-9`/`P0-10`② 六组统一记 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`（原六次分散提交已合并）**；P0-4 / P0-8 / P0-9 / P0-10 的"修复方向"各有未照抄之处，见本表各行）；**部分修复 1 组**（P0-7：① `F-BR-001` 已修 / ② `F-DOM-003` 决定不做）；**未动 0 组** —— 26 条 P0 编号**至此全部有处置登记**（其中 `F-DOM-003` 是"决定不做"而**不是**"已修"）。以上均只加状态标注，**级别与计数口径不变**。

> **非 P0 的提交登记（本轮新增，按维护者提交回填）**：→ **`F-REG-019`**（本轮新编号，P1；`nullptr` 实参经弱指针为键的注册表查询命中"已销毁但未注销"残留条目）由**压缩提交 `cd0dfacb`** 修复：① `Bind(UObject*)` + `FObjectRegistry::GetObject/GetManagedHandle` **三处判空**（2 文件 +25/−8）；② `FObjectRegistry::RemoveReference(const UObject*)` **同族判空**（1 文件 +9/−6）。逐条处置与验证见 §2.1 与 [`08-…/06`](../08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md)。

---

## 2. 计划修（P1，128 条 → 按根因/修复点合并为 15 组）

> 代表条目全部取自 `09-…/01` 总表的 **P1 行**（128 行）。
> **口径说明**：下表列的是**族与代表条目**，不是穷尽清单 —— 一个族内往往还有若干编号（例：G1 强 `GCHandle` 族仅 `F-REG2-002`/`F-REG-007`/`F-CS2A-023` 等就横跨 5 份报告）。**故各组合计不等于 128**；未归族的**单点 P1**（多为"缺防御而非已出错"与单文件语义问题）不逐一列出，完整 128 行见 [`01-全局问题总表.md`](01-全局问题总表.md)。
> **编号一律带报告路径**（31 处重号，见 §0）。

| # | 组（根因一句话） | 代表条目（带报告路径） | 修复方向 |
|---|---|---|---|
| **G1** | **强 `GCHandle` 钉死终结器 → 释放责任被交给永不执行的终结器**（闭环泄漏，本清单**最高频的重复计数族**）🔴 **前提已被本轮实测更正**（"终结器永不执行"不成立：`TArray` 族实测注册 ≥276 / **注销 364** / `GCHandle_Free` **7962** ⇒ 只有"**有终结器但无原生释放点**"的路径才是真缺口；见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §3）。✅ **首刀已修 3 处（`F-REG-007`、`F-DEL-008`、`F-RM-004`+`F-DOM-008`，已随 `8c54c598` 提交）+ 其余七刀（第二～第十刀，同提交）**；🔴 **第十一刀（2026-09-23，已提交 `3da38bb6`）**：① 结构体 `FMember` 同址覆盖缺陷**实测触发并修复**（`over 4→0`/`orphan 4→0`）；② **非地址映射 9 族弱句柄实装**（`rel` 0 → 891/756、残留 −45.7%/−38.8%、终结器计数由全 0 变非零）；③ 地址映射子集经裁定**不弱化**（**该裁定已被 §2.4.12 第十二刀推翻**：改走全弱化）。详见 §2.4.11；🔴 **第十二刀（2026-09-23，已提交 `3da38bb6`）**：**路线 A 全弱化**（新增 `HandleData.IsAlive` 桥接 + 5 个反查点自愈 + 地址映射创建点全弱化，含 C# `StructImplementation` 主路径）⇒ 会话内释放率 **41.9~49.3% → 90.2%**、`addrmap` **1391→117**、`Struct rel 12→1104`；LeanCLR 仍 0（BCL 未定位）。详见 §2.4.12 | `F-REG2-002`（[01-…/03](../01-UnrealCSharp运行时/03-容器委托字符串注册表与模块入口.md)）、`F-REG-007`（[01-…/02](../01-UnrealCSharp运行时/02-对象引用结构体注册表.md)）、`F-CS2A-023`、`F-CS2A-019`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md)）、`F-CS2B-002`、`F-CS2B-012`（[06-…/02b](../06-CSharp运行时与脚本/02b-对象字符串与工具类型包装.md)）、`F-LEAK-001`、`F-LEAK-002`（[08-…/02](../08-专项审计/02-资源泄漏审计（内存句柄与容器）.md)）、`F-INT2-011`（[01-…/10b](../01-UnrealCSharp运行时/10b-Interop注册-容器字符串与对象指针.md)）、`F-ENV-002`（[01-…/01](../01-UnrealCSharp运行时/01-Environment与绑定注册表.md)）、`F-DOM-006`、`F-DOM-008`、`F-RM-004`（[02-…/04](../02-UnrealCSharpCore核心/04-脚本域抽象与三大后端.md) / [02-…/02](../02-UnrealCSharpCore核心/02-反射模型（类字段方法参数属性）.md)）、`F-CS2A-005`、`F-DEL-008`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)） | `HandleData.Alloc` 改 `GCHandleType.Weak`（或 `WeakTrackResurrection`）+ 对需要确定性释放的类型实现 `IDisposable`；补 `MethodBridge.Invoke` 的 out 句柄释放；`FMultiRegistry.inl:54-79`、`FOptionalRegistry::RemoveReference` 补 `GCHandle_Free`；`#if WITH_CORECLR` 屏蔽改为按后端开关（`F-RM-004`/`F-DOM-008` 的 `FFieldReflection.cpp:15-20` 恰好把唯一活跃的 LeanCLR 排除在释放外）。**⚠️ 必须优先于其它泄漏项**：它是"终结器永不执行"的总闸，不先修则大量 P1 泄漏的修复都无效果（见 §4 第 4 条） |
| **G2** | **注册表句柄释放契约不一致 / `TMap::Remove` 之后的失效读** | `F-REG-001`、`F-REG-002`、`F-REG-003`、`F-REG-004`、`F-REG-009`（[01-…/02](../01-UnrealCSharp运行时/02-对象引用结构体注册表.md)）；`F-REG-005`（**已从"撤销"恢复 P2**，见 §5） | 统一 `RemoveReference` 样板（同类实现都有、只有缺陷处缺：`FContainerRegistry.inl:76`、`FDelegateRegistry.inl:75`、`FStringRegistry.inl:76` 可作模板）；`FObjectRegistry.cpp:84/86/88/90/92` 改为**按值拷贝**元素再 `Remove`（`Find` + `Remove` 合并为 `FindAndRemoveChecked`） |
| **G3** | **参数缓冲池 / "只 `Free` 不析构" → 元素与内部堆泄漏** | `F-FN-001`（[01-…/06](../01-UnrealCSharp运行时/06-函数与类描述符.md)）、`F-ABI-032`（[08-…/04](../08-专项审计/04-跨语言ABI与平台兼容审计.md)）、`F-HLP-001`、`F-HLP-020`、`F-HLP-025`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)）、`F-HLP-014`（同）、`F-PROP2-002`、`F-PROP2-003`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)） | 把 `FunctionMacro.h:147` 的 `BufferAllocator->Free(Params);` 抽成独立的 `PROCESS_FREE()` 宏（`Call2/4/6/10/14` 的 `ProcessEvent`/`Invoke` **之后**补上释放）；`FPropertyDescriptor` 补 `DestroyValue_InContainer` 转发（当前只有 `InitializeValue_InContainer`，`FPropertyDescriptor.h:67`）；容器 Helper 的释放改经 `InnerPropertyDescriptor->DestroyValue` 逐元素析构后再释放容器头。**⚠️ 一行一改要小心**：`F-ABI-031`/`F-ABI-032` 与 `F-FN-001` 是**同一病根**（`Malloc` 无配对 `Free` 的宏约定），`08-…/04:1055`/`:1124` 都写明"**聚合时只计一次**" |
| **G4** | **跨域缓存不失效（热重载 UAF / 静默元数据错配）** ✅ **核心已修（2026-09-22，已提交 `8c54c598`；见 §2.3）** —— 六处 `static` 缓存已去静态化；**本组其余代表条目（`F-DYN-003` / `F-DYN-008` / `F-DYN-016` / `F-LIFE-006`）本轮未动** | `F-REFL-002`（[02-…/01](../02-UnrealCSharpCore核心/01-反射注册表FReflectionRegistry.md)）、`F-LIFE-010`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)）、`F-DYN-006`、`F-DYN-003`、`F-DYN-008`、`F-DYN-016`（[02-…/03](../02-UnrealCSharpCore核心/03-动态类型生成.md)）、`F-LIFE-006`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)） | 六个函数级 `static`（`FDynamicGeneratorCore.cpp:1044/1075/1090/1103/1116/1209` — 分析快照记 `:1036/1067/1082/1095/1108/1201`）持裸 `FClassReflection*`，而 `FReflectionRegistry.cpp:867-874` 每次热重载 `delete` 它们 → 改为**以代次（generation id）为键的缓存**或类成员并在 `Deinitialize()` 清空。⚠️ **降 P0→P1 的理由**：`FReflection::HasAttribute` 只是 `Attributes.Contains(P)` 的**指针相等**，命中即指向存活对象，**不是 UAF**，真实后果是**静默元数据错配**。<br>✅ **本轮实际修法（第 3 条路，报告未列但同属其第 1 条建议）**：六个 getter **去掉缓存本身**、返回 by-value —— 数组每次调用从注册表现查重建，「缓存跨 `Deinitialize()` 存活」结构性不可能；**不新增 generation API、不留失效逻辑**。⚠️ 与"代次缓存"的取舍见 §2.3 |
| **G5** | **无锁并发 / `TQueue` 契约违反** | `F-ED2-008`（[04-…/02](../04-编辑器模块/02-工具栏监听器进度对话框与模块入口.md)）、`F-CONC-005`、`F-CONC-001`、`F-CONC-004`、`F-CONC-007`（[08-…/03](../08-专项审计/03-线程安全与并发审计.md)）、`F-CMP-004`（[05-…/01](../05-编译器与跨版本/01-编译器CSharp编译与跨版本宏.md)） | `TQueue<bool> Tasks` 用默认 `EQueueMode::Spsc`，而 `OnBeginGenerator`/`OnEndGenerator` 在**游戏线程（生产者）**调 `Tasks.Empty()`；引擎 `Queue.h:105` 明写 `Empty()` *"To be called only from consumer thread"*、`:201` 的 `IsEmpty()` 同 → 与工作线程 `Dequeue/Pop` 并发可**双重释放队列节点**（`Queue.h:273-276`）。改 MPSC 模式，或把 `Empty()`/`IsEmpty()` 移出游戏线程 |
| **G6** | **关停期复合死锁 / 无超时忙等** | `F-CONC-003`（[08-…/03](../08-专项审计/03-线程安全与并发审计.md)）、`F-CMP-002`（[05-…/01](../05-编译器与跨版本/01-编译器CSharp编译与跨版本宏.md)）、`F-ED2-005`、`F-ED2-007`（[04-…/02](../04-编辑器模块/02-工具栏监听器进度对话框与模块入口.md)）、`F-CONC-010`、`F-CONC-011`（[08-…/03](../08-专项审计/03-线程安全与并发审计.md)） | **决定性一环**：`TaskPrivate.cpp:84-88` 明文 *"task retraction is not supported for named thread tasks"* ⇒ 派给 GameThread 的任务**必然 retract 失败**，编译线程只能走 `TaskGraph.cpp:1509` 纯阻塞，与游戏线程 `Kill(true)` 的 `WaitForSingleObject(INFINITE)`（`MicrosoftRunnableThread.h:100`）**真互等**。修法：`SyncProcess`（`FUnrealCSharpFunctionLibrary.cpp:1569-1574`）加超时 + 可取消 + 错误码；`WaitForCompile`（`FEditorListener.cpp:712-745`）去掉无超时忙等与 `HeartBeat()` 掩盖；`:242` 的同步等待改为"仅游戏线程内联、否则 `AsyncTask` 续接" |
| **G7** | **`[UnmanagedCallersOnly]` 边界不判空 / 不兜异常 / 静默降级** | `F-CS2B-027`、`F-CS2B-009`、`F-CS2B-010`（[06-…/02b](../06-CSharp运行时与脚本/02b-对象字符串与工具类型包装.md)）、`F-ABI-001`、`F-ABI-030`、`F-ABI-034`（[08-…/04](../08-专项审计/04-跨语言ABI与平台兼容审计.md)；⚠️ **`F-ABI-033` 与 `F-ABI-030` 的处置本轮已核对**：前者与 `F-GENC-001` 同属 M-F 族、已随 `492ce5f7` 关闭故**从本组剔除**，后者与 `F-CS2B-027` 是同一行、**合并计一条**——详见本节末"归组纪律"注）、`F-CS1-011`、`F-CS1-028`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)）、`F-CS2A-003`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md)）、`F-CONC-002`（[08-…/03](../08-专项审计/03-线程安全与并发审计.md)）、`F-CS3-001`（[06-…/03](../06-CSharp运行时与脚本/03-引擎实现库Library（跨语言ABI契约）.md)）、`F-ABI-004`（[08-…/04](../08-专项审计/04-跨语言ABI与平台兼容审计.md)） | **🔴 本组的前提被改写（核心修正）**：LeanCLR 下 C++→C# **不走本机函数指针**（`FLeanCLRDomain.cpp:834-852` 经 `Bridge_Invoke` 走解释器），未捕获托管异常由解释器转成 `RtErr::ManagedException` + 全局异常槽（`interpreter.cpp:8368-8376`），插件只打一条日志、调用方拿零值 ⇒ **原「托管异常屏障」的 P0 前提不成立**，应改写为"**防静默降级 / 便于诊断**"。修法：给每个 `[UnmanagedCallersOnly]` 入口包统一 `try/catch` → 错误码 + 诊断日志（含托管堆栈/类型名/方法名/程序集版本）；`ArrayBridge.cs:31` 补 `InIndex < 0`；`Utils.GetClassProperties` 把长度检查提前；`AssemblyLoader.LoadFromStream`（`AssemblyLoader.cs:13-28`）加 try/catch。**⚠️ 切回 CoreCLR 即 P0**（已用 net10.0 探针实机复现 `call 0` fail-fast，`try/catch` 无效且 `TerminateProcess` 绕过 UE 崩溃上报） |
| **G8** | **注册名 ↔ 目标符号错配 / 静态常量可写**（启动期断言即可消灭整族） | `F-CS3-002`、`F-CS3-003`、`F-CS3-004`（[06-…/03](../06-CSharp运行时与脚本/03-引擎实现库Library（跨语言ABI契约）.md)）、`F-INT2-007`、`F-INT2-013`（[01-…/11](../01-UnrealCSharp运行时/11-Interop注册-数学值类型与引擎类.md)）、`F-BIND-003`（[01-…/08](../01-UnrealCSharp运行时/08-绑定层公开头文件（模板元编程）.md)） | 让注册宏**同时接受符号与期望名字**（`BINDING_FUNCTION(&FMatrix::SetColumn, "SetColumn")`），编辑器启动时批量断言 `#Symbol == Name` 并汇总成一条 `UE_LOG`；元数据加"参数名数量 == 形参数量"的 `static_assert`/启动断言；`EKeys` 342 项改 `BINDING_READONLY_PROPERTY`（`F-BIND-003` 的 `TPropertyBuilder.inl:106` `const_cast` 同源）。<br>⚠️ **`F-BIND-003` 与 `F-INT2-013`(11) 同病根，勿重复计**。⚠️ 真 bug 是 `FRotator.Equals` 实际绑到 `FRotator::IsNearlyZero`（`Rotator.cs:196` 只有 1 参重载）与 `SetComponentForAxis` 逐字同形；多数"错配"**功能未丢失**（C# 可见名 = 注册名字符串，`FBindingClassGenerator.cpp:458`） |
| **G9** | **Interop 导出层不判空（P1 部分）** | `F-INT2-003`、`F-INT2-012`、`F-INT2-013`（[01-…/10b](../01-UnrealCSharp运行时/10b-Interop注册-容器字符串与对象指针.md)）、`F-INT2-002`、`F-INT2-008`、`F-INT2-009`、`F-INT2-011`、`F-INT2-014`（[01-…/11](../01-UnrealCSharp运行时/11-Interop注册-数学值类型与引擎类.md)）、`F-HLP-006`、`F-HLP-007`、`F-HLP-016`、`F-HLP-024`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)）、`F-BR-003`（[02-…/06](../02-UnrealCSharpCore核心/06-类型桥接编码设置与引擎监听.md)）、`F-FN-003`、`F-FN-004`（[01-…/06](../01-UnrealCSharp运行时/06-函数与类描述符.md)）、`F-DEL-001`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)，**不可达**）、`F-CS2A-011`、`F-CS2A-014`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md)） | 与 P0-3/P0-4 **同一套 `Checked()` 辅助 + 机械替换**（判空族的补齐面）。<br>⚠️ `F-HLP-016`/`F-HLP-024` 是**另一形态**：构造函数在属性为空时**不初始化 `ScriptMapLayout`/`ScriptSetLayout`**，而所有方法都用它算偏移 → **用未初始化数据做指针算术**，须单独修（初始化或抛错）。<br>⚠️ `F-INT2-016`(11) 的"防御性判空写成 `&In != nullptr`"（对引用恒为真、被优化掉）**必须一并清除**，否则判空覆盖率永远无法确认 |
| **G10** | **容器 Helper 绕过引擎析构与哈希** | `F-HLP-020`、`F-HLP-025`、`F-HLP-024`、`F-HLP-016`、`F-HLP-001`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)）、`F-CS2A-012`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md)）、`F-PERF-001`（[08-…/05](../08-专项审计/05-性能热点与优化清单.md)，与前者同一处、**2 份报告独立命中**） | 引擎 `FScriptMapHelper::EmptyValues:4710` / `FScriptSetHelper::EmptyElements:5541` 在 `Map->Empty` **之前**先 `DestructItems`（已确认为引擎契约）；`delete ScriptArray/Map/Set` 因 `TScriptMap`/`TScriptSet` 无析构只 free 外壳、`Elements`/`Hash` **连 POD 也漏**。<br>🔴 **API 名称防错（已更正）**：**`FindPair` 不存在**（`FindMapPairPtrWithKey:4921` 是线性版本，按错名字改会踩坑）；**真正的哈希入口**是 `FScriptMapHelper::FindValueFromHash`（`UnrealType.h:4952-4963`）/ `FScriptSetHelper::FindElementIndexFromHash`（`:5728-5737`）；**勿把 `FindInternalIndex` 当 O(1) 键查找引用** —— 它是"逻辑序号 → 内部槽位"翻译函数，引擎注释 `UnrealType.h:4792` 自述 *"This is slow, do not use this for iteration!"* |
| **G11** | **属性描述符 `Set` 语义：`InitializeValue` 用在活值上 / 委托 `Set` 既非赋值也不解绑 / 稀疏委托只读路径写假委托** | `F-PROP2-002`、`F-PROP2-003`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)）、`F-PROP2-005`、`F-PROP2-007`、`F-PROP2-011`、`F-PROP2-013`（[01-…/05](../01-UnrealCSharp运行时/05-属性描述符-对象与委托类型.md)）、`F-PROP-007`（[01-…/04](../01-UnrealCSharp运行时/04-属性描述符-基类与基本类型.md)，**待裁定，见 §5**） | **只改 §1.1 的 4 个"活值 `Dest`"点**（另 4 点明确不要改）；`FStringRegistry.inl:67-72` 补 `DestroyValue`（该文件全文无 `DestroyValue`/`~FString()`）；委托属性 `Set` 改为"赋值语义"（先 `Unbind` 旧绑定再写，而不是 `InitializeValue` 清空 + `Add` 追加）；`FInterfacePropertyDescriptor::Identical` 不得把"局部指针变量的地址"当作 `FScriptInterface*` 传给引擎 |
| **G12** | **编辑器对象 / 委托配对不齐备（含跨语言侧同型）** | `F-ED1-001`（[04-…/01](../04-编辑器模块/01-内容浏览器与新建类向导.md)）、`F-ED2-003`、`F-ED2-019`（[04-…/02](../04-编辑器模块/02-工具栏监听器进度对话框与模块入口.md)）、`F-LIFE-006`、`F-LIFE-007`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)）、`F-CS2B-017`、`F-CS2B-018`、`F-CS2B-019`（[06-…/02b](../06-CSharp运行时与脚本/02b-对象字符串与工具类型包装.md)）、`F-DEL-004`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)） | 与 **P0-8 同一套修法**（句柄存入容器 + `ShutdownModule` 统一 `RemoveAll`）：`ClassCollector.cpp:131,142` 的 `OnEndFrame` 反复覆盖同一句柄 → 改存容器；`FEditorListener.cpp:89-97` 目录监视句柄在 `for` 循环里被覆盖 → **逐目录保存**；`InputComponent.cs:235-394` 六个 `Remove*` 补原生解绑（**目前删了 `UFunction` 却不解绑 → 悬垂函数调用 + C# 委托永久泄漏**）；`Function->AddToRoot()`（`F-CS2B-018`）补配对 `RemoveFromRoot`；`F-CS2B-019` 改键（`Action.Method.Name` + 按 `UClass` 取绑定对象 → 同类多实例同名方法被静默丢弃/误解绑）。<br>⚠️ **多播齐备、单播不齐备**：`F-DEL-004` 与"委托解绑路径 ✅ 齐备"并不矛盾，两句话必须并读 |
| **G13** | **生成/编译期静默错误吞没 + 生成不确定** | `F-GEN-003`、`F-GEN-002`（[03-…/01b](../03-代码生成器/01b-类生成器与绑定类生成器.md)）、`F-GEN3-003`（[03-…/03](../03-代码生成器/03-代码分析Doxygen转换与解决方案生成.md)）、`F-CS4-001`、`F-CS4-002`、`F-CS4-003`（[06-…/04](../06-CSharp运行时与脚本/04-Dynamic特性源生成器与模板工程.md)）、`F-DYN-005`、`F-DYN-016`（[02-…/03](../02-UnrealCSharpCore核心/03-动态类型生成.md)；⚠️ **`F-FL-002`（[02-…/07](../02-UnrealCSharpCore核心/07-函数库宏与模板Trait.md)）与 `F-GENC-001` 同属 M-F 族、已随 `492ce5f7` 关闭故从本组剔除**）、`F-CS1-020`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)）、`F-ABI-005`（[08-…/04](../08-专项审计/04-跨语言ABI与平台兼容审计.md)）、`F-LEAK-007`（[08-…/02](../08-专项审计/02-资源泄漏审计（内存句柄与容器）.md)）、`F-PERF-002`（[08-…/05](../08-专项审计/05-性能热点与优化清单.md)） | `SaveStringToFile` 的 `bool` 在 **15 个调用点有 14 个被丢弃**（本切片唯一"确认且级别未变"的 P1）；引入统一 `EscapeCSharpStringLiteral`/`EscapeXml` 并**在生成器出口强制经过它**；`FText` 默认值是 C++ 表达式（`KismetSystemLibrary.h:562` 的 `INVTEXT("Hello")`）→ 产物 `new FText("INVTEXT(\"Hello\")")`，`PrintText` 打印错误内容、**编译 0 警告**；143/255 个特性类无可传参构造、`Value` 从不被读取 → `ExposeOnSpawn`/`CallInEditor`/`Latent*` 失效；`using` 顺序依赖 `TSet` 遍历顺序（抽 400 份产物 **331 份非字典序**）→ 排序；`EndGenerator` 的清理改为"只删自己写的文件"。<br>⚠️ **`F-CS1-020` / `F-ABI-005` / `F-PERF-002` / `F-LEAK-007` 是同一处**（`TypeBridge.MakeGenericType2` 用 `InKeyType` 两次代替 `InValueType`，`TMap<K,V>` 退化成 `Dictionary<K,K>`），被 **4 份报告从 4 个视角独立命中 → 同族，合并计数** |
| **G14** | **C# 侧资源所有权与 API 契约缺失** | `F-CS2A-005`、`F-CS2A-006`、`F-CS2A-015`、`F-CS2A-019`、`F-CS2A-023`（[06-…/02a](../06-CSharp运行时与脚本/02a-容器与指针包装（资源所有权）.md)）、`F-CS2A-003`（同） | 容器类型只实现 finalizer、不实现 `IDisposable` ⇒ 用户**无法确定性释放** native 缓冲；无 `IsDisposed` 状态 ⇒ 释放后继续调用成员是**静默 use-after-free**；`implicit operator` 每次隐式转换都新建对象并注册 native 指针；`stackalloc byte[sizeof(T)]` 的长度来自托管 `sizeof(T)`，native 侧按 UE 反射尺寸写入，**二者无任何校验**（`F-CS2A-003`） |
| **G15** | **平台/后端门控与环境信号处理** | `F-ABI-009`（[08-…/04](../08-专项审计/04-跨语言ABI与平台兼容审计.md)）、`F-ABI-011`（同） | `FCSharpEnvironment` 用全局 `signal()` 覆盖 `SIGSEGV`/`SIGFPE`/`SIGILL`，处理函数里调用**非 async-signal-safe** 的 `UE_LOG` 与托管运行时；`SIGSEGV` 处理函数直接 `return` 造成无限故障循环；Windows/Linux 分支比 Mac 分支少做"保存并恢复原处理器"，且 `Deinitialize` **从不恢复** ⇒ 改为"可选、可恢复、异步信号安全"（处理函数内只 `write()` 到预分配缓冲 + `_exit()`）。<br>`F-ABI-011`：Linux 编辑器缺 `#else` 分支 ⇒ `GetDotNetPathArray()` 函数体为空（非 `void` 无 `return`）、`GetDotNet()` 返回 **macOS 路径** —— **潜伏**（当前五平台全 LeanCLR，切回 Mono/CoreCLR 即生效） |

> ⚠️ **归组纪律（本轮复核新增：同一编号只归主组，避免一组修两次）**：下表是"族与代表条目"，但复核实测有 **13 个编号同时出现在两组** —— 按"一编号一主组"归位后，另一组里的同一编号应读作**前置/顺带**，不得重复计：`F-DYN-016`（**主 G13**；G4 顺带）、`F-LIFE-006`（**主 G4**；G12 顺带）、`F-PROP2-002`(04b)（**主 G11**；G3 顺带）、`F-PROP2-003`(04b)（**主 G3**；G11 顺带）、`F-HLP-001`（**主 G3**；G10 顺带）、`F-HLP-020`、`F-HLP-025`（**主 G10**；G3 顺带）、`F-HLP-016`、`F-HLP-024`（**主 G9**；G10 顺带）、`F-CS2A-003`（**主 G7**；G14 顺带）、`F-CS2A-023`（**主 G1**；G14 顺带）、`F-CS2A-005`、`F-CS2A-019`（**主 G14**；G1 顺带）。
> ⚠️ **2 个"假重复"（重号，不合并）**：`F-INT2-011`（G1 指 `(10b)`、G9 指 `(11)`）、`F-INT2-013`（G8 指 `(11)`、G9 指 `(10b)`）—— 同一编号在两份报告里是**不同发现**。
> ⚠️ **组内重复一对，合并计一次**：`F-CS2B-027`（[06-…/02b](../06-CSharp运行时与脚本/02b-对象字符串与工具类型包装.md)）与 `F-ABI-030`（[08-…/04](../08-专项审计/04-跨语言ABI与平台兼容审计.md)）是**同一行同一缺陷** —— 同为 `Script/UE/CoreUObject/Utils.cs:309-317` 的 `GetClassPropertiesImplementation`，`:313` 的 `Segments[1]` 先于 `:314` 的长度判断求值（按 §1.4 规则 2 合并；合并后按 P1 计）。
> ⚠️ **2 个编号已由 P0-11 关闭、已从组内剔除**：`F-ABI-033`（原 G7）、`F-FL-002`（原 G13）—— 与 `F-GENC-001` 同属 §1.4 的 **M-F**，已随 `492ce5f7` 修复（实测 `Deserialize` 调用点 4 处全部加守卫、覆盖 6 处 `->Values`），总表两行已同步标 **已处置**。
> **本节的"降级 ≠ 不修"提醒**：G7 里 `F-CS1-003`/`F-CS3-001`/`F-ABI-002`/`F-ABI-004`（`MethodBridge.cs:140` 未命中返回 0 → `call 0`）**在本工程当前配置下是"潜伏"**（调用点在生成模板的 `#else` 分支，`Script/Shared.props:12` 已定义 `WITH_LEANCLR`）—— 但它们是**同一个 `nint.Zero` 被 4 份报告各写一次**的重复族，**切回 CoreCLR 后端立即成为 P0**，且 `F-ABI-004`（绑定函数指针只按**方法名字符串**匹配、不校验参数个数/类型）在**任何后端下都成立**。
> 🔴 **上条括号内的两条依据经本轮实测均不成立（2026-09-16 更正）**：① `Script/Shared.props:12` **没有** `WITH_LEANCLR`（它只有 `UE_5_x_OR_LATER`，连 `WITH_EDITOR` 都没有 —— 该文件是"目标已存在即不覆盖"的早期残留，见 §2.1 的构建模型提醒）；② 该 `#else` 分支**当前正在参与编译**（`Content/Script/UE.dll` 实测无 `DllImportAttribute`、引用 `MethodBridge.GetMethod`）。⇒ `F-CS1-003`/`F-CS3-001`/`F-ABI-002` **当前是活跃缺陷**而非潜伏。本轮的处置见 **§2.1**（已改并验证）。原文保留以留痕。
> **另一个活跃路径上的例外**：`F-CS2B-027`（`Utils.cs:313` 在检查 `Segments.Length` 之前索引 `Segments[1]`）**是 LeanCLR 活跃路径** —— `Split(' ')` 无分隔符返回**长度 1** 的数组（`String.Manipulation.cs:1723-1727`）⇒ 必抛 `IndexOutOfRangeException`；现实触发是**含换行的 Attribute 值**（weaver 不转义 + `\n` 连行），后果是**该类全部属性对 UE 消失**。
> 🔴 **更正（2026-09-16）**：`Utils.cs:313` 所在区块是 `#if WITH_LEANCLR`（`:293`），而当前配置不是 LeanCLR ⇒ 该行**当前不参与编译**（"必抛"的结论只在 LeanCLR 下成立）。缺陷本体与后果描述（异常 → `Unhandled_Exception` → 该类属性元数据静默缺失）**不变**，只是可达性由"活跃"改为"**切回 LeanCLR 即活跃**"。本轮已按并集一并修复，见 **§2.1**。
> ✅ **`01-…/07` 重判轮引起的级别变动（2026-09-17，G-7 关闭）**：本表 **G9** 行里的 `F-HLP-016`、`F-HLP-024` 已由 **P1 降为 P2 / 潜伏**（`F-HLP-017` 维持 P2、活跃→潜伏）；**G10** 行里的 `F-HLP-020`、`F-HLP-025` **维持 P1**，且**三条「待重判」（含 `F-HLP-005`）全部关闭**。⇒ 引用 G9/G10 的代表条目与"判空族的补齐面"时按此口径；逐条依据见 [`01-…/07` §10](../01-UnrealCSharp运行时/07-容器Helper.md)。

---

### 2.1 G7「活跃路径子集」处置记录（2026-09-16，**已压缩提交为 `1551383b`**）

> 本节是本轮新增的**逐条处置登记**（口径同 §1.2：以本表为准）。G7 的活跃路径子集 = **8 处代码点 / 14 个编号**（`F-CS2B-027`、`F-CS2B-009`、`F-CS2B-010`、`F-ABI-001`、`F-ABI-030`、`F-ABI-034`、`F-CS1-011`、`F-CS1-028`、`F-CS2A-003`、`F-CONC-002`、`F-CS3-001`、`F-ABI-004`、`F-CS1-003`、`F-ABI-002`）—— **10 个编号已修、4 个仅部分处置**（`F-CS2B-010`、`F-CS1-003`、`F-CS3-001`、`F-ABI-002`），全部通过**两端编译验证**。
>
> 🔴 **先读这条口径更正**：本文件 §2 的 G7 行与本节末注写"潜伏/活跃"时，前提是"**当前配置 = LeanCLR**"。经本轮实测，**该前提不成立** —— 见 [`00-总览/03`](../00-总览/03-修复优先级评估.md) §1.3 的**事实更正**：当前 `Config/DefaultUnrealCSharpSetting.ini` = Windows/Linux/Mac **CoreCLR**、Android/iOS **Mono**，**没有任何平台是 LeanCLR**。因此：
> - `F-CS2B-027`/`F-ABI-030`（`Utils.cs` 的 `#if WITH_LEANCLR` 区块）**当前不参与编译**（切回 LeanCLR 才生效）；
> - `F-CS1-003`/`F-CS3-001`/`F-ABI-002`（`MethodBridge.GetMethod` 未命中返回 0 → 生成代码 `call 0`）**当前已参编且活跃**（§2 末尾注把它们读作"潜伏"是 LeanCLR 前提下的结论）；
> - 且 `F-ABI-004`（只按方法名字符串匹配）**在任何后端下都成立**。
>
> 本轮按**两后端并集**施工（两个方向都改），避免下次切后端时返工。
>
> 📌 **提交状态（2026-09-17 复核更新）**：维护者已把本轮**全部 22 个文件**压缩为**一次提交** **`1551383ba3aa8bfea564ea5844c15c7ea642b660` "FRegisterArray BufferSize && MethodBridge ParamCount"**（2026-09-17 01:47:30 +0800，**22 个文件 +288/−105**，父提交 `2113e1e9`；`git show --stat` 清单 = C# 9 个 + C++ 13 个）。⚠️ **分散提交已压缩**：该组原先是 `cbf55bf6` / `71154011` / `09797144` 三次（提交信息均为 `t`），现已合入这一次 —— **本文件与 README 一律只引用 `1551383b`，不再出现那三次的哈希**。**复核结论**：`git diff 1551383b 09797144` 为空 ⇒ 压缩提交与三次分散提交的最终树**逐字等价**；对提交内**新增行**扫描：注释 0 / `throw` 0 / 日志（`Console.Error.WriteLine`、`UE_LOG`）0 / try-catch 0 / `static_assert` 0，符合本轮口径。另：新模板 `TGetFunctionParamCount.inl` 的 include 落在**直接使用方 `FClassBuilder.inl:3`**（提交时从 `Macro/BindingMacro.h` 挪来；与 `TPropertyValue.inl:9` 引 `TGetArrayLength.inl` 同一种写法），本节按改动后的落点记述。**提交后复跑**（2026-09-17 01:53，CoreCLR = 当前 ini 配置、产物与提交同源）：`UnrealCSharp-CoreCLR-Windows-Development-2026-9-17-1-53-42-668.csv` **1700 例 / 0 失败**，两条间歇用例均为 `true`。
> ⏱ **复测待办（不构成"已修"依据）**：运行期用例已跑通 —— CoreCLR `…-2026-9-16-20-10-44-838.csv`、`…-20-18-15-312.csv`、`…-2026-9-17-1-53-42-668.csv` 与 LeanCLR `…-2026-9-17-0-44-40-521.csv` 均 **1700 例 / 0 失败**，用例集与改动前基线（`…-2026-9-16-12-31-48-936.csv`）逐一相同。**但** 2026-09-17 01:00 的一对 LeanCLR CSV（间隔 4 秒、同会话两次运行）中，前一个为 1700/0、后一个为 **1698 通过 / 2 失败**，失败项为 `RawMethodHelperOutGetObjectFunction` 与 `RawMethodHelperOutGetClassFunction`（断言"native 传入 `nullptr` 的 out 参数在托管侧应为 null"）。磁盘上含该两例的 121 个 CSV、53 对同会话 CSV 中**仅此一对不一致**（改动前的 CoreCLR/LeanCLR 历史运行全绿），**未定位、未复现**，倾向测试面/时序因素，留待多轮复测判定。
>
> ✅ **复测结论（2026-09-17，带运行期证据；原文保留以留痕）**：**已定位，且不是"测试面/时序因素"** —— `nullptr` 实参在 C++ 侧经 `FCSharpBind::Bind(UObject*)`（`FCSharpBind.cpp:42-50`）查询**以 `TWeakObjectPtr<const UObject>` 为键**的 `Object2ManagedHandle`（`FObjectRegistry.h:16`/`:49`）时，**命中"对象已销毁、条目未注销"的残留条目**并返回一个**有效句柄**；托管侧 `HandleData` 用强 `GCHandle` 持有包装，于是 C# 的 `ref UObject`/`ref UClass` 入参成为**已销毁对象的存活包装**（非 null）。"同会话第二轮才失败"由此解释：PIE 停止会拆掉整个世界、套件自身也大量 spawn/destroy，残留条目在第二轮才有机会被 `null` 查询命中。**与后端无关**：同一探针 **CoreCLR 6/6、LeanCLR 24/24** 命中（不带强制销毁的自然命中 LeanCLR 5/36 轮）—— 原观测只是恰好落在 LeanCLR 会话，**不要**记成 LeanCLR 专属。**修法一行**：`Bind(UObject*)` 与 `FObjectRegistry::GetObject/GetManagedHandle(const UObject*)` 在 `Find` **之前**判空即回 `InvalidManagedHandle`（下游 `FCSharpBind.inl:58-61` 本来就有同形判空 ⇒ 缺陷只是"判空发生在查表之后"）。✅ **该修法已施工并双后端验证、且已提交（压缩提交 `cd0dfacb`）**：`Bind(UObject*)` + `FObjectRegistry::GetObject/GetManagedHandle` 三处判空（2 文件 +25/−8）、`FObjectRegistry::RemoveReference(const UObject*)` 同族判空（1 文件 +9/−6）。形态一律**正向 `if` 包裹 + 末尾默认返回**（项目风格，不用提前 `return`）、无注释/日志/异常。同一强探针下 **CoreCLR 6/6、LeanCLR 9/9 由"全部命中"变为"全部干净"**，最终形态（正向包裹）另复验 CoreCLR **6/6 干净**；探针移除并还原配置后共 8 轮 1700 例 / 0 失败、与基线逐名一致（压缩提交后另跑 1 轮 1700/0）。**仍开放**：`Object2ManagedHandle` 中**已销毁对象的条目不会清理**（`NotifyUObjectDeleted` 对 `UStruct`/`UClass` 走 `RemoveClassDescriptor` 分支、不调 `RemoveObjectReference`）。逐项证据、复现装置、两份样本与验证口径见 [`08-…/06`](../08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md)（新编号 **`F-REG-019`**）。

| # | 代码点（插件内相对路径） | 成员编号 | 本轮改法 |
|---|---|---|---|
| 1 | `Script/UE/CoreUObject/Utils.cs:313-314` | `F-CS2B-027` = `F-ABI-030`（同一行，按 §1.4 规则 2 **合并计一条**） | `Segments[1]` **之前**补 `Segments.Length < 2` 短路：`if (Segments.Length < 2 \|\| int.TryParse(Segments[1], …) == false \|\| Segments.Length - 2 != ValueCount)`。`:320` 的 `Segments[0]` 由此安全，`:331` 的 `Segments[ValueIndex + 2]` 仍由 `Length - 2 == ValueCount` 保证 |
| 2 | `Script/UE/CoreUObject/SynchronizationContext.cs` | `F-ABI-034`、`F-CONC-002`、`F-CS2B-009`、`F-CS2B-010` | `Send` 的跨线程分支在入队后补 `ResetEvent.WaitOne()` —— `using var` 的作用域随之覆盖等待，句柄活到游戏线程 `Set()` 之后 ⇒ **同步语义实现**、"句柄在使用前被 Dispose"的机制消亡。⚠️ **同代码点的 `F-CS2B-010` 与 `Tick` 兜底本轮未修**：`Process` 的逐任务隔离、`Tick` 的最外层兜底、`TaskInfo.Invoke` 的 `finally` 防替换**都要靠 try/catch**，按本轮口径不做 ⇒ `Process`/`Tick`/`TaskInfo.cs` 保持原样（`TaskInfo.cs` 与 HEAD 逐字一致） |
| 3 | `Script/Weavers/UnrealTypeWeaver.cs`（**并改工程部署副本** `Script/Weavers/UnrealTypeWeaver.cs`，见下方⚠️） | `F-CS1-028` | `GetTypeSize` 返回类型 `sbyte` → `int`、`BufferSize` 局部变量同样改 `int`；新增 `GetLdcI4(int)`（值在 int8 范围内发 `Ldc_I4_S`，否则发 `Ldc_I4`），**6 处**发射点（4 处属性 + 2 处 RPC）全部改用它 ⇒ 参数总尺寸 ≥ 128 字节不再回绕成负立即数（原 `(sbyte)` 求和 + `Ldc_I4_S` 必然溢出） |
| 4 | `Script/Interop/Bridge/TypeBridge.cs:100-171` | `F-ABI-001` | `GetNamespace`/`GetName`/`GetFullName` 三处改为按 **UTF-8 字节数**取字符前缀：三个方法**保持原文形态不变**，只把 `Math.Min(××.Length, InStringSize - 1)` 这一个实参换成新增的 `GetUTF8CharCount(××, InStringSize - 1)`（逐字符算 1/2/3/4 字节、代理对按整对计入、容量不足即停在**字符边界**；helper 与文件其它私有 helper 一起放在文件末尾，非 `unsafe`），不再拿 UTF-16 字符数当字节数上界；截断到 `InStringSize - 1` 时返回 `InStringSize - 1`，与 `TGetUtf8String.inl` 既有的"翻倍重试"协议**兼容**（无需改 C++）。**未加 try/catch**（越界异常已由机制消除） |
| 5 | `Script/Interop/Bridge/FieldBridge.cs` | `F-CS1-011` | 槽位语义按 `IScriptTypes.h:79-81` 已声明的 `IManagedHandle` 对齐：**引用类型字段**走 `HandleData.GetObject(InValue)` / `HandleData.Alloc(值)`（不再把装箱 `long` 当字段值、也不对任意对象 `Convert.ToInt64`）；值类型补 `bool`/`nint`/`nuint` 分支（`bool` 必须单列 —— `Convert.ChangeType(long, bool)` 会抛）。**格式与命名均回到修改前**：`switch` 表达式形态、`if (Field != null)`、**保留原签名 `long` 与原命名 `LongToField`/`FieldToLong`**（该槽位就是 `IScriptTypes.h:79-81` 的 `IManagedHandle` = int64，ABI 不变；改 `nint` 只是无谓改动）—— 因此该文件与 HEAD 的差异仅 3 行（两个表达式内部）。**未加 try/catch**；字段查找未命中仍返回 `null`/0 |
| 6 | `IScriptTypes.h:83`＋`:166`、`FScriptDomainImpl.inl:600-634`、`FBindingMethod.h`、`FBindingMethodRegister.h`、`FBindingClassRegister.h/.cpp`、`FClassBuilder.h/.inl/.cpp`、**新增 `Template/TGetFunctionParamCount.inl`**（`Macro/BindingMacro.h` 引入）、`MethodBridge.cs`、`UnrealTypeSourceGenerator.cs`、`FLeanCLRDomain.h/.cpp` | `F-ABI-004` | **注册 ABI 扩一列形参个数**：`method_bridge_register_binding_fn` = `(names, methods, paramCounts, length)`；个数由新增的 `static constexpr` 重载 `TGetFunctionParamCount()` 在**编译期**从函数指针类型取出（`Result (*)(Args...)` 重载 + 泛型兜底 `-1`；取不到即跳过核对，如类型已被擦成 `void*` 的属性访问器）。C# 侧 `RegisterBinding` 存 `(指针, 个数)`，`GetMethod(ref slot, name, paramCount)` 核对；生成器把 `method.Parameters.Length` 传下去。**LeanCLR 侧另有一处**：`FLeanCLRDomain::RegisterBinding` 存 `PInvokeParamCounts`（原生地址 → 个数），`PInvoke_Classify` 在按 C# 声明原型调用**之前**比对 `InManagedMethod->parameter_count`，不一致即返回 `RtErr::ExecutionEngine`、不按错误原型调用（`PInvoke_Dispatch` 是按 C# 元数据的 `parameter_count` 构造函数原型去调的 ⇒ 不一致时读的是垃圾寄存器/栈槽） |
| 7 | `MethodBridge.GetMethod`（同上文件） | `F-CS1-003` = `F-CS3-001` = `F-ABI-002`（**同一个 `nint.Zero` 被 3 份报告各写一次**） | 增参 `int InParamCount`（**保持原风格**：`GetMethod` 仍是 `if (InSlot == nint.Zero) { InSlot = … ? Method : nint.Zero; }` + `return InSlot;`，只把原三元表达式扩两条 `&&` 条件；`RegisterBinding` 同形扩一参、一判空、一赋值）；未命中 / 个数不符时**静默返回 0**（返回值契约与原文一致）。⚠️ **`call 0` 本身未消除** —— 本条唯一的行为差别是"**个数不符**"从"照错原型调用（读垃圾寄存器/栈槽）"改为"返回 0（fail-closed）"；**可见化（日志/异常）按本轮口径全部撤回** ⇒ 本条**只算部分处置** |
| 8 | `FRegisterArray.cpp` + `Script/UE/Library/TArrayImplementation.cs` + `Script/UE/CoreUObject/TArray.cs` | `F-CS2A-003` | 9 个带缓冲的导出（`Get`/`Set`/`Find`/`FindLast`/`Contains`/`Add`/`AddUnique`/`RemoveSingle`/`Remove`）各补一个 `int32 InBufferSize`；守卫写成**正向 `if (IsBufferLargeEnough(ArrayHelper, InBufferSize)) { … }` 包裹**（与同文件 `Get`/`Set` 同形；**不用负向守卫 + 提前 `return`**，本项目风格），原生侧新增 `IsBufferLargeEnough()`：**先判 `InArrayHelper` 与 `GetInnerPropertyDescriptor()` 非空**（`FPropertyDescriptor::Factory` 以 `nullptr` 表示"不支持"—— 该文件内 `return nullptr` 四处；同型判空先例 `FArrayHelper.cpp:41`），再比对 `GetBufferSize()`，**不足即不读不写**（静默按默认值返回）（`Get` 返回、`Find*` 返回 `INDEX_NONE`、`Contains` 返回 0、`Add*`/`Remove*` 返回 0）；C# 侧 **18 个调用点**传 `sizeof(T)`（值类型）或 `sizeof(nint)`（引用类型） |

> ⚠️ **构建模型提醒（本轮实测，后续施工必读）**：本工程 `Script/` 下有**两份**织入器/生成器源码 —— **插件模板**（`Plugins/UnrealCSharp/Script/{Weavers,SourceGenerator}/…`）与**工程部署副本**（`Script/Weavers/UnrealTypeWeaver.cs`、`Script/SourceGenerator/UnrealTypeSourceGenerator.cs`，占位符已被替换为 `Game`/`UE`）。`Weavers.csproj`/`SourceGenerator.csproj` **只编译工程副本**（没有 `Compile Include` 插件目录），只有 `Interop.csproj`/`UE.csproj` 才显式编译插件目录。⇒ 改 `UnrealTypeWeaver.cs`/`UnrealTypeSourceGenerator.cs` **必须两处同改**，否则"看着改了、构建用的还是旧逻辑"。（本轮首次只改了插件侧，构建报 `CS7036` 才暴露。）

**验证（本轮实做，可复核）**

| 项 | 命令 / 判据 | 结果 |
|---|---|---|
| C# 默认配置（当前 = CoreCLR 分支） | `dotnet build Script/Script.sln` | **0 warning / 0 error** |
| C# LeanCLR 分支 | `dotnet build UE\UE.csproj -p:DefineConstants=…UE_5_6_OR_LATER;WITH_EDITOR;WITH_LEANCLR`（Interop/Weavers 同） | **0 warning / 0 error** |
| C++ 当前配置（CoreCLR） | UBT `UnrealCSharpTestEditor Win64 Development` | **Result: Succeeded**；`Plugins/UnrealCSharp/Intermediate/…/UnrealEditor/Development/UnrealCSharpCore/Definitions.UnrealCSharpCore.h` = `WITH_CORECLR 1 / WITH_LEANCLR 0` |
| C++ **LeanCLR 分支** | 临时改 `WindowsScriptDomainType=LeanCLR` → 同一 UBT 目标 | **Result: Succeeded**；definitions = `WITH_CORECLR 0 / WITH_LEANCLR 1`；产物 DLL 含本轮新增的 **LeanCLR 专属**日志串 `PInvoke signature mismatch`（UTF-16 命中 1）⇒ 该分支**确实参编**（不是空跑）。**验证后已还原 ini 并重构建回 CoreCLR**（两项均已复核） |
| 桥接两侧形参个数 | 脚本逐一比对 `FRegisterArray.cpp` 的 29 个实现 ↔ `TArrayImplementation.cs` 的 29 个声明 | **29/29 一致**（本轮改动的 9 个均为 4 参或 3 参，两侧相同） |
| **工程自带测试套件（实机）** | `UnrealEditor.exe <uproject> -game`（TestMap 的 Level BP 触发 `UTestCoreSubsystem::Test()` → `Saved/UnrealCSharp-*.csv`） | **1700 例 / 0 失败（`UnrealCSharp-CoreCLR-Windows-Development-2026-9-16-20-10-44-838.csv`）**，且用例名集合与修复前基线 `…-12-31-48-936.csv` **逐名一致（仅基线有 0 / 仅新有 0）**。⚠️ **这次实机运行是本轮唯一一次运行期验证，且它抓到过一个真回归**，见下方"未做/代价" |

**未做（直说）**

- **运行期验证未做**：没有启动编辑器、没有构造"签名不一致""缓冲不足""跨线程 Send"的活体用例 —— 本轮只到**编译 + 静态核对**（与既有各轮的"未编译未运行"纪律一致，且本轮至少补上了"编译"这一档）。⚠️ **本条只描述 G7 施工轮本身**；2026-09-17 的复测轮已另建装置（编辑器 + Python 驱动 PIE、一进程 3 轮 PIE）并跑了 89 轮运行期用例，见 [`08-…/06`](../08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md)。
- `F-ABI-004` 只比**个数**、不比**类型**：注册表里没有参数类型元数据，`int`↔`long`（x64 下 ABI 兼容）这类错配仍检不出。
- `TMap`/`TSet` 的同型缓冲协议**未动**（本轮只做 `TArray`；`TMap.cs`/`TSet.cs` 有同构的 `stackalloc` 路径，仍无长度校验）—— 属同族待办。
- `PInvoke_Classify` 的核对只覆盖**经 `FBinding` 注册**的桥接；`LogBridge.LogLeanCLR` 那类手工 `RegisterPInvoke` 项不在表内，读作"未知"跳过。
- `Send` 改为真同步后**新增了"游戏线程不 tick 时挂住"的等待面**（原实现是"入队即返回"，不会挂但也不同步）：本轮只实现同步契约，未加超时/取消（属 `F-CS2B-017` 族与 G6 死锁面）。
- 工程部署副本与插件模板是**手工双改**：没有把 `CopyTemplate` 的"目标已存在即不覆盖"改成可刷新（那是另一个问题，见 §5 的存疑清单）。
- 运行期断言（`ensure`/`check`）**一个都没加**；**新增注释 0、新增 `throw` 0、新增日志 0、新增 `try/catch` 0**（`Console.Error.WriteLine`、`UE_LOG`、`#include "Log/UnrealCSharpLog.h"`、try/catch 一律未引入；曾加过的已在同一轮内撤回）—— 失败一律**静默**收敛到既有返回值（未命中回 0/`null`/`INDEX_NONE`、缓冲不足则不读不写）。**代价（直说）**：`F-CS2B-010`（`Process` 逐任务隔离 / `Tick` 兜底 / `finally` 防替换）与 `F-CS1-003`=`F-CS3-001`=`F-ABI-002` 的 **`call 0`** 因此**仍未修** —— 这两类修复在实现上必须依赖 try/catch 或异常。
- 🔴 **`F-CS2A-003` 的第一版校验写错过口径，被实机测试抓到（必须留痕）**：首版 `IsBufferSizeEnough()`（现名 `IsBufferLargeEnough()`）拿 **`GetTypeSize()`（= `FProperty::GetSize()`）** 当"原生要读写的字节数"，而 compound 描述符（本工程把 `FString`/`FName`/`FText`/`UObject*`/`TSubclassOf`/**所有 UStruct** 都归入 compound）真实只读写 **`GetBufferSize()` = `sizeof(void*)` = 8 字节的句柄**；C# 侧对引用类型元素传的正是 8 字节 ⇒ **每一次 compound 元素的 `Add`/`Get`/`Set`/`Find` 都被静默拒绝**。证据：`Script/Game/.../TestCore/TestCoreSubsystem.cs` 的 C# `TestEqual` 走 `TestResults.Add(new FTestResult{…})`，于是**测试套件的 C# 侧用例整体消失** —— CSV 从 **1700 例掉到 595 例**（1105 = 恰好是 C# 源码里的用例名字面量数；C++ 侧 `Source/.../UnitTest/**` 那 595 例走 C++ `TestEqual`，不受影响）。改用 `GetBufferSize()` 后实测恢复 **1700 例 / 0 失败**。⚠️ **留给后人的口径**：本条的"原生按 UE 反射尺寸写入"**只对 primitive 元素成立**（primitive 的 `GetBufferSize()` = `ElementSize`）；**compound 元素的缓冲区是句柄**，拿 `GetSize()` 去比一定误判 —— 审计原文（`06-…/02a`）把两者混为一谈，是这处误写的直接来源。

---

### 2.2 「07 泄漏族」收口轮处置记录（2026-09-17，**已提交；压缩提交 `cd0dfacb`**）

> **对象** = §2 里与 G10/G11 相关、且重判后仍**未修且活跃**的 6 条 P1：`F-HLP-001`、`F-HLP-014`、`F-HLP-019`、`F-HLP-020`、`F-HLP-025`、`F-HLP-026`。
> **逐条改动、两处「未照抄报告建议」的理由、边界**见 [`01-…/07` §11](../01-UnrealCSharp运行时/07-容器Helper.md)。改动面 = 插件原生侧 **7 个文件**（压缩提交 `cd0dfacb` 里本轮泄漏族部分计 **+98/−18**；其中**本补丁 +96/−16**，另 2 行是维护者对 `#if/#else` 两分支 `Rehash` 行的**纯缩进**对齐），三个 Helper 的 `.h/.cpp` + `TPropertyValue.inl`，**C# 侧与生成器零改动**。
>
> **验证（可复核）**：① UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**；② 运行期 `Saved\Retest\run-batch.ps1 -Tag P1-leak-fix-final -Runs 1`（当前 ini：Windows = CoreCLR）**1700 例 / 0 失败**，且与基线 `…-2026-9-16-12-31-48-936.csv` **用例名逐名一致**（仅新有 0 / 仅基线有 0）—— 格式修订后已**重新构建并复跑**，归档证据对应最终形态；③ 针对 `F-HLP-014` 的 `DestroyValue`+`Set` 做了**全元素类型审查**（23 处 `Set` 覆写 + 唯一的 `DestroyValue` 空覆写）⇒ 对所有非 POD 元素均安全；④ 新增注释 / `throw` / 日志 / `try`-`catch` / `ensure` **均为 0**，行宽与空白自审通过（详见 `01-…/07` §11.3）。
>
> **未做（不得读作"已验证"）**：① **泄漏收益未量化** —— 套件容器用例只有 `int`/`UObject` 元素，**没有 `TArray<FString>` 这类"元素自身拥有资源"的类型**，故运行期结论**只是"无回归"**（量化仍属 §6 第 15 条）；② **未跑 LeanCLR**（改动全在原生侧、与后端无关，但未复跑）；③ ✅ **已提交（压缩提交 `cd0dfacb`；本轮泄漏族部分 7 文件 **+98/−18**）** ⇒ `09-…/01` 六行已同步为「**已处置（`cd0dfacb` 已修复）**」。⚠️ **提交比本补丁多 2 行**（+96/−16 → +98/−18），是维护者对 `FMapHelper.cpp`/`FSetHelper.cpp` 的 `#if/#else` 两分支 `Rehash` 行做的**纯缩进**对齐，且**发生在本轮运行期回归之后** ⇒ 归档证据（`P1-leak-fix-final`）对应"提交树去掉那 2 行缩进"的状态；两者只差空白，**提交后未重跑**。
>
> **两处偏离报告建议（须并读 §11.2）**：**① 销毁器并列新增第 5 参**，未替换 `bNeedFreeData`（⇒ 10 个非拥有构造点零改动、未传销毁器时行为与改前逐字等价）；**② `Empty` 走手工析构循环**，未用引擎 `EmptyValues`/`EmptyElements` —— 后者会绕过 `FEnumPropertyDescriptor::DestroyValue` 的虚覆写，且用的是施加偏移的 `DestroyValue_InContainer`（`UnrealType.h:981-987`），与插件全线的 `FProperty::DestroyValue`（`:968-974`，直接调 `DestroyValueInternal`）语义不同。

### 2.3 G4「域重载缓存失效」收口轮处置记录（2026-09-22，**已提交 `8c54c598`**）

> **修复提交**：**`8c54c59866712ff5127735acc7a9629efda767fc`**（2026-09-23 12:59:33 +0800，提交标题 `GCHandle Free && GCHandleType.Weak`，父提交 `9deda0af`，**46 文件 +227/−204**）—— **引用一律使用 `8c54c598`**。
> **对象** = G4 的**核心机制**：6 个函数级 `static TArray<FClassReflection*>`（= `F-REFL-002` / `F-DYN-006` / `F-LIFE-010` —— **同一处代码的三次登记**，一次修复动作）。
> **改动面** = 插件原生侧 **2 个文件**：`Source/UnrealCSharpCore/Public/Dynamic/FDynamicGeneratorCore.h`（6 行声明：`static const TArray<FClassReflection*>& GetXxxMetaDataAttributes()` → `static TArray<FClassReflection*> GetXxxMetaDataAttributes()`）+ `…/Private/Dynamic/FDynamicGeneratorCore.cpp`（6 个 getter 去静态化：`static auto& ReflectionRegistry` → `const auto&`、`static TArray<FClassReflection*> X = {…}` → `TArray<FClassReflection*> X = {…}`）。**C# 侧、生成器、消费点零改动** —— `SetFieldMetaData`（`const TArray<FClassReflection*>&` 形参）与 6 处调用点原样可用，by-value 返回值作为临时量绑定 `const&`，C++17 下不产生额外拷贝。**净改动 24 行增 / 24 行删**（补丁留档 `Saved/Retest/fix-G4-metadata-cache.patch`）。
> **为什么选"去掉缓存"而不是报告首推的"generation 计数器"**：① 缺陷判据就是"缓存**永不失效**" —— 去掉缓存即**结构性**消除，不需要任何失效钩子；② 代次方案要给 `FReflectionRegistry` 添成员 + 添公开访问器（本项目口径偏好**零新增公开 API**），且仍依赖"只有 `Deinitialize()` 会删除反射对象"这一不变量；③ 成本可忽略：六个数组每次调用重建（Property **83** 项 / Function **53** 项 / Class 21 项 / Struct 5 / Enum 3 / Interface 3 —— 本轮实测计数），全是**成员指针读取 + 一次小分配**，而调用点在**编辑器侧动态类型生成**路径上，同一调用栈里本来就有最多 83 次哈希查表的 `HasAttribute` 循环与 `SetMetaData` 的字符串/元数据写入。⚠️ **语义零变化**：返回内容与顺序逐字不变（数组字面量一行未改）。
> **验证（可复核）**：① UBT `UnrealCSharpTestEditor Win64 Development` → **Result: Succeeded**；② **两阶段域重建装置**（`Saved/Retest/run-r1-verify.ps1`：PIE#1 → `UnrealCSharp.Editor.SetActive 0` → `SetActive 1` → PIE#2，即"卸载并 `delete` 全部 `FClassReflection` → 重建 → 再生成"）→ 两份 CSV **各 1704 例 / 0 失败**，与基线（`baseline/UnrealCSharp-CoreCLR-Windows-Development-2026-9-17-1-53-42-668.csv`）**仅差已知的 4 条 `CSharpNativeEvent*` 用例**（`onlyInBaseline=0`），`ensure`/`assert` 行 **0**（归档 `Saved/Retest/out/G4-MetaDataCache/`）；③ 标准 `-game` 回归 `run-batch.ps1 -Tag G4-final` → **1704 例 / 0 失败**（归档 `Saved/Retest/out/G4-final/`）；④ 新增注释 / `throw` / 日志 / `try-catch` / `ensure` **均为 0**、无新增公开 API（`git diff` 可复核）。
> 🔴 **未取得（不得读作"已验证"）：修复前"陈旧指针"的运行期对照没拿到。** 本轮为它做了 **3 次**装置尝试 —— `SetActive 0/1`、`Compile`（无源码改动）、`Compile`（改了一个 C# 动态类源码迫使重编），并配**临时指针同一性探针**（`[G4PROBE] <getter> cached=%p live=%p stale=%d`，结论取出后已随本轮一并移除、`git diff` 对 `Source/` 只剩那 2 个文件的去静态化）。实测结果：**460 行探针全部落在"C# 程序集加载时的那一次生成"上**（08:58:29 / 09:03:51），`stale=1` 计数 **0**；三次重载（`SetActive 0`→`1`、两次 `Compile`）**之后都没有再触发生成**。⇒ **本工程当前的动态类型生成只在 C# 程序集加载时发生一次**；要复现"重载后仍用陈旧缓存"，需要一次**确实加载了新程序集并重新注册 C# 类**的重载（本轮 200 s 窗口内未等到；`Compile` 的完成/重载时点也未在日志里留下可判读的标记）。**故本条的运行期证据是"无回归 + 域重建路径跑通"，不是"复现了旧缺陷"**；缺陷本体的"必然性"仍按 [02-…/01](../02-UnrealCSharpCore核心/01-反射注册表FReflectionRegistry.md) 与 [02-…/03](../02-UnrealCSharpCore核心/03-动态类型生成.md) 的静态推演读（含"后果取决于地址复用 ⇒ 多数为**静默元数据丢失**、少数为**写错元数据**"这一更正）。**建议的补证路线**（留给下一轮）：在**真实热重载**（改 C# 源 → 编译 → 新程序集加载）下重放同一探针，或在 `SetFieldMetaData` 内加一次性 `ensure`（报告 §验证方式 的原建议）。
> **本组未动的代表条目（留给后续轮次）**：`F-DYN-003`（三张以 `UClass*` 为键的表从不清理）、`F-DYN-008`（`SetClass` 在无 '.' 路径上什么都不做 ⇒ `PropertyClass` 永久 `nullptr`）、`F-DYN-016`（同名同元数重载撞 `UFunction` 名）、`F-LIFE-006`（四组动态类型静态注册表永不清理 + `SDynamicClassViewer` 的进程级单例）。⚠️ **`F-DYN-010`（依赖图 `NodeArray` 只增不减 + 节点闭包捕获裸 `FClassReflection*`，P2）与本条同主题但本轮未动**：它的修法要先决定 `Reset()` 落在哪条生成入口（全量 `FDynamicGenerator::Generator()` 与文件变更 `Generator(InFileChangeData)` 的语义不同），并需确认"清掉上一轮已完成节点"不会让 `OnCompleted` 回调落空，语义面比本条大。

### 2.4 G1「句柄释放责任归属」首刀轮处置记录（2026-09-22，**已提交 `8c54c598`**）

> **本轮的完整交付物是独立报告**：[`08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md)（释放链条、责任归属表、实测更正、未覆盖清单）。
> **修复提交**：（已提交 `8c54c598`；改动面 = 4 文件 **+5/−4**：`Source/UnrealCSharp/Public/Registry/FMultiRegistry.inl`（补 `FDomain::GCHandle_Free` + 补 `#include "Domain/FDomain.h"`）、`Source/UnrealCSharp/Private/Registry/FOptionalRegistry.cpp`（补 `FDomain::GCHandle_Free`）、`Source/UnrealCSharpCore/Private/Reflection/FPropertyReflection.cpp` 与 `FFieldReflection.cpp`（各删一对 `#if WITH_CORECLR`/`#endif`））。**C# 侧零改动、零新增公开 API、零日志/`ensure`/`try-catch`**。
>
> 🔴 **先说前提更正（本轮最重要的一条，已带装订探针实测）**：G1 原文的"**强句柄 ⇒ 终结器永不执行 ⇒ 全族闭环泄漏**"**不成立**（至少对主力族不成立）。编辑器两阶段装置实测：`TArray_RegisterImplementation` ≥ **276** 次、`TArray_UnRegisterImplementation`（**唯一调用者就是终结器** `TArray.cs:17`）**364** 次、`FDomain::GCHandle_Free` **7962** 次 ⇒ **原生侧释放点会先放掉句柄，对象随即不再被钉住、终结器照常运行**。**更正后的判据**：真缺口 = "**有终结器、但没有任何原生释放点**"。据此，原建议的"全局 `Alloc` 改弱句柄 + `IDisposable`"（D=2 高风险）**本轮决定不做**，施工重心改为"按判据逐条补齐"。⚠️ **不推翻的部分**：`F-CS2A-005`/`F-CS2A-023` 的"**无法确定性释放**（无 `IDisposable`）"依然成立；字符串族是否同形**未单独实测**。
>
> **本轮修的三处**（逐条依据与同族对照见 `08-…/07` §4）：
> | # | 缺口 | 报告登记 | 改法 | 对齐对象 |
> |---|---|---|---|---|
> | **A1** | `FMultiRegistry.inl:54-79` 的 `RemoveReference` 全程无 `GCHandle_Free`（六个指针包装族：`TSubclassOf`/`TWeakObjectPtr`/`TLazyObjectPtr`/`TSoftObjectPtr`/`TSoftClassPtr`/`TScriptInterface`） | `F-REG-007`（[01-…/02](../01-UnrealCSharp运行时/02-对象引用结构体注册表.md)）；`F-CS2A-023` 的"同构性"段亦点名 | 在 `(InRegistry->*ManagedHandle2Value).Remove(...)` 之后补 `FDomain::GCHandle_Free(InManagedHandle);`；文件头补 `#include "Domain/FDomain.h"` | `FStringRegistry.inl:76`、`FContainerRegistry.inl:76`、`FDelegateRegistry.inl:75` |
> | **A2** | `FOptionalRegistry.cpp:55-77` 的 `RemoveReference` 无 `GCHandle_Free`（同文件 `Deinitialize:31` 却有 ⇒ 自相矛盾） | `F-DEL-008`（[01-…/07b](../01-UnrealCSharp运行时/07b-委托Handler与OptionalHelper.md)） | 在 `ManagedHandle2Helper.Remove(...)` 之后补 `FDomain::GCHandle_Free(InManagedHandle);` | 同上 |
> | **A4** | `FPropertyReflection.cpp:22-27`、`FFieldReflection.cpp:15-20` 的 `ScriptDomain->Free` 被 `#if WITH_CORECLR` 包住 | `F-RM-004`（[02-…/02](../02-UnrealCSharpCore核心/02-反射模型（类字段方法参数属性）.md)）、`F-DOM-008`（[02-…/04](../02-UnrealCSharpCore核心/04-脚本域抽象与三大后端.md)） | **删掉两处 `#if WITH_CORECLR`/`#endif`** | `FMethodReflection.cpp:34-42`、`FClassReflection.cpp:506-514`（均无条件）；`Free` 内部本就判 `HandleDataFreeFn != nullptr`（`FScriptDomainImpl.inl:342`） |
>
> **验证**：① UBT `UnrealCSharpTestEditor Win64 Development` **Succeeded**；② 编辑器两阶段域重建（`run-r1-verify.ps1 -Tag G1-final`）两份 CSV **各 1704 例 / 0 失败**、`ensure`/`assert` 0 行（`Saved/Retest/out/G1-final/`）；③ `-game` 回归（`-Tag G1-final-game`）**1704 例 / 0 失败**，与基线仅差已知 4 条 `CSharpNativeEvent*` 用例（`Saved/Retest/out/G1-final-game/`）；④ 本轮**全部临时探针已移除**（`FRegisterArray.cpp`/`FDomain.cpp` 已还原，`git diff` 只剩上述 4 个文件）。
> **未做（不得读作"已验证"）**：① A1/A2 的**运行期收益未量化**（触发者是终结器 → `RemoveXReference`，而本轮装置里没有能与"域卸载 `Deinitialize` 的批量释放"区分开的可观测场景）；② A4 **只在 CoreCLR（当前 ini 配置）下构建与回归**，Mono/LeanCLR 未单独构建；③ `08-…/07` §5 的四条缺口（`F-INT2-011`(10b)、`F-LEAK-002`、`F-ENV-002`、`F-REG2-002`）**一条都没修**。
> **下一刀建议顺序**（按"无原生释放点 + 可验证性"）：~~`F-INT2-011`(10b)~~（✅ 第二轮已修字符串族）、~~`F-ENV-002`~~（✅ 第二轮）、~~`F-LEAK-002`~~（✅ **第三轮已修**，见 §2.4.4）→ **`F-REG2-002`**（唯一剩下的"无 owner 注册"缺口）；另需单独复核 **`F-CS1-001`/`F-DOM-006`**（LeanCLR 下 `HandleData.Clear()` 唯一调用点永不执行 ⇒ 连批量兜底也没有）。

#### 2.4.4 第四刀：`F-LEAK-002` 跨语言调用的 out 句柄（2026-09-22，**已提交 `8c54c598`**）

> **机制**：C# 侧 `MethodBridge.Invoke` 对 `ref`/`out` **引用类型**参数写 `*Param = HandleData.Alloc(Parameter)`（新句柄），而 C++ 侧读回值后**从不释放** —— 返回值路径有（`FDomain::GCHandle_Free(ReturnValue)`），out 路径没有。
> **两处改动**：① `TMethodHelper.inl:145-148`（`GetReferenceValue`：读回后补 `FDomain::GCHandle_Free(InManagedHandles[Index]);`）；② `FManagedFunctionDescriptor.cpp`（out 循环：新增 `const auto InputCompoundManagedHandles = CompoundManagedHandles;` **调用前快照**，`Set` 后**仅当槽位值变化**才 `GCHandle_Free`）。
> **判据（既有先例）**：同文件 `:61-64` 的返回值路径就是 `Set(&ReturnValue, …)` 之后立即 `GCHandle_Free(ReturnValue)` ⇒ "C# 为 out/ref 引用型参数新 `Alloc` 的句柄属本调用所有"。
> ⚠️ **明确不释放**：**影子句柄**（`InShadowManagedHandles[Index]` 来自 `IManagedHandleFromObject`，只是把指针打包成 handle）与**未变化的槽位值**（否则会误释放输入句柄）。
> **实测（临时日志探针，已移除）**：两阶段装置 —— `TMethodHelper` 路径 **命中 62 次**（≈ **每轮 PIE 31 个** out 句柄；修复前每一次都漏"句柄 + GCHandle + 被 root 的托管对象"）；`FManagedFunctionDescriptor` 的 out 路径 **0 命中**（本套件未覆盖 ⇒ 该处**仅编译验证**，不得读作"已验证"）。
> **验证**：UBT **Succeeded**；两阶段域重建 **2 × 1704 例 / 0 失败**；改动面 = 2 文件 +9 行，无新增 API/日志/`ensure`。

#### 2.4.2 第三刀：`F-ENV-002` binding wrapper 泄漏（2026-09-22，**已提交 `8c54c598`**）

> **机制**：`FBindingRegistry.inl` 的两处 `AddReference` 都 `new TBindingAddressWrapper(...)`；而 `RemoveReference`/`Deinitialize` 的 `delete Value.AddressWrapper` 被 `if (Value.bNeedFree)` 挡住 ⇒ `bNeedFree == false` 的注册**每次漏一个 wrapper 堆对象**（表项被删了，内存没回收）。
> **改法（= 报告建议）**：把"是否释放**被指向对象**"下沉进 wrapper —— `FBindingAddressWrapper` 增 `bool bNeedFree`（构造参数下发 `TBindingAddressWrapper`），派生析构改为 `if (bNeedFree) delete static_cast<T*>(Value);`；`FBindingAddress` 的 `bNeedFree` 成员**删除**；两处清理一律 `delete Value.AddressWrapper;`。
> **语义边界（重要）**：`bNeedFree == false` 的语义只是"**不释放 pointee**"（该对象不归本注册所有），**wrapper 自身永远由本注册 `new` 出来、必须删除** —— 这正是原实现把两件事混为一个标志的错误。
> **实测（临时计数探针，已移除）**：两阶段装置每次域卸载打印 —— 第 1 周期 `created=33 deleted=33 nofree=4`；累计 `created=66 deleted=66 nofree=8` ⇒ **修复前每周期固定漏 4 个 wrapper**，修复后**零漏**。
> **验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Succeeded**；两阶段域重建 **2 × 1704 例 / 0 失败**；`ensure`/`assert` **0**。改动面 = 3 文件（`FBindingRegistry.{h,inl,cpp}`），无新增 API/日志/`ensure`。

#### 2.4.1 第二刀：`F-INT2-011`(10b) 字符串族（2026-09-22，**已提交 `8c54c598`**）

> **范围 = 报告清单 12 处中的 5 处**（`FString`/`FName`/`FText`/`FAnsiString`/`FUtf8String` 的 `*_ToStringImplementation`）。**改法**：取值后补 `HandleData.FreeImplementation(Handle);`（`Script/UE/Library/*Implementation.cs` 5 文件）。⚠️ **必须用 `FreeImplementation` 而不是 `Free`** —— `HandleData.Free` 标了 `[UnmanagedCallersOnly]`，托管代码不可调用（这也是 `08-…/02` §7.3 "`HandleData.Free` 在 `Script/` 树 0 命中"的原因）。
> **为什么这 5 处可以无条件补**：原生侧 5 个 `ToStringImplementation` **一律**走 `IScriptDomain::NewString(...)`（`FRegisterString.cpp:42-47`、`FRegisterName.cpp`、`FRegisterText.cpp`、`FRegisterAnsiString.cpp`、`FRegisterUtf8String.cpp` 同形）⇒ 每次都是 **新建** 句柄、`count=1`、**不进任何注册表**、无其它持有者；C# 侧只把 `string` 拷出后即丢弃包装 ⇒ 释放是唯一正确的配对。
> **⚠️ 其余 7 处未动（有意）**：`TOptional_Get`、`TArray_GetCompound`、`TMap_*Compound`×5、`TSet_GetEnumeratorCompound`、`TLazyObjectPtr_Get`、`TSubclassOf_Get` —— 它们返回**包装对象**（句柄随后仍被该包装使用），必须先逐点确认"句柄是否为本调用新建"；报告自己亦写明**"这一修法有一个前提必须确认"**。
> **实测（本轮新增，C++ 侧计数探针，已移除）**：`-game` 套件一次完整运行里 `FString.ToString()` 调用 **1** 次、`FDomain::GCHandle_Free` **283** 次 ⇒ 本套件对该路径**几乎不覆盖**，故**收益未量化**（真实收益在"用户代码大量字符串化"的场景：日志/插值/`Debug.Log($"{FName}")`）。
> **验证**：`dotnet build Script/Game/Game.csproj -c Debug`（含 `UE.csproj`/`Interop.csproj`）**0 warning / 0 error**；`-game` 回归 **1704 例 / 0 失败**（与基线逐名一致）；两阶段域重建亦 **2 × 1704 / 0**；**无新增注释/日志/`try-catch`**，无 API 变更。
> **构建模型要点（本轮踩坑，留给后续轮次）**：① `UE.csproj`/`Interop.csproj` **直接编译插件树**（`Include="..\..\Plugins\UnrealCSharp\Script\{UE,Interop}\**"`），改插件源即可，无需拷贝；② **`Game` 工程引用的是发布产物 `Content/Script/Interop.dll`（二进制引用）** ⇒ 改了 Interop 必须先编 Interop 再编 Game；③ **编辑器在跑时改 C# 会让 `Game.dll` 与 `Interop.dll` 版本错配**（实测：错配导致 25 条 `RawDynamicGet*Property` 失败）；④ 插件自带的"发布过期"判据 `IsScriptPublishOutdated()` **只比对 `Shared.props` 与已发布 DLL 的时间戳**，**不看源码时间戳** ⇒ 改 C# 源**不会**触发自动重编（须用 `GeneratorScriptCode` commandlet 或 `UnrealCSharp.Editor.Compile` / 手工 `dotnet build`）；⑤ 反复构建会留下 **MSBuild 复用节点**占用 `Script/UE/obj/**/UE.dll`，使后续构建静默失败（须 `dotnet build-server shutdown` 或杀节点）。

#### 2.4.6 第六刀：`F-DOM-006` + `F-CS1-001` 批量清理被守卫连带跳过（2026-09-22，**已提交 `8c54c598`**）

> **机制**：`Script/Interop/AssemblyLoader/AssemblyLoader.cs` 的 `Unload()` 把 `HandleData.Clear(); TypeBridge.Clear();` 写在 `if (Context != null)` **之内**；而 **LeanCLR 从不引用 `AssemblyLoaderLoadFromStreamFn`**（`FLeanCLRDomain.cpp` 0 命中）⇒ `Context` 恒 null ⇒ **整个清理段每次卸载都被跳过** ⇒ 句柄表 + `GCHandle` + 注册包装**跨热重载只增不减**（LeanCLR 连 VM 都不卸载）。**它是 §2.4.5 的 `F-REG2-002`「接受域卸载回收」方案的前置**：卸载不清理，"有界"就不成立。
> **改法**：把**两个 `Clear()` 提到守卫之外**（无条件执行），`if (Context != null)` 内只留"ALC 卸载 + GC 等待循环"。
> 🔴 **已在缺陷唯一生效的后端 LeanCLR 上实测**（临时探针，已移除；切后端 = 改 `Config/DefaultUnrealCSharpSetting.ini` 的 `WindowsScriptDomainType` + UBT 重建）：
> | 探针 | CoreCLR | LeanCLR |
> |---|---|---|
> | `Unload()` 里 `Context == null` | `89000000`（非 null） | **`89000001`（恒 null —— 前提成立）** |
> | `HandleData.Clear()` 里的句柄数 | `88000049` | **`88000047`（Clear 真的执行了，每次卸载释放 47 个）** |
>
> ⇒ **修复前**：LeanCLR 下这两个探针只会打印"恒 null"、**不会有 Clear 行**；那 47 个句柄 + 其 `GCHandle` + 注册包装**永久驻留**。
> **回归**：LeanCLR 两阶段 **2 × 1704 / 0**、CoreCLR 两阶段 **2 × 1704 / 0**、`ensure`/`assert` 0；改动面 = **1 个 C# 文件**（`AssemblyLoader.cs`，纯去缩进 + 提前两行），无新增 API/日志/`try-catch`。
> **顺带验证（独立收获）**：切后端后 `Shared.props` 由插件自动重写并重编 C#，两次运行分别产出 `UnrealCSharp-LeanCLR-…csv` 与 `UnrealCSharp-CoreCLR-…csv` 且各自 1704/0 ⇒ **`F-BLD-022` 的修复在真实后端切换下有效**。
> ⚠️ **仍未修**：`F-REG2-002`（§2.4.5 的三选一，需维护者拍板）。

#### 2.4.5 第五刀：范围收窄 + 量化收口（2026-09-22，**无新代码改动**）

> 本轮不新增修复，产出**两条会改变排期的结论**（逐条依据见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.1.1 / §5.1.2）：
>
> **① `F-INT2-011`(10b) 的实际范围 = 5 处，不是 12 处** ⇒ 本条**已全部处置**（字符串族 5 处已在 §2.4.1 修复）。其余 7 处经逐点核实**不是泄漏**：
> | 站点 | 原生侧写法 | 句柄归属 |
> |---|---|---|
> | `TSubclassOf_Get` | `FCSharpEnvironment::Bind(Multi->Get())`（`FRegisterSubclassOf.cpp`） | 对象绑定的**既有**句柄（UObject 销毁时由 `FObjectRegistry` 释放） |
> | `TOptional_Get` | `PropertyDescriptor->Get(Value, &ReturnValue)`（`FRegisterOptional.cpp`） | **返回给用户的包装对象**的句柄 |
> | `TArray_GetCompound`/`TMap_*Compound`×5/`TSet_GetEnumeratorCompound`/`TLazyObjectPtr_Get` | `PropertyDescriptor->Get(Value, RETURN_BUFFER)`（`FRegisterArray.cpp` 等） | 同上 |
>
> **判据**：后 7 处 C# 侧把 `HandleData.GetObject(Handle)` 的结果**直接返回给用户**，该包装此后仍要用 `GetHandle(this)` 调原生 ⇒ **补 `FreeImplementation` 会把包装句柄打成 0、打断后续调用**。
>
> **② `F-REG2-002` 已量化，但修法需维护者拍板**（原文的"自 owner"建议**不释放任何东西**，只补簿记）：
> | 时机（两阶段装置） | 3 参注册 | 5 参注册 | `RemoveReference` 命中 | 域卸载时残留 |
> |---|---|---|---|---|
> | 第 1 次域卸载 | **382** | 44 | 38 | **165/115/108 = 388**（校验 `382+44−38=388` ✅） |
> | 累计 | 764 | 88 | 67 | 168/118/111 |
>
> ⇒ **每域周期约 390 个容器条目驻留到卸载、显式释放率约 9%**。⚠️ **后端差异**：CoreCLR（当前配置）下卸载会回收 ⇒ **有界**；**LeanCLR 下 `HandleData.Clear()` 因 `Context` 恒 null 永不执行**（`F-DOM-006`）⇒ **永久泄漏**。
> **需维护者三选一**：(a) `HandleData.Alloc` 改**弱句柄**（D=2，波及全部跨语言对象生存期）；(b) 给容器/委托包装实现 **`IDisposable`**（新增公开 API，与既有"零新增公开 API"口径冲突）；(c) **接受"域卸载时回收"** 并先修好 `F-DOM-006` 以确保卸载确实执行。

#### 2.4.7 第七刀：`F-REG2-002` 的解决路径（2026-09-22，**原型已实现并验证，已提交 `8c54c598`**）

> **先量化"谁在注册"**（这是选对修法的前提）：3 参注册 **382 次/域周期** 中 —— **C# ctor（`new TArray/TMap/TSet`）309（81%）** / 属性读视图（`NewWeakRef`）43（11%）/ 编组（`TPropertyValue`）30（8%）。⇒ **主因是"用户 `new` 出来的包装被强句柄钉死"**，而不是"临时视图"。
> **解法（三层）**：**L1** `HandleData.Alloc(obj, bPinned, **bWeak**)` 按标志选 `GCHandleType`（默认仍强 ⇒ 其余句柄语义不变）；**L2** 5 个 C# ctor 的**包装**句柄改弱（`Type` 句柄保持强）；**L3** 端到端加 `bIsWeak`：`Op(ObjectBridgeNewObject, …, 1→2)` + 函数指针 typedef + `IScriptDomain::NewObject(handle, bIsWeak=false)`（三后端头同步，均走共享实现）+ `FClassReflection::NewObject(bool)` + C# `ObjectBridge.NewObject(nint,int)` + 5 个描述符 `NewWeakRef` 传 `true`。
> **明确不动**：5 参（带 owner + 地址映射）注册保持**强**句柄 —— 那些句柄会被 `GetObject(address)` 反查、寿命绑定 owner。
> **实测（编辑器两阶段；临时计数器，已移除）**：显式释放 **38 → 137（原型①）→ 162（原型②）→ 180（原型③，×4.7）**；域卸载时残留 **388 → 289 → 264 → 246（−37%）**；每轮均 **2 × 1704 / 0 无回归**。
> **✅ 三后端验证（用户已接受协议面变更后完成）**：**CoreCLR / LeanCLR / Mono 各 2 × 1704 / 0**（分别产出 `UnrealCSharp-{CoreCLR,LeanCLR,Mono}-Windows-…csv`）—— 新的 op 参数个数（2）通过了 LeanCLR 的 `PInvoke_Classify` 参数个数校验；验证后 ini 已还原为 CoreCLR。
> **边界**：① 改变了这 5 族句柄语义（"进程级常驻"→"随 C# 引用回收"），未穷举第三方用法；② **协议面变更**（`ObjectBridge.NewObject` 1→2 参，✅ 已获维护者接受）⇒ 与旧版 DLL/C# 程序集不兼容；③ 仍是"最终回收"而非"确定性释放"（后者仍需 `IDisposable`）；④ **同型缺口新发现**：`TPropertyValue.inl` 另有 **18 处 `NewObject()`** 服务于**其他家族**（`TSubclassOf`/`TWeakObjectPtr`/`TLazyObjectPtr`/`TSoftObjectPtr`/`TScriptInterface`/结构体等），其注册表**多数带地址映射**，**不能照搬本方案**（弱化会让地址映射指向已回收对象），需先定"地址映射失效时如何重建包装"——⇒ ✅ **已在 §2.4.8 测量完毕**（闭环 5 族全中、残留 3184 条/周期；已切成"可安全弱化 1807 条"与"需先定失效语义 1377 条"两个子集）；⑤ −37% 是"PIE 结束立即卸载"的快照下限，**稳态与内存量级未量化**。详见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.1.2

#### 2.4.8 第八刀：同型闭环的**分族量化**（2026-09-22，**无新代码改动，仅测量**）

> **背景**：`F-REG2-002`（§2.4.7）只覆盖容器/委托两族的 **3 参注册**。`08-…/07` §5.1.2 边界 ④ 与 §6.1 都如实留了"其他家族未测量"。本刀补上：把**字符串族（`FName`/`FString`/`FUtf8String`/`FAnsiString`/`FText`）、多引用族（6 个子族）、`FBindingRegistry`、`FStructRegistry`、`FOptionalRegistry`** 全部装表实测（临时 C++ 探针：6 个 `AddReference` / 4 个 `RemoveReference` 站点 + 5 个 `Deinitialize` 逐子表计数；探针已移除）。**结论先行：闭环不只在容器族 —— 这 5 族同样"每周期注册数千、释放十几"，且多出一层"原生堆块"泄漏。**

| 族 | 注册/周期 | 带地址映射 | 自持堆块（`bNeedFree`） | 显式释放 | 卸载残留 |
|---|---|---|---|---|---|
| `FString` | **1220** | 22 | 1198 | 0 | **1220** |
| `FStructRegistry` | **1221** | 1221 | 1199 | 11–12 | **1209–1210** |
| `FName` / `FText` | 115 / 115 | 22 / 22 | 93 / 93 | 0 / 0 | 115 / 115 |
| 多引用族 6 子族合计 | 494 | 71 | 393 | 0 | 494 |
| `FBindingRegistry` | 33 | 33 | 29 | 2 | 31 |
| `FUtf8String` / `FAnsiString` / `FOptionalRegistry` | 0 / 0 / 0 | — | — | — | 0 |
| **合计** | **3198** | **1391** | **3005** | **14（0.44%）** | **3184** |
| *对照：容器/委托族（§2.4.7 修后）* | *382* | *0（3 参无地址映射）* | *0* | *180（47%）* | *246* |

> **三点判读**：① 显式释放率 **0.44%**、残留 **3184** = 容器族的 **12.9 倍**；**11 个子族（字符串族 5 + 多引用族 6 + optional）的 `rel` 全为 0** ⇒ 其终结器解绑通道在整轮里**一次都没跑**（闭环完全成立）；只有 `FStructRegistry`（11–12）与 `FBindingRegistry`（2）非零 —— 这两族**确实有注入式终结器/析构器通道**（`UnrealTypeWeaver.cs:119-161` 给 `[UStruct]` 注入 `Finalize`；binding 走 `TDestructorHelper.inl:18`），但**探针无法区分**这 14 次来自终结器还是 owner 引用析构（`FStructReference.h:14` 同入口）⇒ **未细分**，覆盖面至多 14/1254 ≈ 1%。② **94%（3005/3198）是 `IsNeedFree=true` 的自持堆块** —— `FReturn` 分支先 `CopyValue(...)`（`FMemory::Malloc`）再交注册表（`FunctionMacro.h:128/143` + `TCompoundPropertyDescriptor.inl:33-48`）、编组 `new std::decay_t<T>(*InMember)`（`TPropertyValue.inl:94/147`）、`FRegister*` 自建 `new`、`TConstructorHelper.inl:22/42` 的 `new Class(...)` ⇒ 这 3005 个**原生堆块在域周期内 GC 碰不到、也没人释放**（性质比容器族重一级）。③ 来源是**每次跨语言调用的编组**（不是每对象一次）⇒ **单会话内驻留随调用量近似线性增长**，**跨域重载有界**（两周期数字逐字节相同）。完整表 + 代码路径 + 边界见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.2。
>
> **修法可行性已切片**（**尚未实现**）：**非地址映射 1807 条/周期（注册 1807 / 残留 1807 / 释放 0，含约 1777 个自持堆块）→ 可照搬 §2.4.7 的弱句柄通道**（该子集只能按句柄查、没有 `GetObject(address)` 反查 ⇒ 不会留下指向已回收对象的地址映射）；**地址映射 1391 条/周期（残留 1377，含 struct 全部 1209）→ 不可照搬**，须先定"反查命中已回收包装"的语义（**(a)** 删条目 + 重建包装，需要"句柄是否仍活"的查询并改变 `ReferenceEquals` 语义；**(b)** 继续用强句柄）。
>
> **同刀登记的结构风险（代码面，未触发，不作已证缺陷）**：`FStructPropertyDescriptor::Get(..., FMember)`（`FStructPropertyDescriptor.cpp:10-22`）**不先查地址映射**（字符串/多引用的同名分支会先查），每次 `NewObject()` + `AddStructReference<false>` ⇒ 同一结构体属性反复读会**每次新增句柄条目并覆盖地址映射**（旧句柄从此无人能释放）。本轮 1221 次注册**零覆盖**（`regs == 残留 + rel`）⇒ **需专项用例才能定论**。
>
> **验证**：UBT **Succeeded**；装表运行与去表后的收尾运行**各 2 × 1704 / 0**、`ensure`/`assert` **0**；探针 0 残留（三个曾装表的 `.cpp` 与 HEAD 逐字节一致）。**未做**：任何修法均未实现/未测量、只测 CoreCLR、未量化字节量级与稳态曲线。

#### 2.4.9 第九刀：binding 所有权判据编译期化 + `bNeedFree` 全量核对（2026-09-22，**已提交 `8c54c598`**）

> **起因**：第三刀（§2.4.2）把"是否释放 pointee"从注册表侧的 `FBindingAddress::bNeedFree` 下沉进 wrapper ⇒ `FBindingRegistry.cpp` 的两处清理**不再用该标志判断**，而字符串/多引用/结构体三族的注册表**仍在用**同名标志 ⇒ 逐条核对"其余那些能不能删"。
> **核对结论（静态：声明/写入/读取三点全列；`Script/` 全树 `NeedFree` 0 命中）**：**没有任何一个 `bNeedFree` 是死变量**，可删的只有 binding 那一个。`FStringRegistry::TStringAddress::bNeedFree`（`FStringRegistry.h:19`，读于 `.inl:67` + `.cpp:26/44/63/83/102`）、`FMultiRegistry::TMultiAddress::bNeedFree`（`FMultiRegistry.h:29`，读于 `.inl:67` + `.cpp:26/44/62/80/98/116`）、`FStructRegistry::FStructAddress::bNeedFree`（`FStructRegistry.h:27`，读于 `.cpp:29/126`）**三族都在真实使用**（`false`/`true` 两个取值在成员路径、`FReturn`、编组拷贝、`FRegister*` 推送四条路径上都出现）；容器/委托/Optional 的 5 个（`bNeedFreeData`/`bNeedFreeProperty`/`DelegateHandler::bNeedFree`）**本来就是"判据放在被拥有对象体内"的范式**，与 binding 改完后的形态一致。
> **本刀改动（行为等价，判据从运行期搬到编译期）**：`FBindingAddressWrapper` 删掉 `bNeedFree` 成员与 ctor 第二参数（只剩 `void* Value`，虚析构保留 ⇒ 仍可经基类指针 `delete`）；`template <typename T, auto IsNeedFree> struct TBindingAddressWrapper` 的析构改 `if constexpr (IsNeedFree) { delete static_cast<const T*>(Value); }`；两个注册点改 `new TBindingAddressWrapper<T, IsNeedFree>(InObject)` / `<T, false>(InObject)`。
> ⚠️ **实测踩坑（复用价值）**：原代码靠 **CTAD**（`new TBindingAddressWrapper(InObject, …)`）让 `T` 对常指针实参推导成 `const X`；所有权一旦变成必须显式给出的非类型模板参数，ctor 若仍收 `T*` 就**接不住 `const T*`**（实测 3 处 `C2665`：`<T,false>`/`<FRawTestStruct,false>`/`<FEnhancedInputActionEventBinding,false>`）⇒ ctor 与 `delete` 都改按 `const T*`。
> **收益**：wrapper 少 1 字节、判据零运行期分支；`FBindingRegistry.h` 改动面 23 行 → 14 行。**验证**：UBT **Succeeded** + 两阶段域重建 **2 × 1704 / 0**、`ensure`/`assert` 0（`Saved/Retest/out/G1-env002-tidy`）；⚠️ **未复测**第三刀的 wrapper 计数（探针已移除，按本库口径记作未复测）。
> **未做（已评估、未采纳）**：把 ~16 处 `if (bNeedFree) { FMemory::Free(...); … }` 清理块去重 —— (i) 各族自带 `FreeValue()`（最小风险，16 → 3 份）；(ii) 新增 `TNeedFreeValueWrapper<T> : TValueWrapper<T>` mixin（字符串+多引用合成 1 份，结构体自带 1 份）；(iii) 三族全多态化 ❌（每次注册多一次 `new`，而这三族每周期注册 2500+ 次）。三族的 wrapper 是**按值存 TMap 的普通结构**、pointee 是裸指针，**没有析构钩子可挂判据**，所以不能照搬 binding。详见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.3 ⇒ ✅ **本清单的"(i)/(ii)/(iii) 未做"已被 §2.4.10 取代：最终采用真 CRTP 基类 `TOwnedValue`**（不是这里的 (i)/(ii)）

#### 2.4.10 第十刀：`TOwnedValue<Derived>` —— 所有权/释放抽成 CRTP 基类（2026-09-22，**已提交 `8c54c598`**）

> **对象** = §5.3/§2.4.9 里评估过、当时未做的"16 处清理块去重"。**形态 = 真 CRTP**：基类持标志 + 门禁，派生只实现释放。
> **新增** `Source/UnrealCSharp/Public/Registry/TOwnedValue.inl`：`explicit TOwnedValue(bool InNeedFree)` + `bool bNeedFree` + `void Free() { if (bNeedFree) { static_cast<Derived*>(this)->FreeImplementation(); bNeedFree = false; } }`。
> **三个 wrapper 改为多继承**：`TStringAddress<T> : TValueWrapper<T>, TOwnedValue<TStringAddress<T>>`、`TMultiAddress<T,IsType> : TValueWrapper<T>, TOwnedValue<TMultiAddress<T,IsType>>`、`FStructAddress : FStructAddressBase, TOwnedValue<FStructAddress>`；三者的 `bNeedFree` 字段**删除**，改为实现 3 行钩子 `FreeImplementation()`，且钩子做成 **`private` + `template <typename> friend struct TOwnedValue;`** ⇒ **唯一公开入口 = 带门禁的 `Free()`**（防绕过标志把借用的属性内存 `FMemory::Free` 掉）。
> **15 个调用点**（字符串 6 / 多引用 7 / 结构体 2）由 6–13 行条件块变成一行 `Value.Free();` ⇒ 门禁 **16 份 → 1 份**。
> **命名记录**：`TNeedFreeValue`（`NeedFree` 是动词短语做定语，语序别扭）→ `TOwnerValue`（❌ 英语读作 "owner 的值"；且本库 `Owner` 已固定指**宿主句柄**：`AddReference(InOwner,…)`/`OwnerManagedHandle`/"3 参无 owner vs 5 参带 owner"口径，源码 **332 处**命中）→ 定名 **`TOwnedValue`**（`true`＝值归我 / `false`＝借用）。**方法名分工（维护者定名）**：对外 `Free()`、派生钩子 `FreeImplementation()`（贴合本库 `*Implementation` 惯例：`TStringRegistryImplementation`、`FRegisterString::RegisterImplementation`、C# `*_RegisterImplementation`）；⚠️ 第一版曾反过来（对外 `FreeValue()`、钩子 `Free()`），已改名并重新构建复验。
> **先例与边界**：自引用模板基类本库已有（`FClassCollector : TSharedFromThis<FClassCollector>`、`FUnrealCSharpEditorCommands : TCommands<FUnrealCSharpEditorCommands>`），**但多继承在插件自有类里此前 0 命中**（本刀首次）；⚠️ 更正本条上一刀的说法 —— **CRTP/自引用模板参数其实有先例，只有多继承没有**。
> **改动面**：8 文件 **+66/−116（净 −50 行）** + 新增 1 文件（`TOwnedValue.inl` **已随提交 `8c54c598` 入库**；行尾已对齐全树 CRLF）。**验证**：**CoreCLR**（改名前的第一版）UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（`Saved/Retest/out/G1-ownedvalue`，CSV `UnrealCSharp-CoreCLR-…`）、**LeanCLR**（改名后的最终形态）UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（`Saved/Retest/out/G1-ownedvalue-rename`，CSV `UnrealCSharp-LeanCLR-…`）；两轮 `ensure`/`assert` 0、`LogBlueprint: Error` 0。⚠️ **两次回归后端不同**（改名前 CoreCLR、改名后 LeanCLR —— 维护者本地已把 `WindowsScriptDomainType` 切到 LeanCLR，未改回）⇒ "同一后端上跑改名前后两版"**未取得**；且**未复测计数**（探针已在第八刀后移除）。**未做**：容器/委托/Optional 的 `bNeedFreeData`/`bNeedFreeProperty`/`Handler::bNeedFree` 未动；绑定族的 `delete` 语义未纳入该基类（基类只做 `FMemory::Free`）。详见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.4

---

#### 2.4.11 第十一刀：结构体 `FMember` 同址覆盖缺陷（定论 + 修复）+ 非地址映射 9 族弱句柄实装（2026-09-23，**已提交 `3da38bb6`**）

> **对象 = §2.4.8 留下的两个待决项**：① 登记为"未触发"的结构风险；② 已切片但未实现的弱句柄修法（1807 条/周期）。
> **① 定论（实测触发）—— 新编号 `F-REG-020`**：`FStructPropertyDescriptor::Get(…,FMember)` **不先查地址映射**（与字符串/多引用族及其自身 `NewRef` 相反）⇒ 同一 `(UScriptStruct*, 地址)` 被第二次读到就**覆盖地址映射并孤立旧句柄**（旧句柄被强 `GCHandle` 钉死、`RemoveReference` 的等值守卫永不成立 ⇒ 无任何释放路径，驻留到域卸载）。触发用例 = 同一调用点的 `SetStructValueFunction` ×2 + 同一 `ref` 变量 `OutStructValueFunction` ×2（**临时**，已还原）；实测 `[G1PATH] memberGet 8→12 / memberGetHit 0→4`、`[G1FAM] Struct over 0→4 / orphan 0→4`、两周期逐字节相同。
> 🔴 **② 方法学更正**：§2.4.8 用「`regs == 残留 + rel` 成立 ⇒ 零覆盖」是**无效判据**（覆盖不减少 `ManagedHandle2Value` 条目，等式照样成立）；正确判据 = **`addrmap == remain − over`**（`1209 = 1209 − 0` / `1213 = 1217 − 4` 两向对上）。详见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.5.1。
> **③ 最小修复（3 行，与同族同形）**：`FMember` 改为"先 `GetObject(Struct, Src)` 查表、未命中才 `NewObject()` + `AddStructReference<false>`" ⇒ 实测 `over 4→0`、`orphan 4→0`、`Struct regs 1229→1225`、**2 × 1706 / 0 失败**。语义变化 = 同址重复读返回**同一托管包装**（= 字符串/多引用族与 `NewRef` 的既有契约）；**`FReturn` 不改**（新堆拷贝，复用会指向已释放副本）。
> **④ 实装（C++ 19 处 + C# 11 处，零协议面改动）**：只改 `IsMember=false` 的创建点 —— 5 个字符串描述符 `Get(…,FReturn)`、6 个多引用描述符 `Get(…,FParameter/FReturn)`、`TPropertyValue.inl` 的 `TString/TMultiPropertyValue::Get<IsReference>`，以及 11 个 `Script/UE/Library/*Implementation.cs` 的 `HandleData.Alloc(InX)` → `(InX,false,true)`（`InType` 保持强）。**实测**：9 族合计注册 1944→1946、**`rel` 0 → 891 / 756**（释放率 **0% → 45.8% / 38.9%**）、**残留 1944 → 1055 / 1190（−45.7% / −38.8%）**、`over` 全 0；`[G1FIN]` 终结器计数由**全 0** 变为非零（`FString` 611/1136、`FName`/`FText` 45/106、`TSubclassOf` 42/152、`TWeakObjectPtr` 25/52、`TLazyObjectPtr` 23/46、`TSoftObjectPtr`/`TSoftClassPtr` 45/89、`TScriptInterface` 10/35）。
> ⚠️ **施工坑（复用价值）**：`TPropertyValue.inl` 中"`NewObject();` + `if constexpr (IsReference)`"的块**有 5 个**（字符串/多引用/**binding ×2**/**optional**），按结构批量替换会**误伤地址映射的 binding/optional 族**（本轮实测误改 3 处并已回退）—— 门控必须看"紧随的注册调用是否 `IsMember=false`"。
> **⑤ 裁定：地址映射子集（1391 注册 / 1377 残留）本轮不弱化**，走「(b) 继续强句柄 + 上面的复用修复」：弱化前必须先定义"地址映射失效"语义（`GetObject(address)` 会交回已回收包装的句柄）；而地址映射子集里唯一会覆盖/孤立的路径（结构体 `FMember`）已修掉；结构体 `FReturn`（1209 条残留的大头）地址每次都是新堆块、**不能**复用也不能弱化。
> **验证**：`dotnet build Script.sln` **0 warning / 0 error**；UBT **Succeeded**；**无探针最终回归 = 两阶段域重建 2 × 1704 / 1704、0 失败、`ensure`/`assert` 0、`LogBlueprint: Error` 0**；探针全树 0 残留。**未做**：只测 CoreCLR（未复测 LeanCLR/Mono）、字节量级未量化、两个域周期 `rel` 不同 ⇒ 仍是 GC 时机决定的"最终回收"而非确定性释放（`F-CS2A-005`/`F-CS2A-023` 不变）、`(a)` 删条目重建方案未设计。**已知残余风险**：`Script/UE/CoreUObject/Unreal.cs:45` 的 `HandleData.GetHandle(new FString(…))`（全插件唯一"临时包装句柄逃出表达式"）弱化后理论可被提前回收（本轮未观测到失败），建议落到局部变量。详见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.5；装置与四轮留档 = `Saved/Retest/probe-g1-family-v2/README.md`、`Saved/Retest/out/G1-11-{struct-verdict,weak-verdict,weak-final,clean}`；补丁 = `Saved/Retest/fix-G1-11-weak-handle.patch`。
> 🔴 **跨后端复测（2026-09-23，同装置同探针，见 §5.5.7）**：收益 **CoreCLR 891/756（45.8% / 38.9%）≫ Mono 123/23（6.3% / 1.2%）≫ LeanCLR 0/0（0%）**，三后端均 **2 × 1704 / 1704、0 失败、0 ensure/assert**。**LeanCLR 的零收益已排除两层**：① 把全插件唯一的强制 GC 从 `AssemblyLoader.Unload` 的 `Context != null` 守卫移出**仍全 0**（该改法**已还原、不采纳**）；② 同路径探针类终结器 **3/3 执行**、`GC.CollectionCount` 0→1→2→3，而 11 个包装族的终结器**一次都不执行** ⇒ 缺口 = **这些包装类的终结器未被 LeanCLR 的 GC 排队**（具体环节未定位）。⇒ **本刀收益是 CoreCLR/Mono 的现象；LeanCLR 下这 11 个子族的闭环仍在（残留 1944/周期，与改造前逐值相同）**，引用数字时**不得跨后端外推**。

---

#### 2.4.12 第十二刀：**路线 A —— 地址映射条目全弱化**（2026-09-23，**已提交 `3da38bb6`**）

> **对象** = §2.4.11 裁定「不弱化」的地址映射子集（1391 注册 / 1377 残留）。维护者决定改走全弱化。
> **实现三件事**：**A1** 新增存活查询桥接 `HandleData.IsAlive(nint)`（新 Op `HandleDataIsAlive`；`IScriptDomain::IsAlive` 共享实现，LeanCLR 经 `Bridge_Invoke`，三后端头各一行 `override`；**桥接不可用时按「存活」兜底**，绝不误删）；**A2** 5 个注册表的地址反查加**自愈**：命中后若句柄已死 → `RemoveReference` + 摘地址映射 → 返回 invalid ⇒ 调用点既有的「未命中即新建」分支重建包装；**A3** 弱化地址映射的创建点（native 19 处 + **C# 2 文件**）。
> **实测（同一套件）**：A 轮桥接冒烟 `lookup=22/dead=0`；B 轮（漏改 C# struct 主路径）`Struct rel` 12→**14/20**（几乎不动）⇒ **暴露主创建源是 C# 侧 `new T{…}`**（`StructImplementation` 的 `HandleData.Alloc` 仍是强句柄）；C 轮补上后 **`Struct rel 623/536`**、`remain/addrmap 1209→598/685`；**D 轮（套件中途强制 GC）合计注册 3165 / 会话内释放 2854 = 90.2%**、`String rel 1101/1220`、5 个子族残留清零、**`addrmap` 1391→117**、`over=0/orphan=0`；E 轮 **LeanCLR 仍全 0**（`rel/dead/heal` 皆 0；BCL 是预编译 `mscorlib.dll`，**未定位**）；F 轮无探针最终回归 **2 × 1704/1704、0 失败**。
> **口径变化**：地址映射契约 = 同一地址恒返回同一**存活**包装；旧包装被回收则删条目重建（C# 侧不再可能拿到指向已回收对象的死句柄）。
> **代价/边界**：反查命中多一次桥接往返（本套件 22~63 次/周期，**未做性能量化**）；**A2 自愈已用专项用例验证正确**（读属性 → 释放句柄但保留地址映射 → 再读：`dead=1/heal=1`、重建包装读出正确值，`G1SelfHealOrphanedHandleValue` 通过）；container/delegate 两族不弱化也不自愈；Mono 未复测；✅ **「视图包装收不掉」已定位销案**（run J/K）：普通包装对照**可回收**、视图包装释放句柄后仍不收 ⇒ 根不是句柄；把"创建并丢弃"放进独立方法且该地址未在调用者帧出现过时**正常回收** ⇒ 真因是 **Debug JIT 把调用者帧早期临时对象报为存活**（测量假象），D 轮 90.2% 的结论不变。补丁 `Saved/Retest/fix-G1-12-routeA.patch`；留档 `Saved/Retest/out/G1-12-A-{1-bridge,routeA,routeA2,heal,leanclr,clean}`。详见 [`08-…/07`](../08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) **§5.6**。

---
## 3. 不要修 / 已撤销（32 条）

> 来源：`09-…/01` 总表中 `严重度` 首字段为 `撤销` 的 **32 行**（逐条汇总）。这些条目**保留编号**（不重排、不删除），但**请从排期中移除**。
> ⚠️ **本清单不得直接结案**：其中若干条的"撤销"证据不足，见 §5 的"存疑，勿当结案"清单。

| # | 编号（+ 出处报告） | 原级别 | 一句话理由（摘自总表/报告） |
|---|---|---|---|
| 1 | `F-ARCH-005`（[00-总览/01](../00-总览/01-架构与数据流总览.md)） | P2 | 动态 UClass 永久 `AddToRoot()` 累积根集 —— **已证伪**：`FDynamicClassGenerator.cpp:393 AddToRoot()` 与 `:529-543`（`ReInstance` 内）的 `RemoveFromRoot()/MarkAsGarbage()` 成对 |
| 2 | `F-ARCH-006`（[00-总览/01](../00-总览/01-架构与数据流总览.md)） | P2 | 全局单例群析构顺序不确定导致句柄泄漏 —— **已证伪**：`FScriptDomainFactory.cpp:47-59` 在 `Deinitialize()` 后 `delete` 整个域并置 `IScriptDomain::Set(nullptr)` |
| 3 | `F-REG2-001`（[01-…/03](../01-UnrealCSharp运行时/03-容器委托字符串注册表与模块入口.md)） | **P0** | `NewWeakRef(bIsCopy=true)` 把 UObject 属性内存当堆对象 `delete` —— **机制证伪**（分配器匹配，`bIsCopy=true` 唯一来源是 `CopyValue` 的 `FMemory::Malloc`）；残留的真缺陷是"池化瞬态形参帧被别名"，已在 P1 族跟踪 |
| 4 | `F-REG2-008`（[01-…/03](../01-UnrealCSharp运行时/03-容器委托字符串注册表与模块入口.md)） | — | `FUObjectListener::NotifyUObjectDeleted` 未做线程归属判断 —— **已证伪撤销** |
| 5 | `F-PROP2-001`（[01-…/04b](../01-UnrealCSharp运行时/04b-属性描述符-字符串枚举结构体Optional.md)） | **P0** | `FStructPropertyDescriptor::Set` 依赖 `CopySingleValue` 的 memcpy 兜底即双重释放 —— **已证伪**：`FStructProperty` 在 `PropertyStruct.cpp:337-340` 重写 `CopyValuesInternal` → `Struct->CopyScriptStruct`，是成员级深拷贝，结构性不可能（**注意：`05` 的同名 `F-PROP2-001` 是 P0，内容完全不同**） |
| 6 | `F-HLP-004`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)） | P1 | 自建 `FScriptArray` 路径下元素缓冲永不释放 —— **机制成立但不可达**（5 个构造点全传非空 `InData`）；`delete` 经 `~ForAnyElementType` 正常 `Free(Data)` |
| 7 | `F-HLP-009`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)） | P2 | `Reset(InNewSize)` 与 `TArray::Reset(NewSize)` 语义不符 —— **证伪**：UE 的 `TArray::Reset` 本来就不设置元素个数 |
| 8 | `F-HLP-027`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)） | **P0** | `FSetHelper::GetEnumerator` 的 `nullptr` 被无条件解引用 —— **撤销**：唯一调用者在 `private` 索引器内、紧邻 `IsValidIndex` 且每轮重读 `Num()` → nullptr 分支不可达 |
| 9 | `F-HLP-029`（[01-…/07](../01-UnrealCSharp运行时/07-容器Helper.md)） | P2 | `FSetHelper::Remove` 在 `RemoveAt` 后不做 `Rehash` —— **证伪**：原文 "Remove the element from the hash"，`RemoveAt` 自己维护哈希链。⚠️ 上一版写进计划的"必须与 028 一起修"**必须撤回** |
| 10 | `F-INT2-009`（[01-…/10b](../01-UnrealCSharp运行时/10b-Interop注册-容器字符串与对象指针.md)） | P3 | LeanCLR 走 `[DllImport("UnrealCSharp")]` 但无对应 C 导出符号 —— **已证伪**：LeanCLR 用 `PInvokes::register_pinvoke` 建进程内映射，不需要真实导出符号 |
| 11 | `F-INT2-010`（[01-…/10b](../01-UnrealCSharp运行时/10b-Interop注册-容器字符串与对象指针.md)） | — | 容器"值/句柄"双模式契约依赖隐式对齐 —— **证伪**：不一致类不存在 |
| 12 | `F-INT1-008`（[01-…/10](../01-UnrealCSharp运行时/10-Interop注册-对象与反射类.md)） | P2 | `LoadObject` 失败 → `Bind(nullptr)` 的 `UStruct` 路径 null 安全性未确认 —— **已证伪**：`Bind(UStruct*)` 是 `<auto IsNeedOverride>` 模板、**不可推导**，实走已判空的 `Bind(UObject*)` |
| 13 | `F-INT1-009`（[01-…/10](../01-UnrealCSharp运行时/10-Interop注册-对象与反射类.md)） | P3 | 死注册：`UClass.ClassDefaultObject` 无 C# 消费者 —— **已证伪**：版本宏 `!UE_VERSION_START(5,6,0)` 在 5.6 为 0，该 `.Property` 根本不参与编译（源码 52 条 / 实际生效 **51** 条） |
| 14 | `F-RM-015`（[02-…/02](../02-UnrealCSharpCore核心/02-反射模型（类字段方法参数属性）.md)） | — | `UnboxValue` 返回栈局部地址、双重解引用构成 UB —— **撤销**：LeanCLR 实现返回的不是栈局部地址 |
| 15 | `F-DYN-007`（[02-…/03](../02-UnrealCSharpCore核心/03-动态类型生成.md)） | — | 动态接口原地 `PurgeClass(true)` 后不调 `DestroyPropertiesPendingDestruction()` —— **复核：撤销（非缺陷）** |
| 16 | `F-DOM-026`（[02-…/04](../02-UnrealCSharpCore核心/04-脚本域抽象与三大后端.md)） | P3 | `FStringToString<>` 转换对象被 `const auto` 持有后跨语句使用 —— **已证伪**：`StringConv.h:663-701` 自持缓冲 + 拷贝/移动构造 `= delete`（已由引擎源码彻底证伪，证据充分，**可结案**） |
| 17 | `F-BR-002`（[02-…/06](../02-UnrealCSharpCore核心/06-类型桥接编码设置与引擎监听.md)） | P1 | `GetClass(FProperty*)` 缺 `FUtf8StrProperty`/`FAnsiStrProperty` 分派使专用重载成孤儿 —— **已证伪**：工厂传的是**静态类型指针**，编译期就重载解析到 `FTypeBridge.cpp:475` 的专用重载 |
| 18 | `F-GEN-004`（[03-…/01b](../03-代码生成器/01b-类生成器与绑定类生成器.md)） | P1 | `FRotator`/`FVector` 默认值被静默清成 0 —— **已证伪**：产物 `new FRotator(-90.000000,…)` 值未丢（**产物即地面真值**） |
| 19 | `F-GEN-007`（[03-…/01b](../03-代码生成器/01b-类生成器与绑定类生成器.md)） | P1 | C# 关键字表漏掉上下文关键字 `value` —— **已证伪**：.NET SDK 10.0.401 实测 `value` 属性名往返值 42 正确 |
| 20 | `F-GEN-009`（[03-…/01b](../03-代码生成器/01b-类生成器与绑定类生成器.md)） | P1 | 默认参数字面量在两套实现中语义分歧 —— **已证伪**：两套实现各自自洽 |
| 21 | `F-ED1-008`（[04-…/01](../04-编辑器模块/01-内容浏览器与新建类向导.md)） | — | `CompileFilter` 只累加不清空 —— 撤销 |
| 22 | `F-ED2-026`（[04-…/02](../04-编辑器模块/02-工具栏监听器进度对话框与模块入口.md)） | P3 | 三个路径定制"重复实现" —— **已证伪**：已是正确的三层结构，残留重复仅两个 7 行工厂文件（V1 自述"不是缺陷"） |
| 23 | `F-CMP-031`（[05-…/01](../05-编译器与跨版本/01-编译器CSharp编译与跨版本宏.md)） | — | 说明性条目：`AsyncTask(GameThread)` + `WaitUntilTaskCompletes` 与自旋等待 **经核对不是缺陷，但值得记录** |
| 24 | `F-CS1-004`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)） | P1 | `GetObjectPointer` 返回托管对象裸地址、GC 压缩后失效 —— **已证伪**（LeanCLR GC 非移动）。🔴 **但撤销前提查不到证据 + "无人使用"的理由被 grep 排除 → 见 §5，不得当结案** |
| 25 | `F-CS1-013`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)） | P2 | `FieldBridge` 的 `SetStaticValue`/`GetStaticValue` 零调用点 —— **已证伪**：grep 漏 `*.inl`，`FScriptDomainImpl.inl` 才是真实调用点 |
| 26 | `F-CS1-026`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)） | P1 | `Weavers.csproj` 从未定义 `WITH_LEANCLR` → 生产者/消费者不匹配 —— **已证伪**（已编译 `Weavers.dll` 元数据含只在 `#if WITH_LEANCLR` 内的方法）。🔴 **该条撤销口径仍有分歧 → 见 §5** |
| 27 | `F-CS3-006`（[06-…/03](../06-CSharp运行时与脚本/03-引擎实现库Library（跨语言ABI契约）.md)） | P2 | 3 个 C++ 注册在 C# 侧无声明 —— **更正：只有 1 个成立**（`Contains1`，已并入 `F-CS3-009`），另 2 个是活的（产物 `Script/UE/Proxy/Binding/Object.cs:20`、`Class.cs:23` 有真实调用点） |
| 28 | `F-BLD-001`（[07-…/01](../07-构建与配置/01-构建脚本模块依赖与插件配置.md)） | P1 | 插件 props 硬编码作者机器引擎绝对路径 —— **已证伪**：文件由引擎生成且被 gitignore，UBT 以 MSBuild 全局属性注入 `EngineDir`，props 赋值永不执行 |
| 29 | `F-BLD-006`（[07-…/01](../07-构建与配置/01-构建脚本模块依赖与插件配置.md)） | P2 | Editor 模块把 `UnrealEd` 等放进 `PublicDependencyModuleNames` —— **已证伪**：前提"只在模块内部使用"被公开头反证 |
| 30 | `F-BLD-007`（[07-…/01](../07-构建与配置/01-构建脚本模块依赖与插件配置.md)） | P2 | 同上（`DirectoryWatcher`）—— **已证伪** |
| 31 | `F-LIFE-002`（[08-…/02b](../08-专项审计/02b-UObject生命周期与绑定配对审计.md)） | **P0** | `FDelegateWrapper::Method` 是裸 `FMethodReflection*`，域重载后 UAF —— **已证伪**：热重载是"先删 DelegateRegistry 置空 descriptor、后删域"，`DelegateDescriptor != nullptr` 守卫使 `Method` 永不被触碰 |
| 32 | `F-PERF-029`（[08-…/05](../08-专项审计/05-性能热点与优化清单.md)） | P3 | 编辑器 ticker 注册后永不注销 —— **已证伪**：该位置**正是 V1 自己 §6「已有优化·请勿重复提」表里的一行**，属自相矛盾 |

> **§3 的口径说明**：上表 **32 行**与逐条汇总的"撤销（非缺陷）= 32"一致。
> `F-REG-005` **不在**本表内 —— 它曾被标"撤销（非缺陷）"并已从排期移除，**已恢复 P2 并重新纳入排期**（见 §5）。
> **一处已知的报告内部不一致（如实记录）**：[`01-…/07-容器Helper.md`](../01-UnrealCSharp运行时/07-容器Helper.md) 的 §5.0 索引与 §5 结论把 `F-HLP-027` 写成"撤销（非缺陷）"，而该报告 §7.6 的节标题仍写"P0 × 1（027）"。**最终口径以逐条汇总为准**：`07` 复核后 **P0 只有 `F-HLP-023` 一条**（`F-HLP-027` = 撤销），与本表及 `09-…/01` 总表一致。

---

## 4. 修复顺序建议（不是级别顺序）

> 判据：不先做前者，后者的修复会**引入新错误**、**无法验证**、或**掩盖问题**。

| # | 先做 | 后做 | 为什么顺序不能反 |
|---|---|---|---|
| 1 | **P0-4 容器索引/句柄契约**（C# 侧索引器改抛 .NET 异常） | **P0-3 Interop 导出层判空**（native 侧 `Checked()` 样板） | 不是"先有异常屏障才能抛异常"——LeanCLR 下已确证**托管异常不终止进程**（走 `RtResult`、调用方拿零值），屏障不是硬前置；但改 native 侧会把可自诊断的失败与"合法 0 句柄"混为一谈 ⇒ **先定 C# 侧的契约** |
| 2 | **P0-2 对象/委托描述符 Set 判空** | **§2 组的"挂起/悬空委托"族** | 描述符 `Set` 的判空是"输入契约"的统一落点；不先统一，每个族会各写一套判空，覆盖率无法确认（`F-INT2-016` 的 `&In != nullptr` 就是"看起来判了空、其实被优化掉"的实例） |
| 3 | **P0-5 `TOut` 步长 + `F-GEN2-001` 缺括号** | 任何"多返回值 API"的功能验证 | 两者是 **OUT_BUFFER 协议的写端与读端**，**必须同批改**；只修一端时 `GetIdentity`/`GetDate` 这类多 out 场景仍取不到正确值。⚠️ **两者不是同一族**（见 P0-5），需**分别修** |
| 4 | **§2-G1 强 `GCHandle` 释放责任归属** | **§2-G10 容器/注册表"只增不减"+ 裸地址为键** | 很多"只增不减"其实是"**减的责任被交给了永不执行的终结器**"。不先厘清责任归属，会把**责任错位**误判为**真泄漏**，从而给本来正确的释放代码加多余的 `Compact()` |
| 5 | **§2-G4 域代次（generation）机制** | **§2-G10 + P0-2 的跨域部分** | 否则同一批容器要在"无代次"和"有代次"两套结构上各改一遍，第二遍还会引入新的失效判断。✅ **2026-09-22 更新**：G4 的**六处 `static` 缓存已按"去掉缓存"路线关闭**（§2.3），**并未引入域代次**；本行想保护的"别把同一批容器改两遍"针对的是**注册表/容器的跨域清理**（G10 与 P0-2 的跨域部分），与本条的**描述符元数据属性缓存**不是同一批结构 ⇒ **本行的前置关系对那两者依然成立**，但不再有"先做 G4 代次"这一步 |
| 6 | **P0-7 工厂返回值判空**（`FTypeBridge::Factory` / `FScriptDomainFactory::Create`） | **§2-G7 的"启动期汇总校验"** | 工厂的 `nullptr` 语义是"不支持/未编译进来"的唯一表达；不先统一，启动期校验没有可依赖的失败信号 |
| 7 | **§2-G5 编译入口收口**（同步入口绕过 `IsCompiling()` 守卫） | **§2-G5 的竞态修复（`TQueue` 契约）** | 同步入口绕过守卫时会有**两个 `dotnet build` 并发**，会把真正的竞态因果掩盖掉 |
| 8 | **§2-G6 关停期死锁（`Kill(true)` 互等）** | 进程退出路径的任何验证 | 已经互等的进程无法用来验证别的修复 |
| 9 | **P0-11 `F-GENC-001` 4 处反序列化返回值检查** | 产物类缺陷的验证 | 损坏的 `Intermediate/*.json` 会在**第一次保存资产时**崩，先修它才能稳定复现其它生成期问题 |
| 10 | **§1.1 ⚠️ 误修警戒（活值 Dest 4+4 点）** | 任何"统一给 `Set` 加 `DestroyValue`"的批量改动 | 不先钉住这 4 个"不要改"的点，**按旧清单施工会引入比原缺陷更严重的 UB**（对未初始化内存调析构） |

- **P0-8（编辑器委托句柄配对）可以完全独立进行**：它是全清单里"收益/成本"最好的一组（句柄存进容器 + `ShutdownModule` 统一 `RemoveAll`），不阻塞任何其它修复。
- **P0-9（反射去重键）与 P0-10（动态类型生成）也应早做**：它们各自独立，但都属"失败静默"型（调到错误方法 / 编辑器挂死），越晚发现越贵。**✅ 两条均已修复**（`P0-10`① 记 `492ce5f7`；`P0-10`② 与 `P0-9` 记 `2113e1e9b1bcce88cc4524f9f0fd78ab064a9970`）。

---

## 5. ⚠️「存疑，勿当结案」清单

> **来源**：「『已撤销/非缺陷』清单不可直接结案」的独立复核结论。
> **风险不对称（最重要的方法论之一，M-6）**：
> **高估严重度的代价是浪费工时；错误撤销的代价是缺陷永久漏修。** 而"撤销"带着"已裁决/非缺陷"的标签，**后续最不容易再翻出来** —— 所以撤销判定被列为**最高抽验优先级**。
> **硬约束**：以下条目 **不得**写入"已判定为非缺陷"清单，也**不得**从排期移除。

| # | 编号（+ 出处报告） | 当前记载 | 存疑点 | 处置 |
|---|---|---|---|---|
| 1 | `F-CS1-004`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)） | 撤销（非缺陷）/ 活跃 | 撤销前提**查不到证据**；且"无人使用"的理由被 grep 排除 —— `GetObjectPointer` 在 `FLeanCLRDomain.cpp:125,155,167,176,196` 有 **5 处活跃调用** | ⚠️ **改判"存疑"，不得当结案剔除**。注：LeanCLR GC 非移动（mark-sweep）已实证，故"改 Weak 句柄"的建议**不可采纳** |
| 2 | `F-CS1-026`（[06-…/01](../06-CSharp运行时与脚本/01-Interop桥接与程序集加载.md)） | 撤销（非缺陷）/ 潜伏 | weaver 侧 `WITH_LEANCLR` 在整个插件根 37 命中里**只由** `FSolutionGenerator.cpp:214` 注入 `Template/Shared.props:7` —— 若 weaver 项目也吃该 props 则撤销成立，**否则原发现是真缺陷** | ⚠️ **改判"存疑"**，需核 weaver csproj 是否 `Import` `Shared.props`。🔴 **2026-09-17 复测轮补充证据**：注入逻辑确实存在（`ReplaceDefineConstants`），但写 `Shared.props` 的 `FSolutionGenerator::Generator()` **不在编辑器启动路径上**（只由工具栏/控制台命令/cook 分支触发；`CopyTemplate` 的默认策略是**每次覆盖**，`FSolutionGenerator.h:15-18`）⇒ 切后端后**不跑生成器就必然拿不到新的 `WITH_*`**（⚠️ 本条初版写的"只写一次 / 默认 `false`"已更正，见 [`05-…/01`](../05-编译器与跨版本/01-编译器CSharp编译与跨版本宏.md) 与 [`08-…/06`](../08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md) 的更正块）；实测后果见 [`08-…/06`](../08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md) 的 **`F-BLD-022`**（C++/C# 后端错配 ⇒ 编辑器挂起 60 s 后被 UE 挂起检测判 `Fatal error`）。✅ **2026-09-17 处置更新（阶段性，已被下一条完整版取代）**：`F-BLD-022` 当时**部分修复**（当轮 ①-⑥，见该报告 §3「处置」）—— `Shared.props` 改为**内容不同即写**、新增 `IsScriptPublishOutdated()` 判据并在编辑器启动/PIE 起点触发重编；**「weaver 是否吃该 props」这一存疑点同时得到实测回答**：`Weavers.csproj:7` 确实 `Import ../Shared.props` ⇒ **原发现的"生产者/消费者不匹配"在"props 陈旧"这一窗口内是真缺陷**（撤销只在 props 被正确刷新时成立）。✅ **2026-09-17 处置更新**：`F-BLD-022` 已**修复并提交**（压缩提交 `cd0dfacb`，7 处改动，见该报告 §3「处置」）—— props 改为**内容不同即写**、新增 `IsScriptPublishOutdated()` 判据、并在 **`StartupModule` 最前面（域首次初始化之前）同步构建**；**两个后端方向均通过运行期验证**（LeanCLR 3×1700/0、CoreCLR 3×1700/0）。**「weaver 是否吃该 props」这一存疑点同时得到实测回答**：`Weavers.csproj:7` 确实 `Import ../Shared.props` ⇒ **原发现的"生产者/消费者不匹配"在"props 陈旧"窗口内是真缺陷**（撤销只在 props 被正确刷新时成立）。⚠️ **仍存的边界**：`SyncCompile` 失败静默（残留 MSBuild 节点锁 `obj` 会使修复失效）、`-game`/打包不自愈 |
| 3 | `F-PROP-007`（[01-…/04](../01-UnrealCSharp运行时/04-属性描述符-基类与基本类型.md)） | **P1 / 活跃**（未撤销） | 与 `08-…/01 F-DEAD-007`（判**不可达**）、`01-…/05 F-PROP2-004`（判**不可达**）构成 **2 : 1 分歧**；已裁定"以不可达为主流结论"（依据 `FGeneratorCore.cpp:833` 显式排除 `FFieldPathProperty`），**但残留未排除**：作为受支持容器/结构体的 inner property 是否仍能触发 `FPropertyDescriptor::Factory`（`FPropertyDescriptor.cpp:127`） | ⚠️ **待最终裁定**；未裁定前**不得**按"不可达"结案 |
| 4 | `F-DEAD-007`（[08-…/01](../08-专项审计/01-全局死代码与死宏审计.md)） | P3 / 不可达 | 与 #3 是**三条指向同一类型的同一问题**（`F-DEAD-007` / `F-PROP2-004`(05) / `F-PROP-007`） | ⚠️ 汇总时**合并为一条并注明三个编号**，避免重复占位；裁定未完成前同 #3 |
| 5 | `F-PROP2-004`（[01-…/05](../01-UnrealCSharp运行时/05-属性描述符-对象与委托类型.md)） | P3 / 不可达 | 同 #3（`FFieldPathPropertyDescriptor` 已注册但无任何 `Get/Set` 覆写）。⚠️ **与 `04b` 的同名 `F-PROP2-004`（P0）不是同一条** | ⚠️ 同 #3 |

> **已可结案的（证据充分，不必再追）**：`F-DOM-026`（引擎源码彻底证伪）、`F-GEN-004`/`F-GEN-007`/`F-GEN-009`（.NET SDK 10.0.401 实测 + 产物地面真值）、`F-CS1-003`/`F-CS1-010`（已改为"潜伏"/"仅 CoreCLR/Mono 会杀进程"，`06-…/01` 当前 P0 数 = **0**）。
> **`F-REG-005`（[01-…/02](../01-UnrealCSharp运行时/02-对象引用结构体注册表.md)）已恢复 P2**（见 §2-G2），**必须从任何"非缺陷清单"中移出** —— 它的机制在源码中每一跳都成立（`FStructReference` 析构 → `FStructRegistry.cpp:119` → 回调 `FReferenceRegistry.cpp:44`，且该回调路径**唯一**），失效是**顺序相关而非必然**；复核结论明确"若运行时实测出稳定双删，则应升 **P0**"（见 §6）。
> **`F-MACRO-006`（[01-…/09](../01-UnrealCSharp运行时/09-绑定宏与CoreMacro对照.md)）的 `C2062` 断言已被本机编译器四变体矩阵证伪**（真实失败条件是"调用处多写分号/宏体自带 `;`"，诊断是 MSVC **C2181**、clang `expected expression`），判**部分确认**、可达性 活跃 → **潜伏**。⚠️ 凡引用过"`C2062` 结论"的其它报告需一并更正 —— **该全局清理未见执行记录**。

---

## 6. 实机验证清单（"要跑起来才能定论"）

> 来源：全库级缺口 G-1…G-7、逐报告未核清单、仍在进行项、必须实机验证项，以及各报告的"未覆盖/存疑"表。
> **总根源（G-1）**：**全部 41 份含发现的报告都声明"未编译、未运行、未做 ASan/定量测量"**。⇒ 所有 P0/P1 崩溃类结论都停留在**静态推演**：**代码事实准确，但"必然崩溃"从未被触发过**。
> 自报口径：**36 份有内容**的报告合计约 **303 项**未覆盖/存疑（`08-…/02b`、`08-…/05` 的该表为空表，其 49 条发现的疑点在汇总层全部缺失）。

| # | 要验证什么 | 具体动作 / 断言 | 卡点与出处 |
|---|---|---|---|
| 1 | **全部 P0/P1 崩溃类断言的"必然性"** | 逐条构造最小触发输入，断言**进程存活**或得到**具体 .NET 异常**（而非访问违例） | 需编译器 + 编辑器/PIE；41 份报告全部未做（G-1） |
| 2 | **`F-REG-005` 的"双删 vs 泄漏"** | 运行期测注册顺序：若实测出**稳定双删** → 该条应升 **P0** | 取决于运行期注册顺序，静态不可判 |
| 3 | **`F-ENV-011`：旧句柄命中新域条目** | 依赖 LeanCLR 的 ALC 语义与 `HandleData` 计数器行为（句柄单调计数器 `Clear()` 不重置） | 本机未运行 |
| 4 | **`F-CS2A-023`：finalizer 永不执行** | 断言"强句柄对象在 N 次 GC 后仍可达"以及 native/托管资源**均不释放** | 报告自标"未实机证实" |
| 5 | **`FLeanCLRDomain:831 Empty()`** | `08-…/02` 自认这是**唯一可能推翻该报告某条『否』结论**的点 | 需实机；断言的完备性无法静态证明 |
| 6 | **`F-CS2B-009` / `F-CS2B-010`：`Send` 不阻塞 + `TaskInfo` 换异常** | 跨线程 `Send` 断言 (a) 同步返回；(b) 不抛 `ObjectDisposedException`；(c) 回调抛异常时 `Send` 侧收到**原异常类型** | 唯一未验证环节：`TaskInfo.Invoke()` 是否补偿了等待 |
| 7 | **`06-…/02b` 仍有 15 条未被独立验证** | `004, 006, 007, 014, 015, 016, 021, 022, 023, 025, 027, 028, 029, 030, 031`（其中 `027` 正文现值已升 P1，但未被独立确认） | 预算耗尽（该报告的逐条未核清单） |
| 8 | **全部平台兼容类结论（非 Windows）** | Linux / Mac / Android / iOS 全部为**宏路径推演**；含 **非 MSVC（clang-cl / Clang）是否等价保证 UTF-8** —— 它决定 `F-ABI-022` 撤销的**成立边界** | 需 Linux/Clang 构建（G-3） |
| 9 | **三大脚本后端的运行时内部行为** | 泄漏量级、终结器线程模型、GC 搬迁性、ALC 卸载语义 —— **无法从静态阅读得出** | `Source/ThirdParty/{Mono,CoreCLR,LeanCLR}` 为子模块，本仓库无源码（G-2） |
| 10 | **`F-ED2-001` / `F-ED2-017` 的 UAF 首次落地时点** | 反复"关闭/重载编辑器模块" 20 次断言无访问违例；加"句柄计数 == 注册数"断言 | 需实机跑编辑器；另 `F-ED2-007` 死锁**可复现性**、编译失败蓝图上 `GeneratedClass` 的**真实空值率**同样未测 |
| 11 | **`F-INT2-001`(10b) 越界堆破坏 / `F-HLP-006` 越界 / `F-CS1-003`·`F-CS3-001` 的 `call 0`** | 越界与 `call 0` 的"必然崩溃"均为静态推演；`call 0` 的 fail-fast 已用 net10.0 探针在 **CoreCLR** 下实机复现（`TerminateProcess` 且绕过 UE 崩溃上报、`try/catch` 无效） | LeanCLR 下形态不同，需分别验证 |
| 12 | **`F-CMP-002`：`SyncProcess` 完全无超时** | 把 `dotnet` 可执行文件替换为一个无限 `sleep` 的脚本，断言 (a) 编辑器有超时提示且不冻结；(b) 关闭编辑器能在 N 秒内退出 | 未执行（`07-…/01` 未执行任何构建） |
| 13 | **`F-BIND-001` 的多 out 实测** | `FDateTime::GetDate` 写出槽位应为 `0,0,4` 而 C# 读 `0,4,8`；`GetIdentity`（2×`FString&`）两参数同写 `+0` | 已**逐行推演精确复现**，但仍需实机断言 |
| 14 | **`06-…/03` 的 950 槽 ↔ `BINDING` key 逐条 diff** | 该报告自认"**最大剩余风险面**"；另 734 个 `.Function(` 名字↔语义未核 | 需逐条 diff（非实机，但未做） |
| 15 | **`F-LEAK-012` 的泄漏量实机量化** | ①"元素缓冲泄漏"**已证伪**（`delete` 经容器分配器基类析构 `Free(Data)`，`ContainerAllocationPolicies.h:682-693`）；②"元素析构缺失"成立 → 泄漏收窄为**元素内层堆**，量级未测。⚠️ **原文建议的 `FMemory::Free(ScriptArray)` 修法必须撤销**（会反向制造它想修的缓冲泄漏） | 需实机量化（`08-…/02`） |
| 16 | **生成产物的时效性（G-4）** | `01/11` 明确"无法确认 `Script/*/Proxy/` 与当前 git HEAD `1b29b6ed` **严格同源**"；产物计数是**时点值**（**16 055**（`Script/UE/Proxy`）+ **75**（`Script/Game/Proxy`），每次生成都变化） | 需重新生成 + 编译 |
| 17 | **产物类缺陷改完必须重新生成 + 编译一次** | `F-GEN2-001` 的括号修复影响 **235 处**生成产物（含 `GameplayStatics`/`Actor` 等主路径） | 需实机（P0-5 的收尾动作） |
| 18 | ✅ **`01-…/07` 的 P0/P1 重判（2026-09-17 完成 ⇒ G-7 关闭）** | 已对 **P0 1 条 + P1 10 条逐条重判**并留下可审计结论：`F-HLP-006`（`492ce5f7`）/`F-HLP-007`（`2113e1e9`）**补登为已修**；`F-HLP-016`、`F-HLP-024` **P1→P2、活跃→潜伏**，`F-HLP-017` **活跃→潜伏**；三条「待重判」（`F-HLP-020`/`025`/`005`）**全部关闭**（`020`/`025` 维持 P1，引擎 `Map.h:1955-1958`→`Set.h:1843-1865` 逐行确证 `Empty` 不析构）；另更正 **4 处**依据/建议（含 set 侧替换 API 应为 `EmptyElements` 而非 `EmptyValues`） | 全文与依据见 [`01-…/07` §10](../01-UnrealCSharp运行时/07-容器Helper.md)。⚠️ **未做运行期验证**（仍属本节第 1/15 条）——「已重判」≠「已验证」 |

> **另有两处"未取得"（非实机卡点，但下游会反复问到）**：
> ① **引用完整性**：全量解析记录引用 **2758** 条、一次解析 **96.48%**、坏引用 **19** 处、**行号越界 0 处**；逐条修复核对记录 **已修 18 / 无需修 2 / 跳过 2**，复查后"清单内坏引用**已无残留**" ⇒ **净错误 0**。⚠️ **任务书给出的"3572 条 / 99.75% 一次解析"在本套文档内 grep 不到（未取得）** —— 上述 2758/96.48% 与"净 0"是本文件能引用的实际记载。
> ② **`832`（旧口径）条目下是否引入新重号**：3 条"新发现顺延追加编号"是否带来新重号**未复算（未取得）**；已确认的重号构成仍是 `F-INT2-*` 16 处 + `F-PROP2-*` 15 处 = **31**。
