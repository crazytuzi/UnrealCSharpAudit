# UnrealCSharpAudit

UnrealCSharp 插件（UE 5.6）的**深度审计报告库 + 修复跟踪**。

- **47 份报告 / 834 条发现**：P0 = 26、P1 = 128、P2 = 267、P3 = 381、撤销（非缺陷）= 32
- 产物分三层：**原始报告**（按模块 / 专项，逐条 Finding 带"文件:行"、代码事实、调用上下文、复核证据）→ **逐条汇总的总表**（`09-问题清单/01-全局问题总表.md`）→ **排期视图**（`09-问题清单/00-推荐优先修复清单.md`）
- 结论均来自**静态阅读 + 引擎源码比对**，**未编译、未运行、未压测**；需实机验证的项在各报告的"未覆盖/存疑"章节逐条标注

## 出处（provenance）

| 项 | 值 |
|---|---|
| **分析对象** | UnrealCSharp 插件（`UnrealCSharp.uplugin` VersionName = 1.2.0，作者 crazytuzi） |
| **引擎** | UE 5.6 —— 报告中引擎源码引用一律写成引擎根相对路径（`Engine/Source/...`），不写机器绝对路径 |
| **分析快照** | 插件提交 **`85348c68`**（2026-09-11）—— **推断值**：与总表"汇总时间戳 2026-09-11 01:04"一致，**待确认** |
| **修复提交** | 插件提交链条（父提交依次相接）：**`492ce5f7046cfd07032adda275f94ae6b5be6505` "Null Validation"**（2026-09-15 16:53:12 +0800，54 个文件，父 `85348c68`；P0-1/P0-2/P0-3/P0-11 与 P0-10①）→ **`2113e1e9b1bcce88cc4524f9f0fd78ab064a9970` "Fixed some bugs"**（2026-09-16 12:54:34 +0800，**19 个文件 +222/-152**，父 `492ce5f7`）—— ⚠️ **P0-4/P0-5/P0-6/P0-8/P0-9/P0-10② 六组原先是分六次提交的，现已压缩合并为这一次提交**；**本库引用一律只用 `2113e1e9`，不再出现那六次的哈希** → **`1551383ba3aa8bfea564ea5844c15c7ea642b660` "FRegisterArray BufferSize && MethodBridge ParamCount"**（2026-09-17 01:47:30 +0800，**22 个文件 +288/−105**，父 `2113e1e9`；**G7 活跃路径子集**，见下两行）—— ⚠️ **同样是分散提交被压缩**：该组原先是 `cbf55bf6`/`71154011`/`09797144` 三次（提交信息均为 `t`），现已合入这一次；**本库引用一律只用 `1551383b`** → **`8c54c59866712ff5127735acc7a9629efda767fc` "GCHandle Free && GCHandleType.Weak"**（2026-09-23 12:59:33 +0800，**46 个文件 +227/−204**，父 `9deda0af`；**本库引用 G4 与 G1 一律用 `8c54c598`**。**附带登记（工程侧指针）**：`UnrealCSharpTest` 的 `Plugins/UnrealCSharp` 指针已由 `85a9ea84` 指向 `8c54c598…`（工作区标记 `-dirty`）；子模块内 `Source/SourceCodeGenerator` 指针变更**未包含**该提交，仍留在工作区） |
| **已修复范围** | P0-1、P0-2、P0-3、P0-11 与 P0-10①（`492ce5f7`）+ **P0-4、P0-5（含同批的 P1 条目 `F-GEN2-001`）、P0-6、P0-8、P0-9（末条 P0）、P0-10② 六组（`2113e1e9`，六次分散提交已合并为这一次）** ＝ **10 组 / 26 条编号**；**部分修复 1 组** —— P0-7（① `F-BR-001` 已修；② `F-DOM-003` 经核查**未修复**（修复提交未含 `FDomain.cpp`），且经确认**决定不做**：不符合项目规范）；**未动 0 组** —— 26 条 P0 编号**至此全部有处置登记**（`F-DOM-003` 是"决定不做"，**不是**"已修"）。⚠️ **四处"修复方向未照抄" + 一处边界**：P0-4 的 C# 侧异常语义（静默回 `default`/`null`）、P0-8 的"句柄存入成员容器"（改用 `RemoveAll(this)` 与 `CreateRaw`+`IsBoundToObject`）、P0-9 的"去重键含参数类型 + 键冲突 `ensureAlwaysMsgf`"（查表端只有"名字 + 元数"，加类型会全查不到；同键不同签名现为合法状态）、P0-10② 的"visited 集合 + `ensureAlwaysMsgf`"（visited 判据不成立、日志不做）、P0-6 只覆盖**同线程重入**（跨线程竞争属 P1 族）—— 理由见 [`09-问题清单/00-推荐优先修复清单.md`](09-问题清单/00-推荐优先修复清单.md) §1.2 |
| **G7 活跃路径子集（2026-09-16；已由维护者压缩提交为 **`1551383b`**，22 个文件全含）** | **8 处代码点 / 14 条编号已改完并通过两端编译验证；其中 10 条「已修」、4 条「仅部分处置」**（部分处置：`F-CS2B-010`、`F-CS1-003`、`F-CS3-001`、`F-ABI-002`）。⚠️ **范围按"两后端并集"定**：本轮实测**当前配置不是 LeanCLR**（Windows/Linux/Mac=CoreCLR、Android/iOS=Mono），故 `F-CS2B-027` 当前不参编、`call 0` 族当前反而活跃 —— 见 [`00-总览/03`](00-总览/03-修复优先级评估.md) §1.3 的**事实更正**（连带更正"`Script/Shared.props:12` 已定义 `WITH_LEANCLR`"这条被多处引用的依据）。**口径（本轮实测确认的项目惯例）**：**不新增注释、不新增 `throw`、不新增日志（`Console.Error.WriteLine`/`UE_LOG`）、不新增 try/catch** —— 失败一律**静默**收敛到既有返回值；因此 **`F-CS2B-010`（`Process` 隔离 / `Tick` 兜底 / `finally` 防替换）与 `F-CS1-003`=`F-CS3-001`=`F-ABI-002` 的 `call 0` 仍未修**（这两类修复必须依赖 try/catch 或异常），`F-CS1-003` 一族仅做到"个数不符时 fail-closed 返回 0"。**验证**：`dotnet build Script.sln` **0 warning / 0 error**；C# 的 `WITH_LEANCLR` 分支另用 `-p:DefineConstants=…WITH_LEANCLR` 单编通过；UBT `UnrealCSharpTestEditor Win64 Development` 在 CoreCLR 与 LeanCLR 两种配置下均 **Result: Succeeded**（LeanCLR 侧判据 = 产物 DLL/PDB 含 LeanCLR 专属新增符号 `PInvokeParamCounts`）。**运行期完整用例已跑通**：`-game` 模式执行 `UTestCoreSubsystem::Test()`，CoreCLR `…-2026-9-16-20-10-44-838.csv`、`…-20-18-15-312.csv`、`…-2026-9-17-1-53-42-668.csv`（**01:53 为提交后按当前 ini 复跑**）与 LeanCLR `…-2026-9-17-0-44-40-521.csv` 均 **1700 例 / 0 失败**，用例集与改动前基线（`…-2026-9-16-12-31-48-936.csv`）逐一相同；⚠️ 唯一例外是 2026-09-17 01:00 那一对 LeanCLR CSV 的第二个（**2 例间歇失败**，含该两例的 121 个 CSV / 53 对同会话 CSV 中仅此一次）—— ✅ **已定位（2026-09-17 复测轮，运行期证据）**：`nullptr` 实参经 `FCSharpBind::Bind(UObject*)` 查询以 `TWeakObjectPtr` 为键的注册表时**命中"已销毁但未注销"的残留条目**，返回有效句柄 ⇒ C# 侧把 `ref UObject`/`ref UClass` 的 null 入参看成**已销毁对象的存活包装**；**与后端无关**（同探针 CoreCLR 6/6、LeanCLR 24/24 命中），修法一行（`Find` 前判空）—— ✅ **已修并提交：压缩提交 `cd0dfacb216b79a4e831e102612c070164d6ab2a`（父 `1551383b`，2026-09-17 19:46:35 +0800，20 文件 +329/−95）**。修法：`Bind(UObject*)` + `FObjectRegistry::GetObject/GetManagedHandle` 三处判空、`FObjectRegistry::RemoveReference(const UObject*)` 同族判空，形态均为**正向 `if` 包裹 + 末尾默认返回**的项目风格。施工后同探针 CoreCLR 6/6、LeanCLR 9/9 均不再命中，最终形态另复验 CoreCLR 6/6，回归 8 轮 1700 例 / 0 失败（该提交后另 1 轮 1700/0）；**仍开放**：已销毁对象的注册表条目不会清理（`NotifyUObjectDeleted` 对 `UStruct`/`UClass` 走 `RemoveClassDescriptor` 分支）。明细见 [`08-专项审计/06`](08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md)（新编号 **`F-REG-019`**）。**构建模型**：工程侧的 `Script/Weavers/UnrealTypeWeaver.cs`、`Script/SourceGenerator/UnrealTypeSourceGenerator.cs` 是**部署副本**（不在插件仓库内 ⇒ 不在 `1551383b` 之内），已核对含同样改动。**未做**：`F-ABI-004` 的**类型**（非个数）校验、`TMap`/`TSet` 同型缓冲协议、`Send` 的超时/取消。**（上条间歇项的复测定论已于 2026-09-17 的复测轮完成 —— 已定位为 `F-REG-019`，见 [`08-专项审计/06`](08-专项审计/06-复测轮：G7间歇项定位与null句柄缺陷（2026-09-17）.md)；同轮另实测出一条构建模型缺陷 `F-BLD-022`：切后端时 C# 部署面不会重建，实测可致编辑器挂起 60 s 后被判 `Fatal error`（**已修并提交：压缩提交 `cd0dfacb`**，两个后端方向各 3×1700/0，并做了 Windows 与 Android 打包验证）。）****明细见 [`09-问题清单/00-推荐优先修复清单.md`](09-问题清单/00-推荐优先修复清单.md) §2.1**（该节含"构建模型提醒"：`Weavers`/`SourceGenerator` 的工程部署副本与插件模板是**两份**源码，必须同改） |
| **P1 泄漏族收口（2026-09-17，`01-…/07`）** | 维护者提交 **`cd0dfacb216b79a4e831e102612c070164d6ab2a`**（压缩提交；其中本轮泄漏族修复为 **7 个文件 +98/−18**）—— 修 **6 条 P1**：`F-HLP-001`、`F-HLP-014`、`F-HLP-019`、`F-HLP-020`、`F-HLP-025`、`F-HLP-026`（＝重判后仍"未修且活跃"的全部）。改动 = 三个 Helper 的 `.h/.cpp` + `TPropertyValue.inl`；**C# 侧与生成器零改动**。⚠️ **提交数与本补丁数差 2 行**：本补丁为 +96/−16，另 2 行是维护者对 `FMapHelper.cpp`/`FSetHelper.cpp` 的 **`#if/#else` 两分支 `Rehash` 行做的对齐修订**（纯缩进，非本修复内容）。**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Result: Succeeded**；运行期 `Saved/Retest/run-batch.ps1 -Tag P1-leak-fix-final`（CoreCLR）**1700 例 / 0 失败**、与基线逐名一致 ⇒ 详情与"两处未照抄报告建议"的理由见 [`01-…/07` §11](01-UnrealCSharp运行时/07-容器Helper.md) |
| **G4 收口轮（2026-09-22；✅ 已提交 `8c54c59866712ff5127735acc7a9629efda767fc`，父 `9deda0af`）** | **对象 = G4 的核心机制**：6 个函数级 `static TArray<FClassReflection*>`（`F-REFL-002` / `F-DYN-006` / `F-LIFE-010` = **同一处代码的三次登记**）。**改法 = 去掉缓存本身**（报告首推的"generation 计数器"未采用）：6 个 getter 去静态化、返回值改 **by-value**，数组每次调用从 `FReflectionRegistry::Get()` **现查重建** ⇒ "缓存跨 `Deinitialize()` 存活"结构性消亡，**零新增公开 API、零失效逻辑、消费侧零改动**。改动面 = **2 文件 24 增 / 24 删**（`Public/Dynamic/FDynamicGeneratorCore.h` + `Private/Dynamic/FDynamicGeneratorCore.cpp`；补丁留档 `UnrealCSharpTest/Saved/Retest/fix-G4-metadata-cache.patch`）。**验证**：UBT `UnrealCSharpTestEditor Win64 Development` **Succeeded**；**两阶段域重建装置**（PIE#1 → `UnrealCSharp.Editor.SetActive 0` → `SetActive 1` → PIE#2，即"卸载并 `delete` 全部 `FClassReflection` → 重建 → 再生成"）**2 × 1704 例 / 0 失败**、与基线仅差已知 4 条 `CSharpNativeEvent*` 用例、`ensure`/`assert` **0**；`-game` 回归 **1704 / 0**。🔴 **未取得**：**修复前"陈旧指针"的运行期对照** —— 3 次装置尝试（`SetActive 0/1`、无改动 `Compile`、改 C# 源后 `Compile`）配临时同一性探针（`[G4PROBE]`，已移除）实测**探针 460 行全部落在程序集加载时的那一次生成**（`stale=1` 计数 **0**），三次重载后**都没有再触发生成** ⇒ **本工程当前的动态类型生成只在 C# 程序集加载时发生一次**；故本条的运行期证据 = "无回归 + 域重建路径跑通"，**不是"复现了旧缺陷"**，后果判定（多数为静默元数据丢失、少数为写错元数据）仍属静态推演。**本组其余代表条目未动**：`F-DYN-003`/`F-DYN-008`/`F-DYN-016`/`F-LIFE-006`（同主题的 `F-DYN-010` 依赖图重置亦未动）。详见 [`09-问题清单/00-推荐优先修复清单.md`](09-问题清单/00-推荐优先修复清单.md) **§2.3** |
| **G1 首刀轮（2026-09-22，**已提交 `8c54c598`**）** | **交付物 = 独立报告 [`08-专项审计/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md)**（释放链条 / 责任归属表 / 实测更正 / 未覆盖清单）。🔴 **前提更正（本轮最重要一条）**：**"强句柄 ⇒ 终结器永不执行"经实测不成立** —— 编辑器两阶段装置实测 `TArray` 注册 ≥**276** / **注销 364**（唯一调用者就是终结器）/ `FDomain::GCHandle_Free` **7962** ⇒ 原生侧释放点会先放掉句柄、对象随即不再被钉住。**更正后判据：真缺口 = "有终结器、但没有原生释放点"**；故原建议的"全局改弱句柄 + `IDisposable`"（D=2）**本轮决定不做**。**已修 3 处（4 文件 +5/−4、C# 侧零改动）**：`F-REG-007`（`FMultiRegistry.inl` 的 `RemoveReference` 补 `FDomain::GCHandle_Free` + 补 include）、`F-DEL-008`（`FOptionalRegistry::RemoveReference` 同补）、`F-RM-004`+`F-DOM-008`（`FPropertyReflection.cpp`/`FFieldReflection.cpp` 各删一对 `#if WITH_CORECLR`，与 `FMethodReflection`/`FClassReflection` 的无条件写法对齐）。**验证**：UBT **Succeeded**；编辑器两阶段域重建 **2 × 1704 例 / 0 失败**（`ensure`/`assert` 0）；`-game` 回归 **1704 / 0** 且与基线逐名一致；**全部临时探针已移除**。**未做**：A1/A2 的运行期收益未量化；A4 只在 CoreCLR 下构建回归；`08-…/07` §5 的 4 条真缺口（`F-INT2-011`(10b)、`F-LEAK-002`、`F-ENV-002`、`F-REG2-002`）一条未修 |
| **G1 第二刀（2026-09-22，**已提交 `8c54c598`**）** | **对象 = `F-INT2-011`(10b) 的字符串族子集（5 / 12 处）**：`Script/UE/Library/{FString,FName,FText,FAnsiString,FUtf8String}Implementation.cs` 的 `*_ToStringImplementation` **取值后补 `HandleData.FreeImplementation(Handle);`**（⚠️ 不能用 `Free` —— 它是 `[UnmanagedCallersOnly]`）。这 5 处原生侧**一律** `IScriptDomain::NewString(...)`（新建句柄、`count=1`、不进任何注册表、无其它持有者）⇒ 释放是唯一正确配对。⚠️ **报告清单其余 7 处未动**（`TOptional_Get`/`TArray_GetCompound`/`TMap_*Compound`×5/`TSet_GetEnumeratorCompound`/`TLazyObjectPtr_Get`/`TSubclassOf_Get` —— 它们返回**包装对象**，须先逐点判定句柄归属）。**验证**：C# 双工程 **0 warning / 0 error**；`-game` **1704 / 0**；两阶段域重建 **2 × 1704 / 0**。**实测（C++ 计数探针，已移除）**：套件一次运行 `FString.ToString()` 仅 **1** 次、`GCHandle_Free` **283** 次 ⇒ **本套件对该路径几乎不覆盖、收益未量化**（真实收益在用户代码大量字符串化处）。**同轮记下 4 条构建模型事实**（`Game` 对 `Interop` 是**二进制引用**须先编 Interop；**编辑器运行期改 C# 会造成版本错配**；`IsScriptPublishOutdated()` **不看源码时间戳** ⇒ 改源不会自动重编；**MSBuild 复用节点会锁 `obj/UE.dll` 使构建静默失败**）与 **1 条环境事实**（`-game` 启动期偶发 `0xC0000005`，**发生在插件 C# 域初始化之前**、重跑即过）。详见 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) §2.4.1 |
| **G1 第三刀（2026-09-22，**已提交 `8c54c598`**）** | **对象 = `F-ENV-002`**（`FBindingRegistry` 的 binding wrapper 泄漏）。**机制**：两处 `AddReference` 都 `new TBindingAddressWrapper`，而 `delete Value.AddressWrapper` 被 `if (Value.bNeedFree)` 挡住 ⇒ `bNeedFree == false` 的注册每次漏一个 wrapper 堆对象。**改法（= 报告建议）**：把"是否释放 **pointee**"下沉进 wrapper（`FBindingAddressWrapper` 增 `bNeedFree`），派生析构改 `if (bNeedFree) delete pointee;`；`FBindingAddress` 的 `bNeedFree` 成员删除；两处清理一律 `delete wrapper`。**语义边界**：`bNeedFree==false` 只表示"不释放 pointee"，**wrapper 自身永远要删**（原实现把两件事混成一个标志）。**实测（临时计数探针，已移除）**：两阶段装置每周期 `created=33 / deleted=33 / nofree=4`、累计 `66/66/8` ⇒ **修复前每周期固定漏 4 个 wrapper，修复后零漏**。**验证**：UBT **Succeeded**；两阶段 **2 × 1704 / 0**。详见 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) §2.4.2 |
| **G1 第九刀：binding 所有权判据编译期化 + `bNeedFree` 全量核对（2026-09-22，已提交 `8c54c598`）** | **起因**：第三刀把"是否释放 pointee"从注册表侧的 `FBindingAddress::bNeedFree` 下沉进 wrapper ⇒ 于是 `FBindingRegistry.cpp` 的两处清理不再用该标志判断，而字符串/多引用/结构体三族**还在用**同名标志。**改动**：`FBindingAddressWrapper` 删掉 `bNeedFree` 成员与 ctor 第二参数（只剩 `void* Value`，虚析构保留）；`template <typename T, auto IsNeedFree> struct TBindingAddressWrapper` 析构改 `if constexpr (IsNeedFree) { delete static_cast<const T*>(Value); }`；两个注册点改 `new TBindingAddressWrapper<T, IsNeedFree>(InObject)` / `<T, false>(InObject)` ⇒ **binding 族已无该标志**（wrapper 少 1 字节、判据零运行期分支、`FBindingRegistry.h` 改动面 23→14 行）。⚠️ **实测踩坑（复用价值）**：原代码靠 **CTAD** 让 `T` 对常指针实参推导成 `const X`；改成显式非类型模板参数后，ctor 若仍收 `T*` 就接不住 `const T*`（实测 3 处 **C2665**）⇒ ctor 与 `delete` 都改按 `const T*`。**全量核对结论**：`bNeedFree` 全库 7 处（binding 1 + 字符串 1 + 多引用 1 + 结构体 1 + 容器三 Helper 4 个字段 + 委托 Handler 2）**没有一个是死变量** —— 字符串/多引用/结构体的三处读于 `RemoveReference` + `Deinitialize`、两个取值都真实出现；容器/委托/Optional 那几处本来就在各自析构里读（"判据放在被拥有对象体内"的既有范式，与 binding 改完后的形态一致）；`Script/` 全树 `NeedFree` **0 命中**。**为什么三族不能照搬 binding**：它们的 wrapper 是**按值存 TMap 的普通结构**、pointee 是裸 `FString*`/`TSubclassOf*`/`UScriptStruct*+void*`，**没有析构钩子可挂判据**。**未做（已评估未采纳）**：~16 处清理块去重（各族 `FreeValue()` / `TNeedFreeValueWrapper<T>` mixin / 三族全多态化 ❌ 每次注册多一次 `new`）。**验证**：UBT **Succeeded** + 两阶段域重建 **2 × 1704 / 0**、`ensure`/`assert` 0（`Saved/Retest/out/G1-env002-tidy`）；⚠️ **未复测**第三刀的 wrapper 计数（探针已移除）。详见 [`08-…/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) **§5.3** 与 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) **§2.4.9** |
| **G1 第十刀：`TOwnedValue<Derived>` 所有权/释放抽成 CRTP 基类（2026-09-22，已提交 `8c54c598`）** | **对象 = 字符串/多引用/结构体三族那 16 处 `if (bNeedFree) { FMemory::Free(…); … }` 清理块的去重**（第九刀已评估、当时未做）。**最终形态 = 真 CRTP**（不是"单继承 mixer"也不是"每族各自 helper"）：新增 `Public/Registry/TOwnedValue.inl` —— 基类持 `bool bNeedFree` + 门禁 `void Free() { if (bNeedFree) { static_cast<Derived*>(this)->FreeImplementation(); bNeedFree = false; } }`；三个 wrapper 改多继承 `TStringAddress<T> : TValueWrapper<T>, TOwnedValue<TStringAddress<T>>` / `TMultiAddress<T,IsType> : TValueWrapper<T>, TOwnedValue<…>` / `FStructAddress : FStructAddressBase, TOwnedValue<FStructAddress>`，各自删掉 `bNeedFree` 字段、改实现 3 行钩子 `FreeImplementation()`，钩子做成 **`private` + `template <typename> friend struct TOwnedValue;`**（唯一公开入口是带门禁的 `Free()`，防绕过标志把借用的属性内存 `FMemory::Free` 掉）。**15 个调用点**（字符串 6/多引用 7/结构体 2）→ 一行 `Value.Free();`，**门禁 16 份 → 1 份**。**命名记录**：`TNeedFreeValue`（动词短语做定语、语序别扭）→ `TOwnerValue`（❌ 英语读作 "owner 的值"；且本库 `Owner` 已固定指**宿主句柄**：`AddReference(InOwner,…)`/`OwnerManagedHandle`/审计库"3 参无 owner vs 5 参带 owner"口径，源码 **332 处**命中）→ 定名 **`TOwnedValue`**（`true`＝值归我 / `false`＝借用），**方法名分工（维护者定名）**：对外 `Free()`、派生钩子 `FreeImplementation()`（贴合本库 `*Implementation` 惯例）；⚠️ 第一版曾反过来，已改名并重新构建复验。**先例**：自引用模板基类本库已有（`FClassCollector : TSharedFromThis<FClassCollector>`、`FUnrealCSharpEditorCommands : TCommands<…>`），**但多继承在插件自有类里此前 0 命中**（本刀首次）—— ⚠️ 这也更正了第九刀"CRTP 与多继承都无先例"的说法：**只有多继承没有**。**改动面**：8 文件 +66/−116（净 −50 行）+ 新增 1 文件（`TOwnedValue.inl` 已随提交 `8c54c598` 入库）。**验证**：**CoreCLR**（改名前的第一版方法名）UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（`Saved/Retest/out/G1-ownedvalue`，CSV `UnrealCSharp-CoreCLR-…`）、**LeanCLR**（改名后的最终形态）UBT **Succeeded** + 两阶段 **2 × 1704 / 0**（`Saved/Retest/out/G1-ownedvalue-rename`，CSV `UnrealCSharp-LeanCLR-…`），两轮 `ensure`/`assert` 0、Blueprint Error 0；⚠️ **两次回归后端不同**（改名前 CoreCLR、改名后 LeanCLR —— 维护者本地已切到 LeanCLR 且未改回）⇒ "同一后端跑改名前后两版"未取得；且**未复测计数**（探针已在第八刀后移除）。**未做**：容器/委托/Optional 的 `bNeedFreeData`/`bNeedFreeProperty`/`Handler::bNeedFree` 未动（它们本就在被拥有对象的析构里）；绑定族 `delete` 语义未纳入该基类。详见 [`08-…/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) **§5.4** 与 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) **§2.4.10** |
| **G1 第四刀（2026-09-22，**已提交 `8c54c598`**）** | **对象 = `F-LEAK-002`**（跨语言调用的 `ref`/`out` **引用类型**参数句柄）。**机制**：C# `MethodBridge.Invoke` 写 `*Param = HandleData.Alloc(Parameter)`（新句柄），C++ 读回值后从不释放（返回值路径有、out 路径没有）。**改法**：① `TMethodHelper.inl:145-148` 读回后补 `FDomain::GCHandle_Free`；② `FManagedFunctionDescriptor.cpp` out 循环**先快照调用前槽位值**，`Set` 后**仅释放"值发生变化"的句柄**。**判据** = 同文件返回值路径 `:61-64` 的现成先例；⚠️ **明确不释放影子句柄**（`IManagedHandleFromObject` 打包的裸指针）与未变化的槽位。**实测（临时日志探针，已移除）**：`TMethodHelper` 路径**命中 62 次**（≈ 每轮 PIE **31 个**此前一直漏的 out 句柄）；`FManagedFunctionDescriptor` 的 out 路径**本套件 0 命中**（仅编译验证）。**验证**：UBT **Succeeded**；两阶段 **2 × 1704 / 0**。详见 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) §2.4.4 |
| **G1 第五刀（2026-09-22，**无新代码改动**）** | **产出两条改变排期的结论**：① 🟢 **`F-INT2-011`(10b) 的实际范围 = 5 处（已修）而非 12 处** —— 其余 7 处（`TSubclassOf_Get` 走 `FCSharpEnvironment::Bind(...)`、`TOptional_Get`/`TArray_GetCompound`/`TMap_*Compound`/`TSet_GetEnumeratorCompound`/`TLazyObjectPtr_Get` 走 `PropertyDescriptor->Get(..., RETURN_BUFFER)`）返回的是**给用户的包装对象**，其句柄要继续用 ⇒ **补 `FreeImplementation` 会打断后续原生调用**，属**非缺陷**；② 🔴 **`F-REG2-002` 已量化、修法需拍板**：两阶段装置首周期 `reg3=382 / reg5=44 / remove=38 / 卸载时残留 388`（校验 `382+44−38=388` ✅）⇒ **每域周期约 390 个容器条目驻留、显式释放率约 9%**；CoreCLR 下**有界**（卸载回收）、**LeanCLR 下永久**（`F-DOM-006`）。**需三选一**：弱句柄（D=2）/ `IDisposable`（新增公开 API）/ 接受域卸载回收 + 先修 `F-DOM-006`。原文的"自 owner"建议**不释放任何东西**（只补簿记），本轮如实标注。详见 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) §2.4.5 |
| **G1 第六刀（2026-09-22，**已提交 `8c54c598`**）** | **对象 = `F-DOM-006` + `F-CS1-001`**（域卸载的批量清理被守卫连带跳过）。**机制**：`Script/Interop/AssemblyLoader/AssemblyLoader.cs` 的 `Unload()` 把 `HandleData.Clear(); TypeBridge.Clear();` 写在 `if (Context != null)` **之内**，而 **LeanCLR 从不引用 `AssemblyLoaderLoadFromStreamFn`**（`FLeanCLRDomain.cpp` 0 命中）⇒ `Context` 恒 null ⇒ **清理段每次卸载都被跳过**（LeanCLR 连 VM 都不卸载）。**改法**：两个 `Clear()` 提到守卫之外（无条件执行），`if` 内只留 ALC 卸载 + GC 等待。🔴 **在该缺陷唯一生效的后端 LeanCLR 上实测**（临时探针，已移除）：`Context == null` 打印 `89000001`（CoreCLR 为 `89000000`）、`HandleData.Clear()` 打印 **`88000047`**（CoreCLR `88000049`）⇒ **Clear 真的执行了、每次卸载释放 47 个句柄**；修复前 LeanCLR 只有"恒 null"、没有 Clear 行。**回归**：**LeanCLR 2 × 1704 / 0** + **CoreCLR 2 × 1704 / 0**（后端已还原为 CoreCLR）。**顺带验证**：切后端时 `Shared.props` 由插件自动重写并重编 C#（`F-BLD-022` 的修复在真实切换下有效）。详见 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) §2.4.6 |
| **G1 第七刀：`F-REG2-002` 解决方案（2026-09-22，**原型已实现并验证，已提交 `8c54c598`**）** | **先量化来源**（此前的决策前提）：3 参注册 382 次/域周期 = **C# ctor（`new TArray/TMap/TSet`）309（81%）** + 属性读视图（`NewWeakRef`）43（11%）+ 编组（`TPropertyValue`）30（8%）⇒ **主因是"用户 `new` 出来的包装被强句柄钉死"**，故正确修法是"**让这些句柄不再钉住对象**"，而非逼用户加 `IDisposable`。**三层实现**：**L1** `HandleData.Alloc(obj, bPinned, bWeak)`（默认仍强 ⇒ 其余句柄语义不变）；**L2** 5 个 C# ctor 的**包装**句柄改弱（`Type` 句柄保持强）；**L3** 端到端加 `bIsWeak`：`Op(ObjectBridgeNewObject,…,1→2)` + 函数指针 typedef + `IScriptDomain::NewObject(handle,bIsWeak=false)`（三后端头同步，均走共享实现）+ `FClassReflection::NewObject(bool)` + C# `ObjectBridge.NewObject(nint,int)` + 5 个描述符 `NewWeakRef` 传 `true`。**5 参（带 owner + 地址映射）注册保持强句柄**。**实测**：显式释放 **38→137→162→180（×4.7）**（四档 = 基线/原型① L1+L2/原型② +描述符视图/原型③ +`TPropertyValue` 视图）；域卸载残留 **388→246（−37%）**；**✅ 三后端 CoreCLR / LeanCLR / Mono 各 2 × 1704 / 0 无回归**（协议面变更已获维护者接受）。**边界**：① 改了这 5 族句柄语义（未穷举第三方用法）② 协议面变更（1→2 参，与旧版 DLL/C# 不兼容）③ 仍是最终回收，非确定性释放 ④ 🆕 **同型缺口**：`TPropertyValue.inl` 另有 18 处 `NewObject()` 服务于其他家族（`TSubclassOf`/弱指针族/结构体…），其注册表多带地址映射，**不能照搬** ⑤ 稳态与内存量级未量化。详见 [`08-…/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) §5.1.2 |
| **G1 第八刀：同型闭环的分族量化（2026-09-22，无新代码改动，仅测量）** | **对象 = `F-REG2-002` 之外的五个注册表族**（`08-…/07` §5.1.2 边界 ④ 与 §6.1 此前如实留白"其他家族未测量"）。**装置**：临时 C++ 探针 `FG1FamilyProbe` 挂在 5 个注册表的 **6 个 `AddReference` / 4 个 `RemoveReference`** 站点（另记"是否写地址映射"`member`、"是否自持堆块"`owned`），5 个 `Deinitialize()` 逐子表打印条目数；两阶段编辑器装置 × CoreCLR。**结论**：闭环**5 族全中且量级更大** —— 每域周期 **注册 3198 / 显式释放 14（0.44%）/ 卸载残留 3184**（`FString` 1220、`FStructRegistry` 1221 为大头；容器族修后对照 382/180/246 ⇒ 残留 **12.9 倍**）；**3005 条（94%）是 `IsNeedFree=true` 的自持原生堆块**（`FunctionMacro.h:128/143` 的 `CopyValue` 堆拷贝 + `TCompoundPropertyDescriptor.inl:33-48`、`TPropertyValue.inl:94/147` 的 `new std::decay_t<T>`、`FRegister*.cpp` 自建 `new`、`TConstructorHelper.inl:22/42`）⇒ **比容器族多一层纯原生内存泄漏**（容器族 3 参路径只钉托管对象）。**修法可行性已切片（尚未实现）**：**非地址映射 1807 条/周期（释放 0，含约 1777 个自持堆块）可安全照搬弱句柄通道**；**地址映射 1391 注册 / 1377 残留条不可照搬**（`GetObject(address)` 会取回已回收包装 ⇒ 须先定"删条目重建 vs 继续强句柄"的语义）。**同刀登记 1 条结构风险（未触发）**：`FStructPropertyDescriptor::Get(..., FMember)` 不先查地址映射 ⇒ 同址反复读会覆盖地址映射、旧句柄从此无人释放（本轮 1221 次注册**零覆盖**，需专项用例定论）。**验证**：UBT **Succeeded**；装表运行与去表收尾运行**各 2 × 1704 / 0**、`ensure`/`assert` **0**；探针 0 残留（曾装表的 3 个注册表 `.cpp` 与 HEAD 逐字节一致）。**未做**：任何修法均未实现/未测量、只测 CoreCLR、字节量级与稳态曲线未量化。详见 [`08-…/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) **§5.2** 与 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) **§2.4.8** |
| **G1 第十一刀：结构体 `FMember` 同址覆盖定论并修复 + 非地址映射 9 族弱句柄实装（2026-09-23，**已提交 `3da38bb6`**）** | **两件事**：① **定论**（§5.2 登记的"未触发结构风险"）—— `FStructPropertyDescriptor::Get(…,FMember)` **不先查地址映射**（与字符串/多引用族及其自身 `NewRef` 相反）⇒ 同一 `(UScriptStruct*, 地址)` 第二次被读到就**覆盖地址映射并孤立旧句柄**（强 `GCHandle` 钉死 + `RemoveReference` 等值守卫永不成立 ⇒ 无释放路径，驻留到域卸载）。**专项目例实测触发**（同一调用点 `SetStructValueFunction` ×2 + 同一 `ref` 变量 `OutStructValueFunction` ×2，临时改动已还原）：`memberGet 8→12 / memberGetHit 0→4`、`Struct over 0→4 / orphan 0→4`，两周期逐字节相同。**最小修复**（3 行，与同族同形）后 `over 4→0 / orphan 4→0`、`regs 1229→1225`。② **实装**：`IsMember=false` 的创建点接上第七刀的弱句柄通道（**C++ 19 处 + C# 11 处，零协议面改动**）⇒ 9 族合计 **`rel` 0 → 891 / 756**（释放率 **0% → 45.8% / 38.9%**）、**残留 1944 → 1055 / 1190（−45.7% / −38.8%）**；`[G1FIN]` **终结器计数由全 0 变非零**（`FString` 611/1136…）⇒ "强句柄钉死 ⇒ 终结器永不执行 ⇒ 自持堆块无人释放"的闭环在这 9 族上被打开。🔴 **方法学更正**：§5.2 用「`regs == 残留 + rel` 成立 ⇒ 零覆盖」**是无效判据**（覆盖不减 `ManagedHandle2Value` 条目）⇒ 正确判据 **`addrmap == remain − over`**（两向实测对上）。**裁定**：地址映射子集（1391/1377）**不弱化**（需先定义反查失效语义；其唯一覆盖路径已修；struct `FReturn` 地址每次新堆块、不能复用）。**验证**：`dotnet build Script.sln` 0/0；UBT **Succeeded**；**无探针最终回归 2 × 1704 / 1704、0 失败、`ensure`/`assert` 0**。**未做/残余**：只测 CoreCLR；字节量级未量化；两周期 `rel` 不同 ⇒ 仍是 GC 时机决定的最终回收（`F-CS2A-005`/`023` 不变）；`Unreal.cs:45` 的 `GetHandle(new FString(…))` 为唯一"临时包装句柄逃出表达式"点（本轮未观测到失败，建议落局部变量）。🔴 **跨后端复测（2026-09-23，同装置同探针，见 §5.5.7）**：收益 **CoreCLR 891/756（45.8%/38.9%）≫ Mono 123/23（6.3%/1.2%）≫ LeanCLR 0/0（0%）**，三后端均 **2 × 1704 / 1704、0 失败、0 ensure/assert**；**LeanCLR 零收益已排除两层**（把唯一强制 GC 移出 `Context != null` 守卫仍全 0，该改法已还原不采纳；同路径探针类终结器 3/3 执行而 11 个包装族终结器一次都不执行）⇒ 缺口 = **这些包装类的终结器未被 LeanCLR 的 GC 排队**（具体环节未定位）⇒ 收益**不可跨后端外推**，LeanCLR 下闭环仍在。装置与四轮留档 = `Saved/Retest/probe-g1-family-v2/README.md`、`Saved/Retest/out/G1-11-{struct-verdict,weak-verdict,weak-final,clean,retest-leanclr,retest-mono,retest-leanclr-gc,leanclr-gcprobe,final-clean-coreclr}`；补丁 = `Saved/Retest/fix-G1-11-weak-handle.patch`。详见 [`08-…/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) **§5.5 / §5.5.7** 与 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) **§2.4.11** |
| **G1 第十二刀：路线 A —— 地址映射条目全弱化（2026-09-23，**已提交 `3da38bb6`**）** | **对象 = 第十一刀裁定「不弱化」的那一半**（1391 注册 / 1377 残留）。**A1 存活查询桥接**：`HandleData.IsAlive(nint)`（新 Op `HandleDataIsAlive`；`IScriptDomain::IsAlive` 共享实现，LeanCLR 经 `Bridge_Invoke`；**桥接不可用时按「存活」兜底**）。**A2 反查自愈**：5 个注册表命中地址映射后若句柄已死 → `RemoveReference` + 摘映射 → 返回 invalid，调用点「未命中即新建」分支自动重建。**A3 弱化创建点**：native 19 处 + **C# 2 文件**。**实测**：桥接冒烟 `lookup=22/dead=0`；漏改 C# struct 主路径时 `Struct rel` 12→14/20（**暴露 `new T{…}` 才是主创建源**），补上后 **623/536**；**套件中途强制 GC 时合计 3165 注册 / 2854 会话内释放 = 90.2%**（`String 1101/1220`、5 个子族残留清零）、**`addrmap` 1391→117**、`over=0/orphan=0`；**LeanCLR 仍全 0**（BCL 预编译、未定位）；最终无探针回归 **2 × 1704/1704、0 失败、`ensure`/`assert` 0**。**口径变化**：地址映射 = 同一地址恒返回同一**存活**包装，旧包装被回收则重建。**边界**：反查每次多一次桥接往返（未量化性能）；**自愈本轮从未触发**（是安全网，「自愈后值正确」未验证）；container/delegate 不弱化；Mono 未复测。补丁 `Saved/Retest/fix-G1-12-routeA.patch`。详见 [`08-…/07`](08-专项审计/07-G1句柄释放责任归属（2026-09-22）.md) **§5.6** 与 [`09-…/00`](09-问题清单/00-推荐优先修复清单.md) **§2.4.12** || **逐条登记** | [`09-问题清单/00-推荐优先修复清单.md`](09-问题清单/00-推荐优先修复清单.md) 的 **§1.2 处置记录（按修复提交登记）** —— 哪条 finding 由哪个提交修复，以该表为准；**G7 本轮另见同文件 §2.1**（按 `1551383b` 登记，与 §1.2 同口径） |

> **行号口径（重要）**：报告中所有 `文件:行号` 指**分析快照**（`85348c68` 一带）。`492ce5f7`（54 个文件）与 `2113e1e9`（19 个文件）先后改了代码，行号已漂移 ⇒ 核对时请用 `git show 85348c68:<插件内路径>`，不要按当前 HEAD 的行号去对。**G7 本轮（2026-09-16）的改动已压缩提交为 `1551383b`（22 个文件）**；注意 `Script/Weavers/UnrealTypeWeaver.cs`、`Script/SourceGenerator/UnrealTypeSourceGenerator.cs` 另有**工程侧部署副本**（在工程目录、不在插件仓库内 ⇒ **不在该提交内**，需另行同改），因此报告行号与本轮引用的行号都**不等于**当前工作区状态；本轮引用一律写成"函数名 + 邻近代码"或带"本轮改后行号"的说明。
>
> **编号纪律（引用必读）**：存在 **31 处跨报告重号** —— `F-INT2-*` 16 处（`01-…/10b` ↔ `01-…/11`）、`F-PROP2-*` 15 处（`01-…/04b` ↔ `01-…/05`），**同一编号在两份报告里指向完全不同的发现**。引用编号**必须带报告路径**，裸引用一律视为错误。834 条对应 **803 个唯一编号**。
>

## 目录

```
UnrealCSharpAudit/                  ← 本仓库根；各报告的相对链接均以此为基准
├── INDEX.md                        ← 总索引（统一维护）
├── _CONVENTIONS.md                 ← 分析规范（Finding 模板 / 严重度定义 / 死代码判定口径）
├── 00-总览/                        ← 架构总览、执行摘要与 Top 发现、修复优先级评估（含 26 条 P0 逐条处置）
├── 01-UnrealCSharp运行时/           ← Environment/Registry、属性描述符、容器 Helper、绑定模板、Interop 注册…
├── 02-UnrealCSharpCore核心/         ← 反射注册表与模型、动态类型生成、脚本域三后端、类型桥接…
├── 03-代码生成器/                   ← 代码生成框架、类/绑定类生成器、委托结构体枚举与 GameplayTag 生成器…
├── 04-编辑器模块/                   ← 内容浏览器与新建类向导、工具栏/监听器/进度对话框…
├── 05-编译器与跨版本/               ← C# 编译、跨版本宏
├── 06-CSharp运行时与脚本/           ← Interop 桥接与程序集加载、容器与指针包装、引擎实现库、源生成器…
├── 07-构建与配置/                   ← 构建脚本模块依赖与插件配置
├── 08-专项审计/                     ← 死代码与死宏、资源泄漏、UObject 生命周期、线程安全、跨语言 ABI、性能
└── 09-问题清单/                     ← 全局问题总表（逐条汇总）+ 推荐优先修复清单（含 §1.2 处置记录）
```

## 建议阅读顺序

1. [`INDEX.md`](INDEX.md) —— 规模、复核口径、被推翻/存疑结论、系统性问题
2. [`00-总览/02-执行摘要与Top发现.md`](00-总览/02-执行摘要与Top发现.md) —— Top 发现与权威数字表
3. [`00-总览/03-修复优先级评估.md`](00-总览/03-修复优先级评估.md) —— 评分公式、门控规则、26 条 P0 逐条处置
4. [`09-问题清单/00-推荐优先修复清单.md`](09-问题清单/00-推荐优先修复清单.md) —— 一行一条的排期视图 + §1.2 处置记录
5. 具体问题 → 按总表/清单里的"报告路径"进原始报告看代码事实与复核证据

## 免责

本库全部结论为**静态代码阅读 + 引擎源码比对**的产物，**未编译、未运行、未压测**；行号与计数以对应快照为准。据此施工前请先回到源码复核（口径见 `_CONVENTIONS.md` §4「事实纪律」）。
